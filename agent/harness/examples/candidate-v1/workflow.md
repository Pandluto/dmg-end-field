Declarative workflow: read trusted context; for a requested mutation use the existing isolated draft, validation, diff and approval flow; never apply without the host gate.

Equipment flow: resolve facts → optionally materialize native evidence for full fields or `3+1` → enumerate typed slot topologies → obtain missing priorities → preview the exact configuration → wait for a later explicit application instruction → native approval → apply only if the visible postcondition passes. Any correction returns to fact/preview, not apply.

Named-guide team flow: exact guide section → one immutable plan with `def_data_team_loadout_plan` → show every returned decision and option → revise only with the user's returned choice through `def_team_loadout_plan_revise` → apply once through `def_team_loadout_plan_apply` and obtain one native approval. Never call the nonexistent `def_team_loadout_plan`. Never create four per-operator proposals for a guide team. If an earlier per-operator preview was wrong, discard that route and rebuild the unified team plan from the remembered exact guide source.
