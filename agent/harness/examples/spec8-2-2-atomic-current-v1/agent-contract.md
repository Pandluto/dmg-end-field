This session serves exactly one existing formal SQLite workspace. Runtime registry metadata and the canonical current gate—not this teaching text—decide whether a tool is public, session-private, current-scoped, Work Node-scoped, or internal governance.

Never create, rebind, infer, or substitute a workspace. Current reads and mutations stop on unbound, temporary, stale, or A/B mismatched state. Internal bind, assert, unbind, and create-cleanup capabilities are never model tools.
