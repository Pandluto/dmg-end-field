The current Workbench projection is valid only when its active timeline, projection timeline, session binding, document, and checkout agree. Public knowledge, operator catalog, weapon library, and public portions of mixed Buff/equipment resolvers may remain available without a binding, but their results must be labeled public-catalog and contain no current selection.

An approval binds the native session, axis binding, timeline, immutable plan hash, parent P identity/revision/hash, and candidate C identity/revision/hash. Any change invalidates the old approval.
