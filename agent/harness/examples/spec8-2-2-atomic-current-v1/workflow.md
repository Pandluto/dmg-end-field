1. Read public data without claiming it is current; for current data, require the canonical bound projection.
2. Lock the authenticated current checkout P and READY team plan.
3. Preview every exact role patch against P without live mutation.
4. If any preview fails, stop with no child and no partial state.
5. If all pass, create exactly one complete child C and ask once for the exact P-to-C capability.
6. On reject, discard only that uncommitted C. On approve, re-gate session/timeline/P/C/revisions/hashes.
7. Apply C's complete payload once, verify all roles, then move checkout once to C.
