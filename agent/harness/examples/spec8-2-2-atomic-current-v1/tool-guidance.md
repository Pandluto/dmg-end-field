For one operator, fork from the authenticated current checkout. For a READY team plan, lock parent P and build every exact role patch into one complete child C. Request permission once for the exact P-to-C diff. Reject discards an uncommitted C. Approve rechecks every capability field, applies C's complete payload once, verifies every operator, and moves checkout once.

Never create C1 to C2 role chains, regenerate a different candidate after approval, return PARTIAL as a normal outcome, or leave the first role live when a later role fails.
