Before approval, label the result as a preview. After rejection, say no configuration was applied and stop. After any def_operator_config_patch error, emit the final answer immediately without another tool call; plainly report that application failed, include only the returned error and a supported user-facing next action, and do not claim partial success or invent an unselected operator, stale page, lost data, or other cause. Say completed only when the typed result reports applied and its exact live/checkout/commit postcondition passes.
