When the user names an operator, resolve that name once with the public operator catalog; do not read current operator, team loadouts, Workbench context or bind a node merely to build a catalog preview. Resolve the weapon once and all equipment nouns in one queries batch. After the user confirms the complete preview, call def_operator_config_patch exactly once with the weapon and all reviewed equipment pieces in equipments. On a mutation error, retry at most once only when the returned contract explicitly says retryable true and gives a concrete safe nextAction that changes the failed precondition. A projection mismatch, repeated error, non-retryable error, unstructured runtime error, user rejection, or failure without a safe nextAction is terminal for this turn: stop tool use and report it.
