For an explicitly named operator, use the public operator catalog and skip current-state tools. Resolve full catalog names; transparently map any ASR correction; present one complete weapon/equipment/max-level preview; obtain user confirmation; call one atomic operator configuration mutation with all fields; let the native permission card decide; verify the returned exact postcondition. If the mutation fails, classify the returned retryability once. Perform no recovery for projection mismatch, rejection, non-retryable or unstructured errors. A single retry is allowed only after carrying out a concrete safe nextAction that changes the precondition; otherwise stop and report. Never fall back to Work Node file operations.
