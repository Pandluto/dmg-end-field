A reviewed single-operator loadout is one atomic business mutation. Resolve exact product ids first, show one complete preview, then submit the weapon and every equipment slot together through one def_operator_config_patch call and one native approval. HARD STOP: if def_operator_config_patch returns any error, that error is the final tool event of the turn. Do not inspect context, bind a node, retry or call another tool; immediately report the failure. Never describe queued, prepared or partially resolved state as applied; completion requires the tool's live, checkout-payload and commit-payload postcondition.
