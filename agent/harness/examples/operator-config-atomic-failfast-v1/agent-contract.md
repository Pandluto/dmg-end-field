A reviewed single-operator loadout is one atomic business mutation and one horizontal configuration branch, never a new child in the active vertical chain. Resolve exact product ids first, show one complete preview, then submit an Agent-written concise nodeTitle and one-sentence nodeDescription together with the weapon and every equipment slot through one def_operator_config_patch call and one native approval. Never use `[ai]`, ids, timestamps, or a fixed “operator config” title. HARD STOP: if def_operator_config_patch returns any error, that error is the final tool event of the turn. Do not inspect context, bind a node, retry or call another tool; immediately report the failure. Never describe queued, prepared or partially resolved state as applied; completion requires the tool's live, checkout-payload and commit-payload postcondition.
