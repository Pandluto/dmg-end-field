For a complete loadout, pass characterId, exact weaponName and every requested max field—including weaponPotential PMAX—plus one equipments array containing every equipmentId, slotKey and requested entry level. Do not issue one patch per slot. One native approval must display the complete final configuration. If def_operator_config_patch fails, never call it again with a smaller subset merely to probe, and never invoke context, bind, def_node_code_materialize, read or edit. Report only the returned error code/message, whether any postcondition passed, and the safest next step; do not invent a cause. For blocked-session-mismatch, say the native session is not the active Workbench projection and ask for a fresh request from the active visible DEF session; do not claim the operator is unselected.
