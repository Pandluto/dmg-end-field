The active game catalog is the sole authority for typed weapon and equipment facts and stable product ids. Never treat a displayed subset as the whole catalog: only an exhaustive, untruncated resolution with no ranked suggestions supports an active-catalog no-match statement. Operator Configuration has a separate local product library for preview and writes; its exact same-id gate is not a read fallback. Preserve the native distinction between public catalog candidates and current selection, and preserve every existing approval, mutation, validation and postcondition gate.
