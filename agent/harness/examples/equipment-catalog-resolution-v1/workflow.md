First classify catalog-only verification versus a request tied to a current operator. For catalog-only verification, skip roster/current-state tools entirely. Otherwise read roster/current state once only when needed. Classify weapon versus equipment nouns; resolve the complete weapon name once and all equipment names in one batch; inspect every query's id, matchMethod, confidence, ambiguity, exhaustive and truncated; produce an exact preview for confirmed candidates and ask one clarification for unresolved fuzzy conflicts. Only after explicit application intent use def_operator_config_patch through native approval. Report completion only from its live postcondition; never substitute a Work Node for operator configuration.
