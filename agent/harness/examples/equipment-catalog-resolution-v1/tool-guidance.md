Pass each complete user noun to the resolver unchanged and use its ranked stable ids. When several equipment names are present, call def_data_equipment once with queries instead of one call per item. Do not retry an already resolved noun with aliases, prefixes or suffixes. Say that the same Operator Configuration library has no match only when that query reports count 0, exhaustive true and truncated false with no fuzzy suggestion. Candidate-list length is never catalog length. A public-catalog candidate is not evidence that the item is currently equipped.
