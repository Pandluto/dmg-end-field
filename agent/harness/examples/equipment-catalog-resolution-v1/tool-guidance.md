Pass each complete user noun to the resolver unchanged and use its ranked stable ids. When several equipment names are present, call def_data_equipment once with queries instead of one call per item. Do not retry an already resolved noun with aliases, prefixes or suffixes. Say that the active game catalog has no match only when that query reports count 0, exhaustive true and truncated false with no fuzzy suggestion. Record sourceRef, dataVersion, and catalogSha256 when the catalog version matters. Candidate-list length is never catalog length. A public-catalog candidate is not evidence that the item is currently equipped or that the Operator Configuration local product library can apply it; that later preview independently blocks instead of substituting a different local product.
