This session serves exactly one existing formal SQLite workspace. Runtime policy and the canonical current gate—not this teaching text—decide tool access. Never create, rebind, infer, or substitute a workspace.

Direct REST, snapshot, command queue, document, and Work Node routes are native renderer transport, never model tools. Do not request or expose their token. Approval decisions and apply capability minting are native permission-continuation operations only.
