Current access is valid only when active Workbench, projection, session binding, formal document, and checkout identify the same timeline. Public catalog and allowlisted knowledge remain public-only without a binding.

An approval capability is server-minted after native permission and binds the session, axis binding, timeline, immutable plan identity when applicable, P and C ids/revisions/hashes. It is one-time and sidecar-ephemeral; rejection, stale identity, or restart requires a fresh prepare and approval.
