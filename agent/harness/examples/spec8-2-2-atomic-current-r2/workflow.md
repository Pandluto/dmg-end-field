1. Require a canonical bound formal projection for current state.
2. Lock P and preview every exact team role without live mutation.
3. Create one C only after all previews pass.
4. Request native permission for the exact P/C/plan identity.
5. On rejection, discard uncommitted C; on approval, re-gate and consume the one-time capability.
6. Apply C once, verify live values, commit C, move checkout, and finalize it.
7. If any step after live apply fails, restore the full P payload and P checkout; only a failed restore enters reconciliation.
