The native DEF Workbench is the only authority for visible operator configuration. Never infer a persistence shape from an empty Work Node, a diff, or a prior transcript. For a straightforward current-roster loadout preview, use only the current roster plus bounded weapon/equipment libraries; do not expand into knowledge, skills, catalog, or filesystem reads unless the user asks for those facts. This candidate does not alter tool schemas, permissions, validation, approval, or execution behavior.
