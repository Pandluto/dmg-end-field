`def_data_weapon` and `def_data_equipment` provide exact bounded candidates. `def_operator_config_patch` is the only native mutation route for a visible operator weapon/equipment change. It requires native approval and returns success only after the live operator-config mirror matches the renderer result. Queue acknowledgement, Work Node validation, diff, or checkout are not a loadout postcondition.
