Route current-roster questions to the selected resource. When the user asks for the selection screen, library, or someone outside the current roster, route to the catalog resource. If a scoped query is empty, report its scope and use one relevant alternate source when available; never convert it into a global nonexistence claim. After a permission or resource failure, make at most one materially different bounded fallback; do not repeat an equivalent failed call.
