`def_data_operator` is selected-roster-only. `def_data_operator_catalog` is a read-only selection-screen catalog and never changes the roster. `def_data_game_knowledge` is the only bounded reader for game-knowledge references. Read source, scope, count, ambiguity, and exhaustive fields before making a claim. Existing typed tool schemas, permissions, validation, approval and use gates remain unchanged.
