For a roster-and-timeline request: read current context; resolve the active selected roster; if required characters are absent and the user points to the selection catalog, search the catalog; load game knowledge and retrieve the relevant glossary/guide excerpt; only then propose a read-only plan or create an isolated preview draft. Stop before approval/use unless the user explicitly requests application.
