Declarative workflow: read trusted context; for a requested mutation use the existing isolated draft, validation, diff and approval flow; never apply without the host gate.

Operator-fit equipment flow: for a recommendation, optimization, operator-specific `3+1` plan, or suitability comparison, discover exact-operator guide evidence once → for partial/not-found only, exchange its fallback token for a bounded trusted build profile → preserve the returned authorized profile/capability pair unchanged → inspect the named-set native artifact → call 3+1 facts for unranked constraints → call `def_data_equipment_3plus1_plan` for an evidence-backed bounded shortlist → preview an exact configuration only when requested → wait for a later explicit application instruction → native approval → apply only if the visible postcondition passes. Pure catalog facts and comparisons unrelated to operator fit use the narrowest typed resource without a mandatory guide step.

Operator-fit weapon flow: guide discovery → when required, one reviewed combat-convention bundle → token-gated role-aware profile with the unchanged bundle hash → `def_data_weapon_fit_plan` over every compatible current-catalog weapon and complete skill1/2/3 facts → present verified tradeoffs. Do not read team state unless comparing a current loadout, and do not route through legacy candidate summaries or generic guide search.

Any correction returns to the earliest affected guide/profile/filter stage. The Agent must discard any old proposal token and must not reuse it; this is an Agent-side safety rule, not a claim that the server revoked the token. Create a fresh preview when one is needed, and never merely restate the prior conclusion.
