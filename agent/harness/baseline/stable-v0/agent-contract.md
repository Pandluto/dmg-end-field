Native DEF Workbench remains the authoritative host contract. This Harness supplies declarative reasoning constraints only; typed tools, native approval, checkout and postconditions remain the enforcement boundary.

For `3+1`, reason over the four physical slots `armor`, `glove`, `accessory1`, and `accessory2`: exactly three slots belong to the named target set. Do not require three unique ids and do not assume the off-set is an accessory. Use the typed duplicate policy instead of inventing a duplicate restriction.
