# OperatorConfigPage 主界面角色对齐与真实数据渲染 Phase 2 Tasks

## Status

本任务拆分用于推进 `OperatorConfigPage` Phase 2。当前先落框架，后续根据渲染细节讨论继续补充任务。

## Tasks

- [ ] 将 `OperatorConfigPage` 角色 id 集合对齐到主界面当前 1-4 个角色 id。
- [ ] 根据主界面角色 id 从 `operator-draft` localStorage 读取角色完整数据。
- [ ] 从 `sessionStorage['def.selected-characters.v1']` 读取主界面当前角色 `id[]`。
- [ ] 从 `localStorage['def.operator-editor.library.v1']` 读取 `Record<string, OperatorDraft>` 角色库。
- [ ] 梳理本地干员模板编辑器当前单值结构与 `OperatorConfigPage` 渲染需求之间的缺口。
- [ ] 为本地角色模板补上 `1 / 20 / 40 / 60 / 80 / 90` 六档等级维度。
- [ ] 为本地角色基础属性补上等级维度：力量、攻击、敏捷、智识、意志、生命。
- [ ] 将本地角色基础属性等级维度挂到单个属性字段下面。
- [ ] 将每个属性的等级键收敛为 `level1 / level20 / level40 / level60 / level80 / level90 -> number`。
- [ ] 为本地角色技能倍率补上等级维度。
- [ ] 将本地角色技能倍率等级维度挂到每个 skill 的 hit 层。
- [ ] 将每个 hit 的等级键收敛为 `L1-L9 / M1-M3 -> number`。
- [ ] 确认右侧角色切换只在主界面角色集合内切换。
- [ ] 确认 `OperatorConfigPage` 不新增独立角色选择器。
- [ ] 确认主界面角色为空时的页面空态。
- [ ] 确认按当前角色 `id` 读取本页配置缓存。
- [ ] 确认当前角色无缓存时按默认值初始化，并使用 `operator-draft` 读取到的角色数据填充 `character.data`。
- [ ] 梳理角色基础数据区需要渲染的真实字段。
- [ ] 梳理角色等级对前端展示值的影响。
- [ ] 确认角色潜能在 Phase 2 不展示、不生效，留到 Phase 3。
- [ ] 将角色等级支持范围收敛为 `1 / 20 / 40 / 60 / 80 / 90`。
- [ ] 不为 `30 / 50 / 70` 等中间等级补做展示兼容。
- [ ] 页面交互层不开放未支持的中间等级选项。
- [ ] 确认官方角色与本地角色在等级维度结构上的统一消费方式。
- [ ] 梳理武器展示区需要渲染的真实字段。
- [ ] 在武器图片区同层预留区域展示武器等级与攻击力。
- [ ] 当前阶段将武器等级先固定按 `90` 处理。
- [ ] 将武器等级支持范围收敛为仅 `90`。
- [ ] 页面交互层不开放其他武器等级选项。
- [ ] 按当前等级从 `attackGrowth` 读取武器攻击力。
- [ ] 将 `skill1 / skill2` 收敛为单条摘要展示：`name / type / 当前等级值`。
- [ ] 将 `skill3` 整理为多行纯文本展示，并输出到武器区预留大文本区域。
- [ ] 梳理武器等级、潜能、skill 等级对前端展示值的影响。
- [ ] 梳理装备展示区需要渲染的真实字段。
- [ ] 修正护甲位与第一个配件位的页面位置。
- [ ] 让装备 3 个词条框只展示非固定 `effect` 词条。
- [ ] 将装备词条展示格式收敛为 `label / typeKey / 当前档位对应数值`。
- [ ] 为装备词条框补上小字体与换行规则，保证文本不挤占相邻框。
- [ ] 当装备只有 `1` 条或 `2` 条有效词条时，让多余词条框显示 `无`。
- [ ] 将装备词条按钮档位规则调整为 `L0 - L3`。
- [ ] 将装备词条按钮最小 count 调整为 `0`。
- [ ] 将装备词条档位支持范围收敛为 `L0 / L1 / L2 / L3`。
- [ ] 页面交互层不开放其他装备词条档位。
- [ ] 在现有点亮/点灭逻辑基础上复用并小幅修改装备按钮逻辑。
- [ ] 梳理装备词条数量和词条档位对前端展示值的影响。
- [ ] 为 `A / B / E / Q` 占位补上技能详情弹窗入口。
- [ ] 点击普攻占位时展示当前角色所有 `skillType = A` 的 skill。
- [ ] 在技能详情弹窗中按“一个 skill 一个卡片组”组织展示。
- [ ] 在技能详情弹窗中按“一个 hit 一个卡片”展示倍率。
- [ ] 将 hit 卡片尺寸缩小到参考卡片的一半左右。
- [ ] 将 hit 倍率文本格式统一为 `hit: number | 物理 | A`。
- [ ] 按当前技能等级直接读取 `hit.levels.<当前等级键>` 作为倍率值。
- [ ] 将倍率文本里的属性类型读取自 `hit.element`，技能类型读取自 `hit.skillType`。
- [ ] 重构 `operator-draft` 结构以支撑角色等级维度和技能等级维度。
- [ ] 在 `operator-draft` 重构中保留官方角色导入能力。
- [ ] 让官方角色导入后也落入新的 draft 结构。
- [ ] 确认无角色、无武器、无装备时的空态展示。
- [ ] 确认有真实数据时不再展示伪造占位结果。
- [ ] 根据后续讨论补充技能区渲染任务。
- [ ] 根据后续讨论补充面板区渲染任务。
- [ ] 回归验证角色切换、武器展示、装备展示和刷新恢复。
