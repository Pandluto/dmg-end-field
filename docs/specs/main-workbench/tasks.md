# 主界面独立 Tasks

## Status

草案中。

## Tasks

- [ ] 在 Buff 批量编辑界面左区左上角新增 `筛选` 按钮。
- [ ] 为 `筛选` 按钮配置独立 SVG 图标。
- [ ] 点击 `筛选` 按钮进入筛选模式。
- [ ] 筛选模式下 `筛选` 按钮显示高亮态。
- [ ] 筛选模式下禁用其他左上角操作按钮。
- [ ] 筛选模式下禁用技能按钮普通点击选择。
- [ ] 筛选模式下禁用框选模式入口。
- [ ] 筛选模式下禁用取消全部选中入口。
- [ ] 筛选模式下右区切换为 Buff list 目录界面。
- [ ] 从 `def.all-buff-list.v1` 读取所有 Buff。
- [ ] 将每个 Buff 渲染为可选标签。
- [ ] Buff 标签支持点击选中和再次点击取消选中。
- [ ] 保存筛选模式下已选 Buff 标签集合。
- [ ] 根据已选 Buff 标签集合计算匹配技能按钮。
- [ ] 左区自动选中同时拥有已选 Buff 的技能按钮。
- [ ] 左区不拥有已选 Buff 的技能按钮自动取消选中。
- [ ] 筛选结果覆盖当前技能按钮选中集合。
- [ ] 筛选模式不写入 Buff 数据。
- [ ] 筛选模式不改变技能按钮已有 Buff 引用。
- [ ] 再次点击高亮的 `筛选` 按钮退出筛选模式。
- [ ] 退出筛选模式后恢复其他左上角操作按钮。
- [ ] 退出筛选模式后右区返回正常模式界面。
- [ ] 退出筛选模式后保持已选 Buff 标签不变。
- [ ] 在 Buff 批量编辑界面左区左上角新增 `编辑` 按钮。
- [ ] 为 `编辑` 按钮配置独立 SVG 图标。
- [ ] 点击 `编辑` 按钮进入编辑模式。
- [ ] 编辑模式下 `编辑` 按钮显示高亮态。
- [ ] 编辑模式下禁用其他左上角操作按钮。
- [ ] 编辑模式下禁用技能按钮普通点击选择。
- [ ] 编辑模式下禁用框选模式入口。
- [ ] 编辑模式下禁用取消全部选中入口。
- [ ] 编辑模式下禁用筛选模式入口。
- [ ] 编辑模式下保持当前技能按钮选中集合不变。
- [ ] 编辑模式下保持当前已选 Buff 筛选条件不变。
- [ ] 编辑模式下右区切换为编辑模式目录。
- [ ] 右区先展示当前已选技能按钮目录。
- [ ] 当前已选技能按钮目录只展示左区已选中的技能按钮。
- [ ] 计算当前已选技能按钮的共同 Buff。
- [ ] 在右区展示共同 Buff 区域。
- [ ] 共同 Buff 为空时展示空态。
- [ ] 计算当前已选技能按钮涉及的所有 Buff。
- [ ] 在右区展示所有 Buff 区域。
- [ ] 所有 Buff 区域中的重复 Buff 去重展示。
- [ ] 所有 Buff 区域中的 Buff 标签使用白色视觉。
- [ ] 编辑模式本阶段只展示目录，不执行修改、删除或替换。
- [ ] 再次点击高亮的 `编辑` 按钮退出编辑模式。
- [ ] 退出编辑模式后恢复其他左上角操作按钮。
- [ ] 退出编辑模式后右区返回正常模式界面。
- [ ] 退出编辑模式后保持当前技能按钮选中集合不变。
- [ ] 运行 `npm run build`。

## Explicit Non-Tasks

- [ ] 不实现批量删除。
- [ ] 不实现批量替换。
- [ ] 不实现局部修改 Buff 实体。
- [ ] 不修改 Buff 底层 storage 结构。
- [ ] 不改变普通主界面交互。
