# 主界面新一轮 Tasks

## Status

草案中。

## Tasks

- [ ] 使用主界面 Buff 批量操作的深灰色基底作为新界面基底。
- [ ] 保留真右区和假右区。
- [ ] 让新界面轴线基于单元格 `A2` 展开。
- [ ] 从 `A2` 向左展开 `x` 轴。
- [ ] 从 `A2` 向下展开 `y` 轴。
- [ ] 使用非固定灵活布局。
- [ ] 避免强调各组件之间直接依赖或互相关心。
- [ ] 在 `x` 轴上展开一个一个 hit 标签。
- [ ] 让 hit 标签在高度上有间隔。
- [ ] hit 标签选取原界面一样的 hit。
- [ ] 使用透明 flex 容器承载 hit 标签。
- [ ] 让 hit 标签容器可滚动。
- [ ] 在 `x` 轴右手边放置透明 flex 作为 Buff 展区。
- [ ] 在 Buff 展区展示目标抗性。
- [ ] 在 Buff 展区展示已选 Buff。
- [ ] 在 Buff 展区展示已选状态 / 异常。
- [ ] 单个 Buff、状态或异常使用标签展示。
- [ ] Buff、状态或异常标签用小间隔串联。
- [ ] 在 Buff 展区 flex 底部预留 hit 被选中后的微调按钮区域。
- [ ] 使用透明 div 承载微调按钮区域。
- [ ] 微调按钮 CSS 类似现有按钮样式。
- [ ] 在 Buff 展区右侧放置透明 flex。
- [ ] 在 Buff 展区右侧透明 flex 顶部展示总伤。
- [ ] 在 Buff 展区右侧透明 flex 的总伤下面展示计算过程。
