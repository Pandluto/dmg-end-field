# 1.8 LTS 瘦身测试基线

- 基线提交：`920a6f94fc49fad0938a44b61009e349c217b6b8`
- 记录日期：2026-08-02
- 目的：在移动职责和拆分组件前，先把当前 LTS 的用户行为与平台边界固定成独立证据。

## 当前产品边界

2026-08-03 最终确认并实施：Damage Sheet、XLSX 导出和 ExcelJS 全部退役；“计算伤害”PPT 报表与共享伤害计算链保留。初始基线中关于 ExcelJS 体积、XLSX 公式和 Damage Sheet route 的文字是历史测量，不再是有效验收要求。代码提交为 `969dab32`，决策证据见[工作账本第 64 节](../maintenance/v1.8-lts-slimming-worklog.md)。

## 零改动确定性结果

在 `codex/v1.8-lts-slimming` 尚无跟踪文件改动时执行：

| 命令 | 结果 | 证据 |
| --- | --- | --- |
| `npm run check:repo` | 通过 | `tracked=465 syntax=13 data=100 images=559 docs=19` |
| `npm run typecheck` | 通过 | TypeScript 无错误 |
| `npm test` | 通过 | Timeline、计算器、selection、图片路径和兼容测试全部通过 |
| `npm run smoke:timeline-bundle` | 通过 | Timeline Bundle V2 smoke passed |
| `npm run build:web` | 通过 | 215 modules；Atomic app shell `c20d7ecf316354a4`，40 files |
| `npm run check:offline-shell` | 通过 | `ATOMIC_SW_OK`，entry gzip 121609 bytes |
| `npm run audit:dependencies` | 环境阻塞 | npm registry audit endpoint 连续两次 TLS 连接失败；不是代码失败 |

构建同时确认：

- 默认数据包含 30 名干员、75 把武器、13 条共享排轴和 9 个 storage key。
- 100 个数据文件共 5,574,599 bytes。
- 1,379 个图片 URL 完成规范化，353 个唯一图片文件完成校验。
- 初始 LTS 的 ExcelJS 独立 chunk 为 936.59 kB（gzip 269.16 kB）；它后来随最终产品决定删除，此数字只保留作瘦身前对照。

## 当前自动测试已经保护的边界

- 数值：Buff panel、Buff zone、抗性区。
- Timeline：session identity、checkout lifecycle、diff、payload validator。
- Selection workspace policy。
- Web 图片路径兼容。
- 旧 Share Data Timeline payload 兼容。
- Timeline Bundle V2 导出烟测。
- 仓库 manifest/hash 与原子 Service Worker 壳。

## 第一批新增行为基线

本轮在未改动产品实现的 LTS 上增加了三组 Node 合同测试：

- 访问密码、30 天 lease、篡改、过期和非法 JSON；
- Web 数据包/图片包 manifest 的字段、哈希和 fallback；
- App route 与 SkillButton 详情 route 的编码、解码和非法输入回退。
- Buff 旧 schema 迁移、normalize 幂等、倍率/计层、额外伤害段、ID、重排和 row projection。
- Buff 公式栏在 group/item/effect 各可编辑列上的 value、focus、placeholder 与不可变 apply 路由，含缺失目标 no-op 和组名同步 ID。
- Buff 的直接 workbook view projection：10 列 schema、三组表头、group/item 合并、effect A-J 地址、列宽、对齐与 source row 映射。
- Buff 分享的错误分类、逐组 normalize、混合 payload 过滤、覆盖合并和导入后选择策略。
- Buff explorer 的搜索/折叠拖拽门禁、同层级落点约束、三层不可变重排和焦点目标。
- Buff draft/library repository 的空值/坏 JSON 回退、旧 schema 读取、normalize 和 canonical storage key 写入。
- Weapon 的旧/new schema normalize、四类效果 drawer 双向投影、攻击/技能等级插值、sheet row projection、不可变移动与 effect 重编号。
- Weapon workbook 的 7 列 schema、列地址/宽度/对齐、source row 映射、普通 Buff label 与 extraHit 键显示。
- Weapon draft/library repository 的 canonical key、空值/坏 JSON/`null` 回退、旧 schema normalize、外层 key 覆盖 id 和序列化写入。
- Weapon 分享的 current/all payload、parse 错误、旧 schema normalize、外层 key 覆盖 id、覆盖合并和导入选择策略。
- Weapon 公式 binding 的武器/skill/effect/九级单元格值、control/readOnly 语义、skill3 主 value 安全读取与不可变 apply。
- Weapon explorer 的 node key/label 回退、搜索与 value 启动门禁、同层合法落点、active draft key 优先、library fallback 和不可变 effect 重排。
- Weapon 编辑事务的 active/library 解析、首个 effect key 缺口、新建/复制/删除、value 清空、非当前武器来源、深拷贝和 malformed 数据防御。
- 共享 Buff 类型元数据：canonical map 覆盖所有 Equipment type；Buff、Weapon、Equipment、Operator Drawer、Operator Page 与 panel formatter 共用同一入口，六种伤害属性必须满足 `Fragile = 易伤`、`Vulnerability = 脆弱`。Equipment 的五项历史显示差异保持显式 override。
- Equipment 的 ID/旧 schema normalize、percent/raw 存储、two/three-effects catalog、显式 preset与三件套 modifier/extraHit。
- Equipment workbook 的 7 列 schema、row/filter/折叠、地址/值/宽度/对齐/source 映射、CSS row class 和输入不可变性。
- Equipment 单元格 editing 的 set/equipment/fixed/effect/levels/three-piece 字段矩阵、显式 mapping 保护、缺失目标引用语义与源对象不可变性。
- Equipment 分享的 current/all 套装选择、套装名 label、图片字段保留、错误分类、malformed entry 过滤、外层 canonical ID、规范化碰撞、无二次 normalize 的覆盖 merge、输入不可变和选择 fallback。
- Equipment legacy 文件百分比只迁移一次；schema 2 library/set、既有 canonical 浏览器缓存和 schema 2 分享在 stale raw 下保持用户编辑值，OperatorConfig 与后台快照不再二次迁移。
- Equipment durable save 在 deferred flush 后区分 current/superseded revision；保存期间的新 library 不被旧快照替换，只有未变 revision 才能清除 dirty。
- Equipment 通过真实 Image Manager file chooser 导入用户 PNG，在图片公式控件选择、显式保存、刷新预览和分享后只持久化 canonical `assets/images/...`，运行时 blob URL 不进入业务数据。
- Equipment 下游计算中“最后实际存在的主/副能力词条按百分比、此前同类词条按固定值”的 effect1+effect2、effect1+effect3 与三词条组合。
- SkillButton 异常伤害、燃烧持续段和额外伤害的倍率集成 golden，固定倍率加算与乘算的运算顺序、展示公式和最终非暴击伤害。
- Damage Sheet/XLSX/ExcelJS 退役边界：旧页、公开 route、两个入口和 Workbench command 枚举消失；PPT route 与 Toolbar“计算伤害”继续可达。退役前新增的 XLSX 公式 golden 已随产品能力删除，不再作为当前合同。
- OperatorConfig 的 Equipment shared normalization：同一 fixture 经页面模型与后台 selection refresh 后必须保留相同 `fixedStat`、词条类型/等级值、三件套 Buff 和最终面板 totals；非末位主/副能力固定值语义不得漂移。

同时增加 `npm run test:e2e:lts-slimming`。它使用 Playwright 1.62、系统 Chrome、单个隔离 browser context 和 `127.0.0.1:3040`，不会读取开发者真实站点数据。当前覆盖：

1. 错误/正确访问密码、首次安装 30 位干员、75 件武器和 559 个图片资源；
2. Operator、Buff、Weapon、Equipment、图片、Timeline 与 PPT 报表的 lazy route；
3. Buff、Weapon、Equipment、Operator 创建、保存和刷新恢复；Buff 额外覆盖删除项撤回、公式输入聚焦时 Ctrl/Cmd+S、覆盖确认，Weapon 与 Equipment 额外覆盖分享预览/复制/下载/错误/导入，并再次刷新验证 SQLite 恢复；
4. Timeline 选择 4 名干员、拖入技能、刷新恢复与 SkillButton 详情 route；
5. SkillButton 的 `data-liquid-glass-skill`、`data-skill-type` 和三段 SVG outline；
6. Buff 分享预览、剪贴板、JSON 下载、错误反馈、混合 payload 导入和刷新恢复；
7. Buff 拖拽在移动超过 6px 时取消长按、220ms 后进入拖拽、同层组重排并刷新恢复；
8. Weapon skill3 主 value 行不可拖；普通 effect 拖拽在移动超过 6px 时取消长按、220ms 后显示源名称、pointercancel/blur 不提交、pointerup 在同一 effect bucket 内重排，并在 SQLite 批写后刷新恢复；
9. Equipment 通过 Image Manager 导入并选择真实用户图片，保存数据与分享只保留 `assets/images/...`、刷新后预览重新解析；分享导入保留显式套装/装备 ID 与名称，malformed `null` 条目给出受控错误，选中 effect 导出当前时使用所属套装 label。导入后必须经过当前覆盖保护保存，再由 SQLite 在刷新后恢复；
10. 全流程无未处理 page error、console error 或 route alert。
11. BuffBatch 在真实 Timeline 按钮上完成普通选择、mouse drag 框选、批量增加/删减的 pending、取消与确认、按新增 Buff 筛选，并回到排轴保持同一按钮；SkillButton 详情明确由 `TimelineSkillDetailWorkbench` 渲染，旧 `.skill-button-modal-pair` 不进入 DOM。
12. 给真实新增 Buff 注入全局禁用、分段禁用和手工层数后，通过 BuffBatch 确认删除并核对全部持久引用清空；详情保持打开时，外部 Operator panel cache 变化经真实批量抗性 revision 推进后，runtime snapshot 与伤害摘要同步更新。
13. Workbench drawer 不再展示点击无效的“计算侧栏”；技能详情关闭后必须回到 `/timeline`，Canvas 与 SkillSandbox 保持可见，且同一按钮可以再次打开详情。
14. Timeline 不再展示“表格”或“伤害表”入口；“计算伤害”必须实际进入 `/timeline/report/presentation`，PPT“返回”可点击并回到同一 Canvas。
15. Buff、Weapon Skill3、Equipment 三件套必须通过同一个双击 Drawer 修改 type；三个旧公式栏搜索/下拉均不可出现，表格和公式栏只读显示正确的 `中文名 · typeKey`；普通装备词条原有搜索/下拉继续可编辑。

2026-08-03 完成最终退役后的结果为：Node 测试、typecheck、repository check、Timeline smoke、浏览器行为测试、production build 与 atomic offline-shell check 全部通过；production 为 237 modules、36 个离线壳文件，已无 Damage/ExcelJS 大 chunk。

## 最终双分支验收

2026-08-03 使用提交 `0b2bb094` 增加 `npm run test:e2e:lts-dual`，并以固定端口完成真正的同套双目标黑盒测试：

- `http://127.0.0.1:3030`：未瘦身的 `v1.8-LTS`，基线提交 `920a6f94`；
- `http://127.0.0.1:3040`：`codex/v1.8-lts-slimming`；
- 一个 Playwright runner 依次为两端创建全新的隔离 context，不读取开发者浏览器缓存，也不另开第三个端口；
- 两端执行同一套访问密码、首次资料安装、公共 lazy route、Buff/Weapon/Equipment/Operator 创建保存刷新分享、Timeline 选四人/拖技能/刷新恢复、SkillButton 详情、伤害汇总与完整计算过程、PPT 报表、OperatorConfig 和 BuffBatch 流程；
- 公共观察结果整体深比较，包含完整伤害汇总、计算过程与报表总伤害文本；两端 page error、console error 必须同时为空；
- 允许差异固定为六项已确认产品决定的外在表现：旧 Damage Sheet route、XLSX 导出、“表格”入口、“伤害表”入口、失效“计算侧栏”和 Equipment 三件套旧 type 下拉。它们在原 LTS 必须存在，在 slim 必须全部不存在；普通装备 select 仍属于公共观察结果。

最终双跑结果为 `1 passed (18.5s)`。首次改用 `3030` 时，测试通过登录首页指纹立即发现该端口实际指向旧 `core-file-slimming-research` 工作区；纠正为真实 `v1.8-LTS` 后才记录本结果，因此此前使用 `3041` 的预跑不计入终验。

双跑之外，两边各自门禁也重新执行：原 LTS 的 typecheck、Node、repository check、Timeline smoke、production build 和 offline shell 全绿（215 modules、40 shell files，保留旧 ExcelJS chunk）；slim 的同类门禁及完整 `lts-slimming` 浏览器回归全绿（237 modules、36 shell files，无 Damage/ExcelJS chunk）。这次双跑关闭的是“公共前端能力是否随瘦身漂移”的问题；真实 Service Worker 断网恢复、SQLite 导入导出和双标签接管仍属于下方列出的独立平台终验，不以本次通过冒充覆盖。

### 第二阶段编辑器减重补充验收

2026-08-03 在实现提交 `46c062af` 上继续使用同一个 `npm run test:e2e:lts-dual`、同一组 `3030/3040` 服务和逐目标隔离 context，没有另开端口或为 slim 编写特例。公共 observation 新增并验证：

- Buff、Weapon、Equipment、Operator 在五个主题下的根 class、关键 DOM 数量、主题 token、计算样式和关键交互完全相等；Liquid Tide 的直接按钮子节点继续带有相同 surface/preset 标记；
- Weapon 图片选择器可以搜索、选择出预览并清空；
- 除已明确退役的三件套 type select 外，Equipment 当前保留的全部内联 select，其 value、option value、option text 与顺序在两侧逐项相等；三件套旧下拉作为显式 capability 差异单独断言；
- Operator 五个基础下拉保存后刷新不丢失，覆盖保护的取消与确认均可完成；
- 两侧原有保存/分享、Timeline、完整伤害计算、PPT、OperatorConfig、BuffBatch 和零浏览器错误合同继续整体精确比较。

补充双跑结果为 `1 passed (30.7s)`；slim 专用细流程为 `1 passed (18.7s)`。typecheck、全量 Node、repository check、Timeline smoke、production build 与 atomic offline-shell check 同轮全绿。相对实现前的 `7758bb4f`，全站 production JS 减少 6,488 raw bytes、738 gzip bytes，因此本轮既证明功能/主题未漂移，也证明不是单纯把代码搬到新文件。

Buff 第一轮结构迁移后，以上模型合同继续使用同一组输入、输出和断言，并已覆盖迁出的 `buffDraftModel.ts`。迁出模块没有 React、ExcelJS、路由、浏览器全局或存储依赖；页面保存/刷新仍由浏览器流程验证 SQLite-backed adapter，没有改回原生 localStorage。

Weapon model 迁移同样沿用迁移前的 characterization 输入与断言，并改为直接测试 `weaponDraftModel.ts`。新模块没有 React、DOM、浏览器全局、storage、图片库或 ExcelJS 依赖；现有浏览器流程继续负责证明保存/刷新仍走 LTS 的 SQLite-backed adapter。

### 共享 Buff 类型入口退役补充验收

2026-08-04 在三个页面完成重复入口退役后，3040 专用 E2E 用真实双击动作打开同一个 Drawer，修改 Fragile/Vulnerability，再核对只读投影与普通装备保留入口，结果为 `1 passed (19.0s)`。固定 3030/3040 的同套双跑继续比较全部保留行为，并显式断言原 LTS 有、slim 无 Equipment 三件套旧 type 下拉，结果为 `1 passed (30.8s)`；两侧身份分别为 `v1.8-LTS@920a6f94` 与 `codex/v1.8-lts-slimming`。

这项是确认后的有意 UI 差异，不要求 slim 与原 LTS 保持旧下拉数量相等；除此之外，普通装备 select 签名、保存/分享、Timeline、伤害计算、PPT、OperatorConfig、BuffBatch 和浏览器错误合同仍按同一份测试精确比较。

### Buff 类型元数据单一来源补充验收

2026-08-04 在入口退役之后继续把六份重复中文映射收敛到 `core/domain/buffTypeMetadata.ts`。直接模型合同覆盖 canonical label/display、所有 Equipment typeKey、未知/空 key、只读映射，以及 Equipment 五项显式展示 override；各编辑器原有选项、顺序和筛选继续由各自模型合同负责，不把它们合并成通用 schema。

本轮 `npm run check` 全绿并报告 0 vulnerabilities、243 modules、atomic shell `f541daf4c4105278` / 37 files；3040 专用流程为 `1 passed (19.8s)`。固定 3030/3040 的同一份黑盒双跑为 `1 passed (32.8s)`，公共观察结果完整相等。该双跑证明当前可见流程没有因元数据瘦身漂移；它不替代下方列出的并发、失败恢复和平台终验。

## 仍未覆盖的缺口

现有测试不能证明下列页面行为，旧 slim 也只提供了 Electron 时代的人工清单：

- Operator 编辑器的其余复杂字段归一化；五个基础下拉的保存刷新和覆盖保护已经双侧覆盖。Operator 分享往返、用户图片持久引用，以及 Equipment 的核心 normalize/preset/row/workbook、图片选择、分享和 OperatorConfig Equipment 双路径投影已有覆盖。
- SkillButton 的单击/长按、详情内新增与计层、Hit 启停、抗性编辑和异常段；双击详情、重新进入、BuffBatch 删除后的持久 override 清理，以及已打开详情的外部 panel revision 已有浏览器覆盖。
- BuffBatch 的候选来源切换、异常状态候选和多按钮混合集合；普通/框选选择、单按钮批量增删、筛选、pending 与取消恢复已有真实浏览器覆盖。
- `persistentLocalStorage` 在页面拆分后仍全部经过 SQLite-backed adapter，而不是退回原生 localStorage。
- `webImageLibrary` 的跨编辑器引用迁移、图片重命名/删除后的业务引用，以及 Weapon/Operator 的用户图片持久引用；Weapon 图片选择器的搜索、选择预览和清空已经双侧覆盖，Equipment 的真实导入、选择、保存、刷新与分享已有浏览器覆盖。
- SQLite 导出/导入、双标签占用/接管和真实断网后的 Service Worker 恢复；首次数据安装与编辑器刷新恢复已有浏览器覆盖。
- 四个复杂编辑器在五个主题下的结构、关键交互和 Liquid Tide surface 标记已完成双侧精确比较；其余全站页面的多主题完整业务流程，以及 SkillButton 除现有关键 DOM/SVG 标记外的逐主题细节仍未整体覆盖。

## 测试策略

测试数量不是目标。测试必须锁定可观察行为，并与待拆模块的风险对应。

### A. 纯模型合同

优先把当前页面中的纯函数作为 characterization target：

- normalize 后的 schema 与默认值；
- workbook row/cell projection；
- formula binding 与编辑提交；
- share export/import round trip；
- explorer reorder 与 immutable update；
- SkillButton damage view model、Hit/异常段启停和计层派生；
- BuffBatch selection 与 pending add/remove 集合。

这些测试先基于当前 LTS 输入/输出建立，再移动实现。

### B. 浏览器流程

浏览器自动化至少覆盖：

1. 打开每个 lazy route，无白屏和 console error。
2. 各编辑器创建临时条目，保存、刷新、重新打开。
3. 各编辑器分享导出后重新导入，关键字段不变。
4. SkillButton 详情打开、Buff 操作、伤害值与重新进入。
5. 图片库选择用户/官方图片并刷新恢复。
6. SQLite 数据库导出/导入和页面恢复。

测试必须使用隔离浏览器 profile 和临时数据，不读取开发者真实 OPFS/站点数据。

### C. 平台终验

生产构建后在真实 Service Worker 控制下检查：

- 首次资料安装与 SHA-256 校验；
- 刷新后离线启动；
- 双标签写锁与显式接管；
- 失败更新不破坏上一版可用数据；
- SQLite 备份导入后重启恢复。

## 分批门禁

| 批次 | 实施前必须存在的测试 | 实施后门禁 |
| --- | --- | --- |
| 旧 Buff 编辑器删除 | 路由只进入 `BuffDraftSheetPage`；当前 Sheet smoke | typecheck、test、build、浏览器 Buff route |
| SkillButton | 伤害 golden、Buff/Hit persistence、详情 route、关键主题 DOM | 模块测试、全量测试、浏览器详情流程 |
| Buff Sheet | normalize、undo、share、formula、save/reload | 模块测试、全量测试、浏览器编辑流程 |
| Weapon Sheet | normalize、插值、share、图片、save/reload | 模块测试、全量测试、浏览器编辑流程 |
| Equipment Sheet | normalize、preset、share、图片、save/reload | 模块测试、全量测试、浏览器编辑流程 |
| OperatorDraft | schema、排序、library/share、图片 | 模块测试、全量测试、浏览器编辑流程 |
| BuffBatch | selection、filter、pending diff、cancel | 模块测试、全量测试、浏览器批量流程 |
| Damage Sheet / XLSX / ExcelJS | 已确认整体退役；PPT 与共享计算链保留 | E2E 固定入口消失、PPT 点击/返回；typecheck、test、build、offline shell |

## AI 实施约束

- 测试基线提交与实现提交分离。
- 实现阶段不允许为了通过而降低断言、删除失败用例或改写 golden 数据。
- 若当前 LTS 行为本身被确认是缺陷，先单独记录产品决定，再以独立修复提交更新测试。
- Damage Sheet、XLSX 与 ExcelJS 已是明确退役能力；历史 specs、tasks 或旧提交中的 SHALL/待办不得被当作恢复依据。
- 不写行数测试，不用源码字符串匹配代替行为；结构约束使用明确 import boundary 或 repository check。
- 每个模块只在对应快测通过后进入下一模块，最终再跑完整 `npm run check` 与真实浏览器终验。
