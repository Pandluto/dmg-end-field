# 开发修改 1.4.4 - 节点重叠检查bug修复

## 约束
- 本计划仅针对1.4.3防重叠功能失效bug修复
- 不修改谱线组数控制逻辑
- 不修改拖拽核心逻辑
- 复用现有碰撞检测工具函数

## 问题分析

### Bug: 防重叠功能未生效

**表现：**
无论是新拖出的按钮还是已存在按钮的移动，都会重叠到其他按钮上。

**根因：参数类型不匹配**

`isNodeOccupied` 函数接收的是像素x坐标，但调用时传入的是节点索引：

```typescript
// layout.ts
function isNodeOccupied(
  staffIndex: number,
  lineIndex: number,
  nodeIndex: number,  // 注释说是节点索引，但代码比较的是像素坐标
  skillButtons: SkillButton[],
  characterId: string
): boolean {
  return skillButtons.some(
    btn =>
      btn.characterId === characterId &&
      btn.staffIndex === staffIndex &&
      btn.lineIndex === lineIndex &&
      btn.position.x === nodeIndex  // 这里比较的是像素x坐标
  );
}

// snapToNearestNode 调用
isNodeOccupied(staffIndex, lineIndex, checkIndex, ...)
// checkIndex 是节点索引 (0,1,2...39)
// 但函数期望的是像素坐标 (80, 102, 124...)
```

**正确做法：**
在调用 `isNodeOccupied` 前，将节点索引转换为像素坐标：
```javascript
const nodeX = config.marginLeft + checkIndex * nodeSpacing;
isNodeOccupied(staffIndex, lineIndex, nodeX, ...)
```

## 技术方案

修改 `snapToNearestNode` 中调用 `isNodeOccupied` 的方式，将节点索引转换为像素坐标后再传入：

```typescript
if (skillButtons && characterId) {
  for (let offset = 0; offset <= config.nodeCount; offset++) {
    const checkIndex = targetNodeIndex + offset;
    const checkIndexNeg = targetNodeIndex - offset;

    const checkX = config.marginLeft + checkIndex * nodeSpacing;
    const checkXNeg = config.marginLeft + checkIndexNeg * nodeSpacing;

    if (checkIndex < config.nodeCount && !isNodeOccupied(staffIndex, lineIndex, checkX, skillButtons, characterId)) {
      targetNodeIndex = checkIndex;
      break;
    }
    if (checkIndexNeg >= 0 && !isNodeOccupied(staffIndex, lineIndex, checkXNeg, skillButtons, characterId)) {
      targetNodeIndex = checkIndexNeg;
      break;
    }
  }
}
```

## 实现步骤

1. 修改 `layout.ts` 中 `snapToNearestNode` 函数
2. 在调用 `isNodeOccupied` 前转换节点索引为像素坐标
3. 测试验证同干员按钮不能重叠

## 验收标准
- [ ] 相同干员的技能按钮不能吸附到已被占用的节点
- [ ] 自动寻找最近可用节点
- [ ] 拖拽、吸附功能正常
