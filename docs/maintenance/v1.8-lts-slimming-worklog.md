# 1.8 LTS 瘦身工作账本

- 建立日期：2026-08-02
- 实施分支：`codex/v1.8-lts-slimming`
- 产品基线：`v1.8-LTS` / `920a6f94fc49fad0938a44b61009e349c217b6b8`
- 历史参考：`codex/core-file-slimming-research-20260721` / `b4acc204ee49f747e9835143bc7004f8987ee12d`
- 共同祖先：`e090404448d32e63cf8c0c446aa1aca238bd47a1`

## 本轮判断

当前 LTS 是用户行为、浏览器存储、图片、路由、主题和离线交付的唯一事实源。旧 slim 只证明了若干拆分边界可行，不再是可以整体合并或直接复制的实现源。

本轮不把三条历史分支重新合流。实施方式是：在当前 LTS 上先锁定行为，再按旧 slim 的提交证据重新完成模块化；每一批改动独立验证并提交。

确定性结果、自动化缺口和分批门禁见[测试基线](../testing/v1.8-lts-slimming-baseline.md)。

## 当前有效产品决定

2026-08-03 用户再次确认：Damage Sheet、XLSX 导出和 ExcelJS 早已确定全部退役；独立“计算伤害”PPT 报表与共享伤害计算链继续保留。最终证据链为：

1. `68977bc9 refactor(drafts): remove exceljs and split sheet pages` 先删除 XLSX 入口、专用导出器与 ExcelJS；
2. `37809ac8 refactor(damage-sheet): remove legacy worksheet` 随后记录“长期未维护的 Damage Sheet 已不再需要”，删除整页、路由、画布入口和 AI 页面枚举，保留 PPT；
3. 本分支以当前 LTS 为事实源重新实施，并由 `969dab32 refactor(damage-sheet): retire legacy worksheet` 完成。

因此，第 60 节的 ExcelJS KEEP / Damage Sheet MINIMAL SPLIT 是一段已被纠正的中间判断，第 61 节的 XLSX 公式修复只是退役前的过程证据；二者都不是当前需求，不能用于恢复旧功能。

## 提交拓扑

```text
e090404 共同祖先
├─ 16 个独有提交 → b4acc204 旧 slim
└─ 139 个提交    → 920a6f94 当前 v1.8-LTS
```

`git cherry v1.8-LTS codex/core-file-slimming-research-20260721` 显示旧 slim 的 16 个提交全部为 `+`，没有任何提交已被 LTS 以等价补丁吸收。

旧 slim 共改动 62 个文件；共同祖先到 LTS 共改动 1,362 个文件。两边直接重叠 18 个文件：

```text
docs/architecture/evolution.md
docs/architecture/security-boundaries.md
package-lock.json
package.json
src/App.tsx
src/aiCli/aiCliRestAdapter.ts
src/components/BuffBatchEditWorkbench.tsx
src/components/BuffDraftPage.tsx
src/components/CanvasBoard/SkillButton.tsx
src/components/CanvasBoard/index.tsx
src/components/DamageSheetPage.tsx
src/components/EquipmentSheetPage.tsx
src/components/ImageManagerPage.tsx
src/components/OperatorDraftPage.tsx
src/components/WeaponDraftPage.tsx
src/context/AppContext.tsx
src/utils/appRoute.ts
src/utils/mainWorkbenchControl.ts
```

## 本地证据归档

下列目录由 `.gitignore` 中的 `.codex-temp-*` 规则忽略，不进入产品仓库：

```text
.codex-temp-v1.8-lts-slimming/
├─ old-slim-patches/             # 共同祖先到旧 slim 的 16 个 format-patch
└─ archives/
   ├─ branch-reference.bundle    # LTS、旧 slim 与共同祖先 Git 对象
   └─ old-slim-reference-tree.tar.gz
```

旧 slim 最终树中已不存在 `src/exporters/`；ExcelJS 导出器的删除只保存在对应 patch 和 Git bundle 中。当前分支没有复制旧最终树，而是在重新确认产品决定、核对 LTS 新补丁并补足浏览器验收后显式重做。

## 旧 slim 提交账本

| 顺序 | 提交 | 真实作用 | 本轮处理 |
| ---: | --- | --- | --- |
| 1 | `b2185b81` | 记录代码膨胀、运行包和大文件现状 | 归档参考；结论按 LTS 重写 |
| 2 | `d53b659d` | 给出 Electron、AI REST、Canvas、SkillButton 和编辑器的拆分研究 | 归档参考；LTS 不再实施 Electron/AI REST 部分 |
| 3 | `7c7f64a3` | 删除未被路由使用的旧 `BuffDraftPage` 及专用 CSS | 在 LTS 重新确认入口与样式后重做 |
| 4 | `c202338a` | 将 SkillButton 伤害弹窗视图移出主文件 | 先补 SkillButton 行为测试，再按 LTS 当前组件重做 |
| 5 | `bee921d4` | 拆 SkillButton runtime、damage runtime、interaction、info/context menu | 选择性重做；必须保留 LTS 新增主题/SVG/data attribute 与路由行为 |
| 6 | `3144d54f` | 增加 Electron 时代的 SkillButton 人工冒烟手册 | 改写为 Web SQLite/OPFS 与浏览器路由版本 |
| 7 | `68977bc9` | 在一个提交中混合删除 ExcelJS、拆 Buff/Weapon/Equipment、修改 Damage Sheet | 未整体重放；编辑器拆分按 LTS 重做，ExcelJS 删除由最终产品决定单独落实 |
| 8 | `b5a66dea` | 将三个工作表的渲染职责从 controller 移到 view | 在每个页面第一轮拆分后分别重做 |
| 9 | `e79112f0` | 抽 formula、share、explorer drag、image picker | 按 LTS 浏览器 adapter 重做，不能复制旧 transport |
| 10 | `f34930de` | 抽 Damage Sheet projection 与 undo model | 暂停；先决定 LTS 是否继续保留 Damage Sheet |
| 11 | `37809ac8` | 删除 Damage Sheet、路由、入口与 AI 页面枚举，解耦 `WorkbookSheet.css` | 产品决策项；样式解耦可独立实施，功能删除不得夹带 |
| 12 | `5d8f9688` | 拆 OperatorDraft model、library hook 与字段组件 | 重做；使用 `persistentLocalStorage` 与 `webImageLibrary` |
| 13 | `5087af66` | 拆 BuffBatch model、controller 与小型 SkillButton view | 重做；保留 LTS 图片包与主题语义 |
| 14 | `eb0f56ef` | 拆 Equipment 类型、持久化模型和工作表投影 | 重做；LTS 浏览器 SQLite 投影是硬边界 |
| 15 | `2422aa81` | 从 Buff model 抽稳定目录常量 | 在 Buff model 稳定后重做 |
| 16 | `b4acc204` | 从 Weapon model 抽稳定目录常量 | 在 Weapon model 稳定后重做 |

按本轮用途重新归类后，16 个提交不是一条可以顺序重放的迁移链：

| 类型 | 数量 | 提交 | 含义 |
| --- | ---: | --- | --- |
| 在 LTS 上重做 | 6 | `7c7f64a3`、`5d8f9688`、`5087af66`、`eb0f56ef`、`2422aa81`、`b4acc204` | 目标仍成立，但实现必须以 LTS 为源 |
| 选择性重做 | 5 | `c202338a`、`bee921d4`、`68977bc9`、`b5a66dea`、`e79112f0` | 只复用职责边界，拆散混合提交和旧平台 adapter |
| 只作归档 | 3 | `b2185b81`、`d53b659d`、`3144d54f` | 研究或 Electron 时代测试，不进入 Web LTS 实现 |
| 重新做产品决定 | 2 | `f34930de`、`37809ac8` | Damage Sheet、ExcelJS 的保留或删除必须另行验收 |

## 大文件结果核查

下表是共同祖先、当前 LTS 和旧 slim 最终树的行数。它只用于判断旧分支是否真的完成了职责拆分，不作为重构验收指标：

| 文件 | 共同祖先 | 当前 LTS | 旧 slim |
| --- | ---: | ---: | ---: |
| `BuffDraftPage.tsx` | 5,063 | 5,064 | 8 |
| `WeaponDraftPage.tsx` | 3,733 | 3,734 | 8 |
| `EquipmentSheetPage.tsx` | 3,277 | 3,249 | 8 |
| `OperatorDraftPage.tsx` | 2,280 | 2,281 | 1,330 |
| `BuffBatchEditWorkbench.tsx` | 1,997 | 2,006 | 784 |
| `CanvasBoard/SkillButton.tsx` | 2,650 | 2,723 | 851 |
| `CanvasBoard/index.tsx` | 4,749 | 4,679 | 4,744 |
| `DamageSheetPage.tsx` | 2,646 | 2,650 | 已删除 |

旧 slim 完成了三个工作表外壳的迁出，但没有实质瘦下 `CanvasBoard`，`OperatorDraft` 和 `BuffBatch` 也只完成了一部分。其 `useSkillButtonRuntime` 一次返回约 90 个字段，把原组件复杂度集中成新的总控 hook；本轮不会照搬这种拆法。

## 旧实现不能直接信任的代码证据

旧 slim 的拆分模块仍绑定桌面时代依赖：

- `useWeaponDraftPageController.tsx`、`useEquipmentImagePicker.ts`、`operatorDraftPageModel.ts` 等依赖已从 LTS 删除的 `imageBridge`。
- 多个 model/controller 直接读写 `window.localStorage`；当前 LTS 使用 SQLite-backed `persistentLocalStorage`，并在启动时统一 hydrate/flush。
- 旧 SkillButton 冒烟手册要求 Electron SQLite transport；当前 LTS 的 Timeline 与 Work Node 已迁入浏览器 SQLite。
- 旧 slim 没有 LTS 后续的 route lazy loading、Web App shell、Web Locks、PWA、浏览器图片库和主题运行时。
- 旧 slim 的 Damage Sheet/ExcelJS 删除是产品取舍，不是行为等价的纯重构。

因此，旧代码只能用于识别职责边界、函数集合和拆分顺序；任何复制到 LTS 的实现都必须重新接入当前平台层并由当前测试证明。

## LTS 补丁保护点

| LTS 提交 | 涉及区域 | 瘦身时必须保护的行为 |
| --- | --- | --- |
| `8bd5d3e5` | Canvas、AppContext、mainWorkbenchControl | roster selection 与 Work Node 协调 |
| `9374fbf3` | App、Canvas、context、routes、package | 删除真实 Agent/MCP runtime 后的 Web 产品边界 |
| `192b1464` | package、routes、Web runtime | 浏览器启动与平台适配 |
| `b603cba6` | App、五个编辑页、Canvas 控制 | Web workspace navigation |
| `9fc3dd65` | Canvas | 浏览器内 Timeline 命名流程 |
| `342416ab` | Equipment、BuffBatch、package | 浏览器图片包与用户图片存储 |
| `e1965ff2` | Equipment、Weapon、package | 最终移除 desktop runtime |
| `ba276f72` | AppContext、package | Web 本地数据包恢复链 |
| `0414e72f` | ImageManager、Operator、Weapon、Equipment | `webImageLibrary` 与分享图片保留 |
| `e7757f9f` | Timeline repository | Timeline 数据必须继续落在浏览器 SQLite |
| `6a60b1de` | Timeline archive | 旧排轴恢复必须保持原子性 |
| `517714cf`、`d5820654` | Service Worker、离线合同 | 原子离线壳和失败回退不能被结构调整破坏 |
| `b45ae2b5` | Sites worker、图片包 | Web 发布时图片包继续通过站点 worker 流式传输 |
| `9aea09d7` | Damage Sheet | 倍率 Buff 对基础技能倍率的修复 |
| `64ca4b1e`、`f13f5ebc`、`1d49de11` | 全局主题 | Apple、Lieflat Mono、Paper Ink 三套主题语义 |
| `1e6d8b3b`、`6f21374d`、`99f377d0`、`67e35603`、`51dc3eb2` | SkillButton | paper/liquid 主题轮廓、data attribute 与 SVG 结构 |
| `97361e00` | App | 路由动态加载和空闲预加载 |
| `0401deae` | App | 可选主题失败不阻塞启动 |

## 补丁可应用性实验

在未修改的 `v1.8-LTS@920a6f94` 上：

- 普通 `git apply --check` 仅对文档提交、SkillButton 第一刀和两份手册通过。
- `git apply --3way --check` 对前 6 个提交通过。
- 从 `68977bc9` 开始到 `b4acc204` 均产生三方冲突。

“可以三方应用”不代表可以跳过行为测试。尤其 SkillButton 在 LTS 后续增加了主题 DOM/SVG 语义，只能把 clean apply 当作定位线索。

## 新实施顺序（初始计划）

> 第 7–8 项记录开工时的保护性假设，已由 2026-08-03 的最终产品决定替代；实际完成结果见第 64 节。

1. 固定 LTS 确定性基线，并补浏览器行为测试骨架。
2. 删除仍不可达的旧 Buff 编辑器；不同时改当前 Buff Sheet。
3. 先从当前 Buff、Weapon 页面抽稳定目录常量；目录是纯数据边界，应早于 controller/view 拆分。
4. 将 Buff、Weapon、Equipment 分成三个独立迁移批次；每页依次抽纯 model、持久化 adapter、controller 和 view，再抽 share/formula/explorer。
5. 拆 OperatorDraft，再拆 BuffBatch；两者分别保留浏览器图片库和批量选择语义。
6. 以当前 LTS 为源拆 SkillButton，保护主题 DOM、详情路由和 Timeline SQLite 持久化；不复制旧分支的总控 runtime hook。
7. 在功能继续保留的前提下，按需拆 Damage Sheet 的 projection/undo；这一轮不得删除页面或 ExcelJS。
8. 单独做 Damage Sheet/ExcelJS 产品决定；只有所有导出消费方归零并通过对应验收后才能移除 ExcelJS。
9. 最后评估旧 slim 未完成的 CanvasBoard 和 OperatorConfig。

## 提交与工作留痕规则

- 测试先在未改实现的 LTS 上通过，再提交为行为基线。
- 重构提交原则上不得同时放宽或改写对应行为测试。
- 每个实现提交在正文中记录：

```text
Old-Slim-Reference: <commit>
LTS-Guard: <commit[, commit...]>
Behavior-Tests: <paths or commands>
```

- 每完成一批更新本账本：实际变更、偏离旧实现的原因、验证命令、剩余风险。
- 旧 slim、main 或 LTS 出现后续补丁时，先记录其目标与影响文件，再决定是否同步；不做无记录的顺手合并。

## 已完成批次

### 1. 删除不可达旧 Buff 编辑器

- 历史参考：`7c7f64a3 refactor(buff): remove unused legacy editor`。
- 入口证据：`App.tsx` 只懒加载 `BuffDraftSheetPage`；仓库中没有 `BuffDraftPage` 的 import、渲染或路由。
- 实际改动：删除旧 `BuffDraftPage` 组件、只供它使用的 helper/import 和 194 行专用样式，共减少 1,501 行 TSX 与 194 行 CSS。
- LTS 偏离：旧提交发生在主题系统之前；本轮额外清理 Apple/Lieflat/Liquid 主题中两个已经不可达的 `.buff-draft-page` selector，但保留当前 `.buff-sheet-page` 主题规则。
- 保留边界：当前 Sheet-Buff、SQLite-backed `persistentLocalStorage`、分享、ExcelJS 导出以及 Weapon/Equipment 共用样式均未改变。
- 验证：`npm run typecheck`、`npm test`、`npm run check:repo`、`npm run smoke:timeline-bundle`、`npm run test:e2e:lts-slimming`、`npm run build:web`、`npm run check:offline-shell`。
- 构建结果：Buff lazy chunk 从 94.66 kB（gzip 23.86 kB）降至 62.00 kB（gzip 17.15 kB）；共享 Buff CSS 从 21.83 kB（gzip 3.85 kB）降至 18.74 kB（gzip 3.37 kB）。
- 剩余风险：当前 Sheet-Buff 主文件仍然很大；本批只移除不可达代码，没有开始搬动可达职责。

### 2. 抽取 Buff 与 Weapon 稳定目录

- 历史参考：`2422aa81 refactor(buff-sheet): extract buff catalog`、`b4acc204 refactor(weapon-sheet): extract weapon catalog`。
- 实际改动：新增 238 行 `buffDraftCatalog.ts` 和 160 行 `weaponDraftCatalog.ts`；页面只改为显式导入，不移动 controller、持久化、图片、公式或视图逻辑。
- 完整性证据：两个新文件的 SHA-256 分别与旧 slim 最终树中的对应 catalog 完全一致；这证明搬迁没有改变选项顺序、label、映射、默认值或类型。
- LTS 偏离：旧分支从已拆出的 page model 转导出 catalog；当前 LTS 尚未建立 model，因此页面直接导入。Weapon 的 `EFFECT_CATEGORY_OPTIONS` 仍留在页面，因为它不是旧提交认定的稳定目录边界。
- 平台边界：新文件都是纯类型、常量和纯归一化函数，不依赖 `imageBridge`、原生 `window.localStorage`、SQLite 或 Electron。
- 验证：`npm run typecheck`、`npm test`、`npm run check:repo`、`npm run smoke:timeline-bundle`、`npm run test:e2e:lts-slimming`、`npm run build:web`、`npm run check:offline-shell`。
- 剩余风险：catalog 本身已经稳定，但两个页面的 schema normalize、持久化和 workbook projection 仍混在主文件中，下一批必须先为这些纯模型补合同测试。

### 3. 固定 Buff 模型行为并修复归一化幂等

- 触发方式：新增旧 `buffs` schema、倍率/计层、额外伤害段、ID、不可变重排和 row projection 的 characterization test。
- 基线缺陷：`15%法术脆弱` 再次 normalize 后会变成 `15%`，与原代码“必须幂等”的注释和用户显示名保留目标冲突。
- 根因：重复数值清理正则嵌入外层分组后仍使用 `\\1`，实际反向引用了可为空的分隔符分组，把单个数值误判为重复值。
- 修复：重复 token 改为捕获组 2 并使用 `\\2`；只修正分组引用，不改变正常自动命名、旧类型迁移或数值格式。
- 验证：新增测试证明旧 schema 一次迁移后再次 normalize 完全不变；全量 Node、浏览器、build 与离线壳门禁通过。
- 留痕判断：这是测试在当前 LTS 上发现的独立缺陷，不属于旧 slim 的结构改动，因此单独提交后再开始 model 搬迁。

### 4. 从当前 LTS 抽取 Buff 纯模型

- 历史参考：`68977bc9 refactor(sheet-editors): split models and remove legacy xlsx exporter` 中的职责边界，以及后续 `2422aa81` 的 catalog 边界；未重放这两个提交，也未复制其最终 model。
- 实际改动：从当前 `BuffDraftPage.tsx` 原样迁出 draft/effect/item 类型、默认值、旧 schema normalize、倍率与计层更新、不可变重排、导入解析、row projection 和 explorer 状态计算，形成 741 行 `buffDraftModel.ts`；页面由 3,386 行降至 2,663 行。
- LTS-native 边界：纯模型不依赖 React、ExcelJS、路由、`window` 或任何 storage adapter。SQLite-backed `persistentLocalStorage`、撤销快照、分享文件、workbook projection 和页面 controller/view 仍留在现有 LTS 侧，避免带回旧 slim 的原生 `window.localStorage` 实现。
- 行为约束：上一提交建立的 `buffDraftModel.test.ts` 未放宽断言；页面暂时重导出测试目标以保持已有调用边界，后续消费者稳定后再直接指向 model。
- LTS 保护点：保留 `b603cba6` 的 Web workspace navigation、`342416ab` 的浏览器资源包边界和 `0414e72f` 的 Web 图片/分享平台实现；本批不触碰 Damage Sheet 与 ExcelJS 产品能力。
- 验证：`npm run typecheck`、`npm test`、`npm run check:repo`、`npm run smoke:timeline-bundle`、`npm run test:e2e:lts-slimming`、`npm run build:web`、`npm run check:offline-shell` 全部通过；浏览器流程继续证明 Buff 保存/刷新和其余 LTS 关键路由正常。
- 剩余风险：Buff 页面仍同时承担存储、undo、share、ExcelJS projection、controller 和 view；下一刀应先为这些边界补合同，再分别抽 adapter/controller/view，不能为了追求文件变小复制旧分支的大总控 hook。

### 5. 抽取 Buff 公式文本绑定

- 历史参考：`e79112f0 refactor(sheet-editors): extract auxiliary hooks and helpers` 中 121 行 `buffDraftFormula.ts` 的职责切口；本轮以当前 LTS 的 `formulaTextBinding` 为源重新实现，不复制旧 controller 或 model。
- 实际改动：新增 131 行纯 `buffDraftFormula.ts`，集中决定 group/item/effect 在不同列选中时的 value、focus id、placeholder 与 commit 目标；页面保留 `useMemo`、公式编辑器、焦点恢复和保存编排，只从 2,663 行降至 2,584 行。
- 行为合同：新增 146 行测试，覆盖 group 的 ID/名称/描述、item 的 ID/名称/描述、effect 的显示名/条件/描述，以及缺少 summary/item/effect 时返回 null；每条 commit 都验证写入了正确实体和字段。
- LTS-native 边界：新模块只依赖 `buffDraftModel` 类型，不依赖 React、DOM、ExcelJS、路由或存储；因此不会绕过 SQLite，也没有复刻旧 slim 的 93 字段 controller/view props 管道。
- 浏览器证据：现有 E2E 通过公式栏中的“组名称”输入修改 Buff，保存到浏览器 SQLite 后刷新恢复，继续覆盖实际页面接线。
- 验证：`npm run typecheck`、`npm test`、`npm run check:repo`、`npm run smoke:timeline-bundle`、`npm run test:e2e:lts-slimming`、`npm run build:web`、`npm run check:offline-shell` 全部通过。
- 下一刀：先锁定并迁出当前 ExcelJS workbook projection；旧 slim 已删除 ExcelJS 并改成手写数组，只能参考职责名称，不能参考实现。

### 6. 固定并迁出 Buff ExcelJS workbook projection

- 先行合同：`2de5a00f test(buff): characterize ExcelJS workbook projection` 在实现未移动时固定 10 列 schema、索引/效果区/文本区三组表头、group/item 合并、effect A-J 地址、列宽、对齐和 source row 映射。
- 历史参考与拒绝项：旧 `68977bc9` 同时删除 legacy exporter/ExcelJS 并重写工作表，后续旧 `buffDraftPageModel.tsx` 使用手写数组模拟投影；本轮只参考“projection 应独立”的边界，拒绝其产品删除和替代实现。
- 实际改动：把当前 LTS 的 292 行 ExcelJS workbook 构造、merge map、cell text/alignment 映射和 view projection 原样迁入 `buffDraftWorkbook.ts`；页面保留 React cell renderer，从 2,584 行降至 2,308 行。
- 能力保留：新模块仍执行 `new ExcelJS.Workbook()`，production build 中 `exceljs.min` chunk 仍为 936.59 kB（gzip 269.16 kB）；Damage Sheet 和工作表能力没有被悄悄删除。
- 边界证明：projection 模块只依赖 ExcelJS 和 `BuffSheetRow`，不依赖 React、DOM、路由或 storage；characterization test 在迁移前后使用同一断言并全部通过。
- 验证：`npm run typecheck`、`npm test`、`npm run check:repo`、`npm run smoke:timeline-bundle`、`npm run test:e2e:lts-slimming`、`npm run build:web`、`npm run check:offline-shell` 全部通过。
- 下一刀：为 undo repository 注入假存储建立合同，再把快照读写/恢复迁出；生产默认必须继续绑定 `persistentLocalStorage`，组件内 UI 恢复编排暂不移动。

### 7. 抽取 SQLite 绑定的 Buff undo repository

- 历史参考：旧 `buffDraftPageModel.tsx` 把 undo 与 model 混放，并在 `read/write/capture/restore` 中直接使用 `window.localStorage`；本轮只沿用快照字段和最多 8 条的用户行为，不采用旧 transport。
- 实际改动：新增 117 行 `buffDraftUndo.ts`，把三个既有 storage key、快照类型、时间标签、读写、捕获和消费式恢复集中为显式注入 storage 的 repository；`BuffDraftPage.tsx` 在模块边界处明确传入 `persistentLocalStorage`，UI 选中项/焦点恢复仍留在页面。
- 单元合同：109 行内存存储测试覆盖 key 不变、坏 JSON/非数组回退、draftState 深拷贝、两个被保护键的 null/value 快照、8 条上限、缺失 ID、null 对应 remove、恢复后消费目标但保留其他快照，以及时间标签。
- 浏览器合同：扩展 3040 隔离流程，在已保存 Buff 组中创建项、删除项、从“撤回”菜单恢复，再刷新页面；恢复项在刷新后仍存在，证明 repository 继续通过 SQLite-backed 同步镜像持久化。
- LTS-native 证明：新 repository 不导入 React、DOM、`window.localStorage` 或平台单例；生产 adapter 由页面显式注入，无法无意退回旧 slim 的原生存储。
- 页面结果：撤销存储职责迁出后主文件由 2,308 行降至 2,216 行；没有移动 `withUndo`、恢复后的 React state 编排或其他编辑器逻辑。
- 验证：`npm run typecheck`、`npm test`、`npm run check:repo`、`npm run smoke:timeline-bundle`、扩展后的 `npm run test:e2e:lts-slimming`、`npm run build:web`、`npm run check:offline-shell` 全部通过。
- 下一刀：先固定 Buff 分享 parser/merge 的纯合同，再抽分享状态；新实现必须显式接收当前 library 和 repository 写入回调，不能像旧 hook 在 `useMemo` 中偷偷读存储。

### 8. 固定并抽取 Buff 分享数据边界

- 历史参考：旧 `e79112f0` 的 `useBuffDraftShare.ts` 同时管理 parser、DOM 下载、modal state 和存储，而且导出 `useMemo` 偷读存储、确认导入直接写 `window.localStorage`；本轮没有复制该 hook。
- 实际改动：新增 78 行纯 `buffDraftShare.ts`，集中固定 `buff-library-share.v1`、导出包装、逐组 parse/normalize、错误分类、覆盖合并和导入后选中策略；页面继续负责下载、剪贴板、文件 input 与 modal state。
- 显式数据流：导出预览与下载现在接收 React `localLibrary`，确认导入也以当前 library 合并后显式写入 `persistentLocalStorage`；不再在 `useMemo` 或 handler 内暗中重读 storage。
- 单元合同：101 行测试覆盖错误 JSON、错 share type、数组 payload、全无效 payload、混合有效/无效 payload、label/schema 归一化、同 key 覆盖合并，以及保留当前选择/优先导入项/空库回退。
- 浏览器合同：3040 隔离流程读取并校验导出预览、验证剪贴板内容、触发真实 JSON 下载、显示错类型错误、导入混合 payload、确认仅 1 个有效分组，并在刷新后验证导入组仍由 SQLite 恢复。
- 页面结果：分享数据职责迁出后主文件由 2,216 行降至 2,195 行；这一刀刻意不把 modal 状态搬成高参数 hook，先用行为证据建立安全边界。
- 验证：`npm run typecheck`、`npm test`、`npm run check:repo`、`npm run smoke:timeline-bundle`、扩展后的 `npm run test:e2e:lts-slimming`、`npm run build:web`、`npm run check:offline-shell` 全部通过。
- 下一刀：将 explorer drag 的可拖动/合法落点/重排计算先抽成纯 policy 并测试；220ms 长按和全局 pointer listener 在浏览器合同建立前不移动。

### 9. 固定并抽取 Buff explorer drag policy

- 历史参考：旧 `e79112f0` 把规则、React state、220ms timer、DOM 命中和全局 listener 一次性搬进 297 行 hook；本轮先只抽可穷举验证的规则和不可变重排。
- 实际改动：新增 149 行纯 `buffExplorerDragPolicy.ts`，集中 node key/label、搜索时禁拖、组/项折叠前置条件、同层级合法落点，以及 draft/item/effect 三层不可变重排与焦点 row key。
- 单元合同：97 行测试覆盖缺失 label 回退、搜索/折叠门禁、空/自身/跨类型/跨组/跨项落点、三层顺序结果、焦点目标、缺失节点和源数据不变。
- 页面接线：页面用 memoized policy state 调用纯规则，成功重排后仍经过既有 `persistLibraryState` 写入 SQLite；主文件由 2,195 行降至 2,128 行。
- 风险隔离：220ms 长按、移动超过 6px 取消、click suppression、DOM `elementFromPoint` 和 pointerup/pointercancel/blur listener 完全未移动；现有全量浏览器流程继续通过，但这些手势还需要专门 E2E 后才能抽 hook。
- 验证：`npm run typecheck`、`npm test`、`npm run check:repo`、`npm run smoke:timeline-bundle`、`npm run test:e2e:lts-slimming`、`npm run build:web`、`npm run check:offline-shell` 全部通过。
- 下一刀：增加长按取消与真实同层级拖拽/刷新恢复浏览器合同，再迁出 pointer lifecycle；之后才评估剩余 controller/view 是否值得继续拆。

### 10. 在真实手势合同下迁出 Buff pointer lifecycle

- 先行合同：`0b08e2b8 test(buff): cover explorer pointer lifecycle` 在旧内联实现上验证移动超过 6px 会取消、按住超过 220ms 显示 preview、拖到同层组后重排，并在刷新后保持 SQLite 顺序。
- 实际改动：新增 171 行 `useBuffExplorerDrag.ts`，迁出 timer、click suppression、DOM node 解析、pointermove/pointerup/pointercancel/blur listener 和 drag state；主页面只保留 policy state 与合法重排后的业务提交。
- 接口收敛：新 hook 只有三个输入（policy state、reorder callback、drag-start callback）和四个输出（drag state、click suppression、can-start、pointer-down），不接收 library、selection、storage 或十个页面 setter。
- 与旧 slim 的差异：旧 hook 同时接收 10 个业务参数并自行持久化；新 hook 不知道 Buff library 结构，业务重排继续经过已测试的纯 policy 和页面 `persistLibraryState`，避免形成第二个 controller。
- 回归证据：同一条 pointer E2E 在迁移前后原样通过；页面由 2,128 行降至 2,002 行，未改变 220ms、6px、listener capture 或 blur finalize 语义。
- 验证：`npm run typecheck`、`npm test`、`npm run check:repo`、`npm run smoke:timeline-bundle`、`npm run test:e2e:lts-slimming`、`npm run build:web`、`npm run check:offline-shell` 全部通过。
- 下一刀：盘点剩余 2,002 行，优先抽明确 persistence adapter 与展示 helper；只有 view/controller 接口能按领域分组而不是复制 90+ 字段 props 时，才继续拆页面外壳。

### 11. 收口 Buff draft/library persistence adapter

- 历史风险：旧 slim 的 model/controller/share 多处直接使用 `window.localStorage`；此前当前页面虽已改用 `persistentLocalStorage`，但读写和两个 key 仍散落在初始化、保存、导入、删除、undo 恢复等路径。
- 实际改动：新增 63 行 `buffDraftPersistence.ts`，以注入 storage 的 repository 统一 load draft、load library、save draft 和 save library；undo repository 改为复用同一组 canonical key 并继续向后重导出。
- 平台接线：`BuffDraftPage.tsx` 只在模块顶部将 `persistentLocalStorage` 注入 draft 与 undo 两个 repository；组件内部已没有 storage key、`getItem`、`setItem` 或平台单例调用。
- 单元合同：75 行内存测试覆盖空存储、坏 JSON、旧 `buffs` schema、library 外层 key 保留、normalize 以及两个既有 key 的序列化写入；原 undo repository 合同继续通过。
- 页面结果：移除两段 load helper 和散落写入后主文件由 2,002 行降至 1,965 行；分享、撤回、保存、删除和拖拽的浏览器 SQLite 流程全部原样通过。
- 验证：`npm run typecheck`、`npm test`、`npm run check:repo`、`npm run smoke:timeline-bundle`、`npm run test:e2e:lts-slimming`、`npm run build:web`、`npm run check:offline-shell` 全部通过。
- 下一刀：由复核判断剩余 controller/view 是否还有高收益、低耦合切口；若只能形成大 props 搬运，则结束 Buff 第一阶段并转入 Weapon，以免把复杂度改名而不是降低。

### 12. 统一 Buff 公式输入的唯一 apply 路径并停止继续拆分

- 复核结论：当前 Buff 已形成合理的 LTS-native 边界；继续拆 controller/view 会把 JSX 直接依赖的 71 个局部变量重新包装成大 props 管道。唯一仍有高收益的切口，是公式输入存在两套字段映射。
- 根因：`buffDraftFormula.ts` 负责 blur/Enter 的字段提交，页面另有 77 行 `buildDraftWithFormulaTextInput` 负责输入框尚未失焦时保存；普通保存与覆盖确认因此可能和公式 binding 漂移。
- 实际改动：`14e23834` 将 binding 收敛为纯、不可变 `apply(draft, value)`，blur、Enter、聚焦状态下 Ctrl/Cmd+S 和覆盖确认全部复用同一路径；页面由 1,965 行降至 1,827 行。
- 单元合同：覆盖 group/item/effect 的全部文本列、组改名同步生成 ID、缺失 item/effect 安全 no-op 和源 draft 不变；model/workbook 测试也改为直接导入对应模块，不再经过页面测试专用重导出。
- 浏览器合同：3040 隔离流程在公式输入仍保持焦点时触发 Ctrl/Cmd+S，刷新后由 SQLite 恢复新值；另行验证覆盖确认保存同一未提交公式值。
- 验证：提交自身及后续统一门禁的 `typecheck`、全量 Node、repository check、Timeline smoke、3040 E2E、production build 和 atomic offline-shell check 全部通过。
- 停刀决定：Buff 第一阶段到此结束。除非出现实际需求、至少两个消费者共享同一能力，或能形成约 8–12 个领域化输入以内的独立组件，否则不再为了行数继续拆。

### 13. 固定当前 LTS 的 Weapon 纯模型合同

- 历史参考：旧 `68977bc9` 曾把 Weapon model 与页面一起拆出，旧 `b4acc204` 又补齐 catalog；本轮只用它们核对职责清单，测试目标仍是当前 `v1.8-LTS` 页面里的真实函数。
- 先行合同：新增 `weaponDraftModel.test.ts`，覆盖空白 draft/skill/九级数据、中文 ID 与自定义 ID、skill1/skill2 自动 Buff 类型、旧 `effectTypes/effectCategories + levels.passive/effects` 迁移、新 schema normalize 幂等，以及无效等级键过滤。
- 效果边界：同一合同分别固定 fixed、multiplier、derived、extraHit 四类效果从九级数值投影到 Buff drawer、再从 drawer 回写等级表的行为。
- 表格与编辑边界：测试同时固定攻击成长里程碑插值、技能等级特殊坐标、skill3 effect 插值、sheet row 顺序、record 移动、effect 连续重编号、行内等级地址解析和所有不可变更新语义。
- 当前结论：characterization 在现有实现上通过，没有为了拆分改业务；英文名称中的空格当前会被 pinyin 归一化为 `testweapon`，旧 schema 的 effect 顺序以 category key 先出现，这两点已按真实 LTS 行为记录而未擅自修正。
- 暂时改动：页面只给被测试的类型/纯函数增加 export；storage、`webImageLibrary`、图片 URL、DOM 键盘事件、React state 和分享流程没有纳入 model 出口。
- 快速验证：`node scripts/run-ts-test.mjs /src/components/weaponDraftModel.test.ts` 与 `npm run typecheck` 通过。
- 下一刀：把这些已锁定的纯类型与函数从当前页面原样迁入新的 LTS-native `weaponDraftModel.ts`；迁移后的同一测试不得放宽，storage 和图片平台逻辑继续留在页面边界。

### 14. 从当前 LTS 迁出 Weapon 纯模型与 Sheet row projection

- 历史参考：旧 `68977bc9` 提供过 Weapon model 的职责目录，但其最终文件混入 `imageBridge`、路由和平台逻辑；本轮没有复制旧文件，而是从已由 `965b1c46` 锁定的当前 LTS 实现逐段迁移。
- 实际改动：新增 897 行 `weaponDraftModel.ts`，集中 draft/skill/effect schema、旧格式迁移、默认值、四类效果 drawer 双向投影、攻击与技能等级插值、搜索文本、Sheet row projection 和不可变重排；页面由 3,590 行降至 2,796 行。
- 纯边界：新 model 只依赖 pinyin、Buff 领域归一化和 Weapon catalog，不导入 React、DOM、`window`、storage、`webImageLibrary`、图片资产类型或 ExcelJS；浏览器图片 URL 构造仍留在页面平台边界。
- 行为约束：`weaponDraftModel.test.ts` 改为直接导入新 model，同一组旧/new schema、幂等、四类效果、插值、row 顺序和不可变重排断言在迁移后原样通过。
- 页面保留：SQLite-backed draft/library 读写、分享文件、图片库、键盘事件、workbook cell projection、拖拽生命周期、React controller/view 都没有随 model 迁移。
- 下一刀：先为现有 workbook columns/cells/address/effect display projection 建立独立合同，再迁出该纯投影；随后收口 SQLite persistence adapter，不能采用旧 slim 的 `window.localStorage`。

### 15. 固定当前 LTS 的 Weapon workbook projection 合同

- 先行合同：在实现仍位于页面时新增 `weaponDraftWorkbook.test.ts`，固定名称/ID/字段/等级/效果键/数值/描述共 7 列的 key、标题、宽度和默认对齐。
- 地址与映射：测试覆盖 A/Z/AA/ZZ 列名、A1–G1 首行地址、row number、source row key，以及 weapon 图片 URL 解码后的显示值。
- 效果显示：skill1/skill2 value 行继续显示原 statType，skill3 value 行显示 `value`，普通 skill3 effect 映射为 `中文 label · buffType`，extraHit 保留 `damageType / skillType`。
- 不可变性：workbook projection 前后 Sheet row 输入完全一致；当前 15 行 fixture 的 fixed 与 extraHit 地址分别固定为 E12、E14，防止迁移时插入/编号偏移。
- 当前改动：页面仅给三种 projection 类型和三个纯函数增加 export，没有移动实现、修改 JSX 或触碰存储/图片平台逻辑。
- 快速验证：`node scripts/run-ts-test.mjs /src/components/weaponDraftWorkbook.test.ts` 与 `npm run typecheck` 通过。
- 下一刀：将同一实现原样迁入 `weaponDraftWorkbook.ts` 并让测试直接导入；迁移提交不得更改这些断言。

### 16. 迁出 Weapon workbook projection

- 实际改动：新增 106 行 `weaponDraftWorkbook.ts`，迁入 7 列 schema、A/AA 列名、row/cell/source 映射和普通 Buff/extraHit 效果键显示；页面由 2,796 行降至 2,708 行。
- 模块边界：新 projection 只导入 `weaponDraftModel` 的 draft/row 类型与 Buff label helper，不依赖 React、DOM、浏览器全局、storage、图片库或 ExcelJS。
- 行为约束：`weaponDraftWorkbook.test.ts` 改为直接导入新模块；`732c56ab` 建立的列、地址、宽度、对齐、effect display 和输入不变断言未作任何放宽。
- 页面保留：workbook selection、row class、keyboard handling 和 JSX cell editor 仍在页面；本批只移动纯数据投影，不制造 view/controller 参数管道。
- 快速验证：迁移后的 targeted test、`npm run typecheck` 和 `git diff --check` 通过。
- 下一刀：为 draft/library canonical key、坏 JSON 回退、normalize 和 save 建立注入式 repository 合同，再替换页面散落的 `read/writeLocalStorageJson`；生产 adapter 继续显式绑定 `persistentLocalStorage`。

### 17. 收口 SQLite 绑定的 Weapon draft/library persistence

- 历史风险：旧 slim 的 Weapon model/controller 直接读取 `window.localStorage`；当前 LTS 页面虽使用 SQLite-backed `persistentLocalStorage`，但两个 key、JSON helper 和直接 `setItem` 分散在初始化、自动保存、CRUD、导入与拖拽路径。
- 实际改动：新增 64 行 `weaponDraftPersistence.ts`，以注入 storage 的 repository 统一 load draft、load library、save draft 和 save library；页面由 2,708 行降至 2,671 行。
- canonical 语义：保留 `def.weapon-sheet.draft.v1` 与 `def.weapon-sheet.library.v1`；library 外层 key 继续覆盖内部 id，坏 JSON/`null` 回退为空 draft/library，所有读取继续经过当前 `normalizeWeaponDraft`。
- 平台接线：`WeaponDraftPage.tsx` 只在模块顶部将 `persistentLocalStorage` 注入 repository；组件内部不再持有 key、JSON storage helper、`getItem`、`setItem` 或直接平台调用。
- 单元合同：84 行内存存储测试覆盖 key、空值、坏 JSON、`null`、旧 effect schema、library canonical id、normalize 和 JSON 序列化写入；断言明确记录 `undefined` 不进入 JSON 的既有语义。
- 浏览器保护：现有 3040 Weapon 创建/保存/刷新流程继续作为真实 SQLite adapter 合同；本批不改变 400ms 自动保存、覆盖保护或导入逻辑。
- 快速验证：三个 Weapon targeted tests、`npm run typecheck`、`git diff --check` 和 storage boundary 扫描通过；页面只剩组合层一处 `persistentLocalStorage`。
- 下一刀：固定并抽取 Weapon 分享 parse/normalize/merge 数据边界，使导出和导入显式接收当前 library/repository，不在 hook 或 memo 中暗读存储。

### 18. 固定当前 Weapon 分享的真实浏览器往返

- 先行合同：在分享逻辑仍内联于页面时扩展 3040 E2E，读取当前 draft 导出预览并校验 `weapon-library-share.v1`、label 和 payload。
- 浏览器能力：验证剪贴板内容与预览 JSON 完全一致、真实下载文件名为 `.json`、错 share type 显示既有中文错误。
- 导入语义：以外层 `slim-imported-weapon` 和不同内部 id 导入一把改名武器，预览显示 1 把，确认后自动选中导入项，并在刷新后继续由 SQLite 恢复。
- 当前结果：未修改实现的 `npm run test:e2e:lts-slimming` 通过，全流程由约 10 秒增至约 11 秒。
- 下一刀：在此浏览器合同保护下抽纯 share parse/build/merge/selection；DOM 下载、clipboard、File input 与 modal state 不移动。

### 19. 抽取 Weapon 分享数据边界

- 实际改动：新增 90 行 `weaponDraftShare.ts`，集中 current/all 导出 payload、share type、错误文本、parse/normalize、覆盖合并和导入后选择策略；页面由 2,671 行降至 2,643 行。
- 显式数据流：导出函数接收当前 draft 与 React library，导入函数返回已归一化 share file，确认导入显式合并后仍交给 `weaponDraftRepository` 持久化；模块不暗读 storage。
- 单元合同：132 行测试覆盖 current/all 导出、当前 draft 覆盖 library 旧值、错 JSON/type/array/空 payload、label、旧 effect schema、外层 key 覆盖内部 id、覆盖合并和导入项优先选择。
- 既有宽松语义：当前 LTS 会把 payload 中的空对象归一化为“未命名武器”而不是丢弃；测试如实记录这一点。本批不夹带输入校验产品修正，后续若收紧必须单独决策和迁移说明。
- 平台边界：新模块不依赖 React、DOM、clipboard、File、Blob、浏览器全局、storage 或图片库；下载/复制/文件 input/modal state 继续留在页面。
- 浏览器回归：`bde2f42f` 建立的分享预览、复制、下载、错误、导入与刷新 SQLite 恢复流程在迁移后原样通过。
- 快速验证：Weapon share/model/persistence targeted tests、`npm run typecheck`、`git diff --check`、platform boundary 扫描和 3040 E2E 通过。
- 下一刀：先评估并固定 Weapon 公式 binding 的纯 apply 合同；拖拽 pointer lifecycle 在有专门手势测试前不移动。

### 20. 抽取 Weapon 公式 binding 与不可变 apply

- 历史核对：旧 slim 的 `weaponDraftFormula.ts` 与当前 LTS 页面内 390 行 binding 逐行 diff 后，除函数签名外完全一致；本轮仍重新接到当前 `weaponDraftModel`、catalog 与 workbook 类型，不引用旧混合 model。
- 实际改动：新增 438 行纯 `weaponDraftFormula.ts`，集中 weapon/skill/effect/effectLevels 的 value、focus、control、options、readOnly 与 `apply(draft, value)`；页面只保留 `useMemo` 和输入 UI，由 2,643 行降至 2,229 行。
- 单元合同：198 行测试覆盖武器名称/ID/图片/稀有度/描述、skill 名称与 statType、固定系列、普通 effect 名称/业务分类/Buff type、extraHit 只读显示、九级 value/effect 数值写入与清空、无地址 series 只读和缺失 effect no-op。
- 不可变性：所有可写路径验证源 draft 不变；缺失 effect 名称当前返回结构相同的新对象、分类返回原对象，两种既有引用语义均按 LTS 真实行为记录而未擅自统一。
- 纯边界：新模块不依赖 React、DOM、浏览器全局、storage、图片库或 ExcelJS；图片搜索的实际资源与 drawer/modal state 继续由页面管理。
- 快速验证：formula targeted test、`npm run typecheck`、`git diff --check` 和 platform boundary 扫描通过。
- 下一刀：补 skill3 主 value 行名称单元格的安全合同；随后再为 explorer 的拖拽规则与 pointer lifecycle 建立专门浏览器证据。

### 21. 修复 skill3 主数值名称单元格崩溃

- 触发合同：在 Weapon formula fixture 中给 skill3 增加主 value，并请求 `effect-skill3-value` 的名称 binding。
- 基线缺陷：当前 LTS 直接读取 `draft.skills.skill3.effects.value.name`；主 value 存在于 `skill3.levels` 而非 effects，因此点击该名称单元格会抛出 `Cannot read properties of undefined`。
- 修复：skill3 value 名称单元格明确返回只读行标题 `value`，保持既有 focus/key/placeholder 语义，`apply` 为原 draft no-op；skill1/skill2 固定系列的可选 statType 行不变。
- 回归合同：断言 binding 可建立、value 为 `value`、readOnly 为 true 且 apply 返回原引用；其他 30 余条公式断言继续通过。
- 留痕判断：这是新 characterization 在当前 LTS 上发现的独立缺陷，与旧 slim 结构迁移无关，因此单独提交。

### 22. 固定当前 Weapon explorer pointer lifecycle

- 先行合同：扩展 3040 E2E 导入一把含 skill3 主 value 与两个普通 effect 的隔离武器，随后真实操作资源管理器，而不是直接调用内部函数。
- 手势行为：固定按下后移动超过 6px 会取消 pending drag、静止超过 220ms 显示包含源 effect 名称的 preview、同 draft/skill/bucket 的 effect 可成为落点，松开后按 record 插入顺序刷新 UI。
- 持久化行为：重排后的顺序在当前 UI 与刷新后均验证；迁移前实现必须等待页面 400ms draft 自动保存和 SQLite adapter 60ms 批写后再刷新，说明 library 已立即写入，但当前选中 draft 存在短暂旧值窗口。
- DOM 约束：合同使用现有 `data-weapon-drag-*` 属性定位 draft、skill、bucket 与 effect key，并显式滚动到可见区域，避免把左侧长列表的视口位置误当业务失败。
- 当前发现：draft/skill 虽保留 drag node 和 reorder 分支，但 `canStart` 永远拒绝；skill3 的 `value` bucket 又被错误允许启动，长按预览会读取不存在的 `effects.value`。这两个问题留给独立修复，不混入先行合同提交。
- 验证：迁移前实现的 `npm run typecheck` 与扩展后的 `npm run test:e2e:lts-slimming` 通过；全流程约 13 秒。
- 下一刀：先分别修复选中 draft 的即时持久化与 value bucket 启动崩溃，再抽纯 drag policy，最后才迁 pointer hook。

### 23. 修复选中 Weapon effect 重排后的旧 draft 刷新窗口

- 失败证明：把先行 E2E 的刷新等待从“400ms 页面自动保存 + 60ms SQLite 批写”缩短为只覆盖 SQLite 的 120ms，重排后当前 UI 是 `second, first`，刷新后稳定回到 `first, second`。
- 根因：effect drop 立即写了新顺序的 library 并更新 React draft，却没有同时写独立 draft key；刷新初始化后 explorer 对当前选中项优先使用 draft key，因此展示旧顺序，直到 400ms 自动保存补写。
- 修复：当被重排武器就是当前 draft 时，在 `setDraft(nextDraft)` 同一分支显式执行 repository `saveDraft(nextDraft)`；非当前武器仍只改 library，避免无关地切换当前草稿。
- 行为结果：同一 E2E 现在只等待 SQLite adapter 已公开的 60ms 批写边界（测试留 120ms 余量），随后刷新仍保持 `second, first`；不再依赖页面级 400ms debounce。
- 范围控制：没有改变拖拽规则、effect 顺序算法、React listener 或全局 storage adapter，只让同一次用户操作的 library/draft 两个 canonical key 保持一致。
- 验证：修复前同一断言稳定失败；修复后 `npm run typecheck` 与 `npm run test:e2e:lts-slimming` 通过。
- 下一刀：为 skill3 value bucket 的不可拖合同制造失败并修复；随后才进入纯 policy 抽取。

### 24. 禁止 Weapon skill3 主 value 进入 effect 拖拽

- 失败证明：同一浏览器 fixture 含 skill3 主 value；新增断言要求该行没有 `is-draggable`，修复前稳定收到 `buff-sheet-explorer-effect is-draggable`。
- 崩溃路径：旧 `canStart` 只判断 `node.kind === effect && skillKey === skill3`，未排除 `bucket === value`；长按后 preview 又直接读取 `skill.effects.value.name`，而主 value 实际存于 `skill3.levels`。
- 修复：启动规则收紧为 `skill3 + effect bucket`；label helper 对 value 返回 key，对缺失普通 effect 也回退 key，不再做无保护属性读取。
- 浏览器合同：value 行不可拖，真实按住超过 220ms 也不出现 preview；紧随其后的普通 effect 取消、长按、重排和刷新合同继续通过。
- 范围控制：skill1/skill2、draft、skill 仍不可拖，普通 skill3 effect 行为不变；没有把主 value 转换成伪 effect，也没有改变 workbook 行结构。
- 验证：失败断言记录后，`npm run typecheck` 与完整 `npm run test:e2e:lts-slimming` 通过。
- 下一刀：把 node key/label/启动/落点/effect immutable reorder 抽到纯 policy，删除已经证明不可达的 draft/skill reorder 分支。

### 25. 抽取 Weapon explorer 纯 drag policy

- 历史参考：旧 `e79112f0` 的 `useWeaponExplorerDrag.ts` 把 key/label、规则、DOM timer/listener、React state、业务重排和 `window.localStorage` 写入放在同一个 276 行 hook；本轮只参考其职责目录，不采用实现。
- 实际改动：新增 150 行 `weaponExplorerDragPolicy.ts`，集中 drag node 类型、稳定 key、安全 label、搜索与 skill3/effect 启动门禁、合法落点和不可变 record 重排；页面由 2,231 行降至 2,150 行。
- 单元合同：243 行测试穷举 draft/skill/effect/value、搜索禁拖、null/自身/跨 kind/draft/skill/bucket 落点、缺 draft/skill/effect 回退、library/current draft fallback、前后方向移动、缺 key 与源对象不变。
- 死代码清理：当前 UI 从不允许 draft 或 skill 启动拖拽，左侧 draft 又按名称排序；页面中两个不可达的 reorder 分支已删除，policy 也只产生普通 skill3 effect 重排结果。
- LTS-native 边界：policy 只依赖 Weapon catalog/model，不导入 React、DOM、`window` 或 storage；页面仍显式决定何时将 `nextDraft` 与 `nextLibrary` 交给 SQLite-backed repository。
- 行为保护：`d1997d0c` 建立的移动取消、220ms 长按、preview、真实重排与刷新恢复 E2E 在接线前后原样通过；`4ece7d7a` 的 value 不可拖合同继续通过。
- 保留语义：重排源仍优先取 library、缺失时才回退当前 draft，和迁移前一致；这可能影响“未保存编辑后立即拖拽”的组合行为，必须另立失败合同后才能改变，不能夹带在结构迁移中。
- 验证：policy targeted test、`npm run typecheck` 与 `npm run test:e2e:lts-slimming` 通过；完整门禁在提交前执行。
- 下一刀：在同一浏览器合同下只迁 pointer timer/listener lifecycle；hook 不得接收 library、draft、repository 或 setter 集合。

### 26. 修复 Weapon 拖拽覆盖未保存编辑

- 复核证据：普通公式、等级、图片与 drawer 编辑先只更新 React `draft`，library 仍保留上次显式保存副本；`9720bdbc` 为保持迁移纯度暂时记录了旧实现“library 优先”的真实语义。
- 失败合同：构造 library 为 `first, second, third`，当前编辑副本为 `third, first, second` 且未保存修改了名称和内部 ID；要求拖动当前项的 `second → first` 后得到 `third, second, first` 并保留当前名称。旧规则稳定返回 library 的 `second, first, third`。
- 关键修复：reorder policy 新增显式 `activeDraftKey`；源节点 key 等于当前资源树选择 key 时优先 current draft，否则才读取 library。不能只比较 `currentDraft.id`，因为用户可能尚未保存 ID 修改，资源树仍使用旧 key。
- 不可变性：结果写回 source draft key，但 current draft、library 和各级 effects 均不原地修改；非当前武器继续以 library 为源，缺失当前/库目标仍返回 null。
- 页面接线：页面传入 `selectedLocalDraftId || draft.id`，成功结果继续同时保存当前 draft 和 library，因此修复不绕过 SQLite repository。
- 验证：修复前 targeted policy 断言稳定失败；修复后 policy test、`npm run typecheck` 与完整 3040 拖拽/刷新 E2E 通过。
- 独立复核结论：Sol ultra 判断 Weapon 在 pointer hook 后只应再做一次纯编辑事务切口，修复非当前武器复制取错源、未入库 draft context action 崩溃等确定性编辑问题；随后必须 STOP，拒绝旧 slim 的巨型 controller/view。

### 27. 在真实手势合同下迁出 Weapon pointer lifecycle

- 先行保护：`d1997d0c` 已在内联实现上固定移动超过 6px 取消、220ms 长按 preview、同层 effect 落点、松开重排与 SQLite 刷新恢复；`4ece7d7a` 额外固定 value 长按不启动。
- 实际改动：新增 198 行 `useWeaponExplorerDrag.ts`，迁出 hold timer、click suppression、`data-weapon-drag-*` DOM 解析、`elementFromPoint`、pointermove/up/cancel/blur capture listener 与 preview state；页面由 2,150 行降至 2,023 行。
- 接口收敛：hook 只接收 policy state、`onReorder(source,target)` 和 `onDragStart()`，只返回 drag state、消费 click suppression、can-start 与 pointer-down；没有 draft、library、repository、context menu 类型或 setter 集合。
- LTS-native 差异：旧 `e79112f0` 的同名 hook 接收 draft/library/setter 并直接写两个 `window.localStorage` key；新 hook 完全不知道持久化，业务提交仍由页面经过纯 policy 与 SQLite-backed repository。
- 安全解析：未知 kind、非法 skill key、非法 bucket 和缺失 dataset 均返回 null；SKILL_KEYS 与 value/effect bucket 在 DOM 边界显式校验。
- 行为回归：迁移前后的同一 Playwright 合同原样通过，220ms、6px 和 capture listener 语义未变；click suppression 从暴露 ref 收敛成一次性消费函数。
- 已知后续：旧语义下 `pointercancel` 与 window blur 会复用 pointerup 的提交函数。按独立复核意见，下一提交先新增取消不提交合同再修，不能混入本结构提交。
- 验证：`npm run typecheck`、`git diff --check` 与 `npm run test:e2e:lts-slimming` 通过；完整门禁在提交前执行。

### 28. 区分 Weapon pointer 提交与取消

- 失败合同：在第二个 effect 长按进入 preview、移动到第一个合法落点后，分别向 window 发送 `pointercancel` 与 `blur`；两种情况都要求 preview 消失且顺序保持 `first, second`。
- 基线缺陷：迁移前后保留的旧 lifecycle 把 pointerup、pointercancel 和 blur 全部绑定到同一个 finalize，`pointercancel` 合同稳定得到 `second, first`，证明取消被错误提交。
- 修复：hook 拆成 `commitDrag` 与 `cancelDrag`；只有 pointerup 调用 `onReorder`，pointercancel/blur 只清 pending timer 和 drag state。
- 正常路径：同一 E2E 在两个取消场景后再次执行真实 pointerup，仍得到 `second, first` 并在 SQLite 批写后刷新保持，证明修复没有禁掉正常拖拽。
- 范围控制：没有改变 220ms、6px、DOM 命中、policy 或 repository；Buff hook 的同类旧语义不在本提交内，后续若处理必须另立合同。
- 验证：修复前同一 Playwright 断言稳定失败；修复后 `npm run typecheck` 与扩展后的 `npm run test:e2e:lts-slimming` 通过。
- 下一刀：按独立复核只做最后一个纯编辑事务模块，修复非当前武器 effect 复制和未入库当前 draft 操作；完成即 STOP Weapon。

### 29. 收口 Weapon 纯编辑事务并正式停止拆分

- 独立复核：Sol ultra 在 model/workbook/persistence/share/formula/policy/hook 完成后给出 `ONE MORE CUT`，唯一建议是无 React 的编辑事务；明确拒绝旧 `68977bc9/e79112f0` 的 75 字段 view props、964+ 行 controller 和直接 `window.localStorage` hook。
- 实际改动：新增 199 行 `weaponDraftEditing.ts`，集中 active/library draft 解析、新建 effect、复制 effect 与删除 effect/value；页面由 2,023 行降至 1,985 行。
- 单元合同：138 行测试覆盖 current draft 未保存改 ID 时仍按 active key 优先、非当前 library fallback、current 缺失时 library 回退、完全缺失 null、malformed skill 防御和源 draft 不变。
- 新建修复：effect1/effect3 已存在时返回真实首个缺口 effect2，九级初始化为 0，focus row 也使用 effect2；不再用 `Object.keys().length + 1` 猜焦点而误指 effect3。
- 复制修复：源 effect 从解析后的目标 draft 读取，而不是页面全局 current draft；非当前武器右键复制不会复制错武器或无响应，嵌套 levels 深拷贝且 value bucket/缺失 effect 安全返回 null。
- 未入库安全：通用 `updateLibraryDraft` 先调用 `resolveWeaponDraftForEdit`，不会再对 `cloneValue(undefined)` 执行 JSON parse；active current、library fallback 和缺失目标三种结果显式可测。
- 删除语义：value bucket 清空 Lv1–Lv9 value 并保留 description；普通 effect 只删除真实存在项，缺失项不再制造无意义持久化写入。
- LTS-native 边界：编辑模块只依赖 catalog/model，不含 React、DOM、浏览器全局或 storage；页面仍独占 normalize、选中/焦点/drawer 编排和 SQLite repository 提交。
- 验证：editing targeted test、`npm run typecheck` 与完整 3040 浏览器流程通过；完整 Node/repository/Timeline/build/offline 门禁在提交前执行。
- STOP 决定：Weapon 第一阶段到此结束。剩余图片 drawer、clipboard/download、context menu、焦点和 JSX 都是页面组合职责；不再为了行数迁大 controller/view。全局 SQLite 异步写失败恢复另列平台风险，不在 Weapon 中退回原生 localStorage。

### 30. 固定当前 LTS 的 Equipment 分享浏览器往返

- 历史参考：旧 `68977bc9`、`b5a66dea`、`e79112f0` 只用于核对 Equipment 曾经拆过 model、view、controller、formula、image 与 share 等职责；本轮测试直接操作当前 LTS 页面，没有采用旧 Electron-era controller 或 storage 实现。
- 先行合同：在现有保存/刷新流程后选中刚创建的 `Slim E2E Equipment`，读取“导出当前”预览并校验 `equipment-library-share.v1` 及 payload 中真实套装/装备数据。
- 浏览器能力：验证复制后的剪贴板与预览 JSON 完全一致、真实 `.json` 下载无失败，以及错误 share type 显示现有中文反馈。
- 导入语义：从真实导出对象构造隔离的 `gear-set-slim-imported / equipment-slim-imported`，固定外层 key、内部 ID、套装名和装备名；预览显示 1 个套装，确认导入后必须显式保存并通过覆盖保护确认，刷新后再展开资源树仍可恢复。
- 范围控制：本批只新增 Playwright 行为证据，没有改 Equipment normalize、React state、SQLite adapter、分享实现或 JSX；第一次失败仅是测试文本定位同时命中套装名与装备名，收紧为精确名称后通过。
- 验证：`npm run test:e2e:lts-slimming` 通过，完整单 context 流程约 14 秒。
- 下一刀：先给当前 LTS 的 Equipment normalize、旧百分比迁移、显式 preset、row/workbook projection 和不可变编辑建立纯模型 characterization，再按同一断言迁出 LTS-native model。

### 31. 固定当前 LTS 的 Equipment 纯模型合同

- 先行合同：新增 577 行 `equipmentSheetModel.test.ts`；实现仍留在页面，只给纯类型、常量与函数增加测试出口，未改 React、storage、图片、分享或 JSX。
- LTS 补丁保护：覆盖 Lv0–Lv3、two/three-effects catalog shape、普通 percent 的 decimal 迁移与能力值/raw 型例外、旧三件套字段迁移、normalize 幂等，以及更改装备部位/词条类型时不再自动套 preset；显式 `applyEffectValueCatalogForPart` 仍单独可测。
- 数据与编辑合同：固定英文 ID/collision、空库、套装与装备排序、effect 顺序、不可变 set/equipment 更新、7 类 row 顺序、折叠过滤、A/AA 列地址、7 列 workbook cell/source projection 和行 class。
- 三件套合同：modifier 与 extraHit 均覆盖 normalize、drawer 双向投影、业务 category/type/value 与 extra-hit config，防止迁移时只保留普通 Buff 路径。
- 当前怪异语义：ASCII 名称会被 pinyin slug 拆成逐字母连字符；编辑护甲 `defense` 描述会重新套固定属性 preset，得到 `防御力：+56`，改为 `hp` 后才保留自定义 raw；缺失 equipment 时业务数据相同但外层对象会重建；`makeNextId` 对中间缺号按现有数量继续编号。全部先按真实 LTS 固定，不在结构提交里顺手修正。
- 验证：targeted model contract、`npm run typecheck` 与 `git diff --check` 通过。
- 下一刀：从当前页面原样迁出 LTS-native data model；旧 `equipmentSheetDataModel.ts` 只作职责索引，因为它仍混入 React、路由、`imageBridge` 与 storage helper。

### 32. Equipment 独立架构复核与 STOP 边界

- 旧 slim 可用范围：`equipmentSheetTypes.ts`、`equipmentSheetDataModel.ts`、`equipmentSheetPageModel.tsx` 与 `equipmentSheetFormula.ts` 只用于核对职责和 selection matrix。旧 data model 仍依赖 `imageBridge`、`window.localStorage` 与 `desktopRuntime`；旧 controller/view 最终仍约 971/778 行并传递约五十个状态和 callback，因此禁止整文件移植。
- LTS 提交证据：`152a7243` 与旧线 `28f05d03` 的 Equipment patch-id 相同，父提交中的页面也相同；当前 LTS 继承显式 mapping 语义，但旧 slim 不含后来的 `e1965ff2` Web-only 清理与 `0414e72f` `webImageLibrary` 边界。
- 最终模块边界：按 `model -> workbook -> editing -> formula -> persistence -> share` 六步实施。model 只放 schema/normalize/catalog；workbook 放 row/filter/cell projection；editing 返回 next library/focus/created id；formula 提供纯 `apply(baseLibrary, input)`；persistence 注入 storage；share 只做 build/parse/normalize/merge。图片、clipboard、File/Blob、modal/drawer、焦点/折叠、context menu 和 JSX 留在页面。
- 已识别高风险：用户图片当前可能把临时 `blob:` 预览 URL 写进装备数据；合法 share 中 `null` entry 可能让 normalize 抛错；外层 key 与内部 ID 冲突当前由内部 ID 优先；effect description 的表格摘要只读与 inline raw 可编辑语义分裂；legacy percent 启发式会在每次 normalize 重跑；SQLite `setItem` 后 60ms 才批写但页面立即提示成功。
- 已识别并发/一致性风险：新建 effect 在 React state updater 内给外部变量赋值后立刻聚焦；“导出当前”选中 effect 时 label 使用 effect 名但 payload 是整个套装。ASCII slug 等既有怪异行为仍只记录，不夹带修复。
- 实施纪律：结构迁移只保持现行为；每个 correctness 问题必须先制造直接失败合同，再单独修复。用户图片持久引用、share malformed 输入、formula 统一提交和 effect 新建事务属于 Equipment STOP 前必须审计的项目。
- STOP 条件：六个纯边界均有直接测试且不导入 React/DOM/window/route/storage/image platform；广泛页面测试出口已删除；保存、分享、图片、显式 mapping 与刷新 E2E 通过；不创建巨型 controller/view。

### 33. 恢复 Equipment 最后实际词条的下游计算合同

- 历史证据：`28f05d03` 曾在 `operatorPanelCalculator.test.ts` 固定“实际最后一个存在的主/副能力词条才是百分比，前面的同类词条按固定能力值”；当前 LTS 保留了实现，却丢失了该测试文件。
- 新合同：以当前 `buildConfigSnapshot` 增加 132 行聚焦测试，分别覆盖 effect1+effect2、effect1+effect3（中间缺号）和完整 effect1+effect2+effect3。
- 结果语义：最后的 `mainStatBoost/subStatBoost` 进入百分比 totals/panel scale；非最后项归一为固定 `mainStat/subStat`；最后是普通伤害时，前两项都保持固定且伤害加成仍进入 panel display。
- 范围控制：没有恢复旧提交中的整份大 fixture，也未修改 calculator；测试只锁当前 LTS 的下游解释，防止 Equipment schema/model 迁移后看似数据相同、最终面板语义却漂移。
- 验证：targeted calculator contract、`npm run typecheck` 与 `git diff --check` 通过。

### 34. 从当前 LTS 迁出 Equipment schema/normalize/value catalog

- 实际改动：新增 673 行 `equipmentSheetModel.ts`，迁出领域 schema、三件套 modifier/extraHit 适配、ID、legacy percent、preset normalize、two/three-effects catalog、显式 catalog apply、library normalize、空库与只读排序 helper；页面由 3,310 行降至 2,633 行。
- 收紧边界：独立复核后没有把 `applyCellValueToLibrary`、新建/复制/删除事务、`EquipmentRow`、row/filter/workbook projection 一起塞进 model；它们继续留在页面，分别等待 editing/workbook 的先行合同和独立迁移。
- 纯模块约束：新 model 只依赖 pinyin、Buff domain/service、`operatorDraftBuffModel` 与静态 preset JSON，不导入 React、route、DOM、浏览器全局、storage、`webImageLibrary`、图片类型或 ExcelJS。
- 测试接线：model characterization 改为直接导入新模块；editing/row/workbook 断言临时仍从页面测试出口读取，因此同一测试同时证明迁移部分与尚未迁移部分没有断开。
- 启发式留痕：补充固定 raw 不含 `%` 时 value=2 保持 2；raw 仍写 `20%` 时当前 value=2 会被再次 normalize 为 0.02。后者是已知迁移风险，结构提交没有擅自修复。
- 旧 slim 差异：没有复制 `eb0f56ef` 的旧 data model，因此未重新引入 `imageBridge`、`window.localStorage` 或 `desktopRuntime`。
- 完整门禁：typecheck、全量 Node（含下游 calculator）、repository check（tracked=507）、Timeline smoke、3040 E2E（约 14 秒）、production build（234 modules）和 atomic offline shell（`a62a5f365fc5fb0c`，40 files）全部通过。
- 下一刀：把 row/filter/7-column workbook projection 从页面迁入独立 `equipmentSheetWorkbook.ts`；editing 仍不混入该提交。

### 35. 固定当前 LTS 的 Equipment workbook projection 合同

- 测试分层：新增 286 行 `equipmentSheetWorkbook.test.ts`，把原 model 大测试中的 row/filter/workbook 断言迁出；model test 保留 normalize/catalog 与尚未迁出的 editing 事务，不重复覆盖 projection。
- row 合同：固定 set、three-piece header/effect、equipment、fixedStat、effect、effectLevels 的完整顺序、稳定 key 与关键 valueText；percent/flat/缺失等级摘要分别有精确字符串断言。
- 折叠合同：覆盖默认只见套装、展开套装、展开装备、展开全部 effect levels，以及单独折叠三件套效果；所有过滤前后输入 rows 保持不变。
- workbook 合同：固定 7 列 key/title/width/align、A/G/Z/AA/ZZ 地址、row number、A1–G1 套装值、effect1 的 A6–G6 值、sourceRowKey 和各 kind 的现有 CSS class。
- 不可变性：`buildRows` 前后 library 不变，`filterVisibleRows` 与 `buildWorkbookRows` 前后 rows 均与显式快照一致。
- 范围控制：实现仍全部位于页面，本批没有移动函数、修改 JSX 或触碰 editing/storage/share。
- 验证：model/workbook targeted tests、`npm run typecheck` 与 `git diff --check` 通过。
- 下一刀：原样迁入 `equipmentSheetWorkbook.ts`，测试改为直接导入；页面只保留 selection、折叠 state 与 JSX。

### 36. 固定当前 LTS 的 Equipment 单元格编辑事务

- 测试分层：新增独立 `equipmentSheetEditing.test.ts`，从 model test 迁出 `applyCellValueToLibrary`、`makeNextId`、`updateLibrarySet` 与 `updateLibraryEquipment` 合同；model test 现在只负责 schema/normalize/catalog/drawer。
- 字段矩阵：覆盖 set 的 name/buffId/img、equipment 的 name/part/img、fixedStat 的 name/type/raw、effect 的 name/type/raw 与 Lv0–Lv3 write/clear、three-piece 的 category/name/type/value/raw。
- LTS mapping 保护：改 equipment part 不自动套 fixed/effect preset；改成合法 effect type 保留自定义 levels；非法 type 清空 type/levels；defense raw 编辑仍记录为重新套护甲 preset 后的 `防御力：+56`。
- 引用语义：缺失 gear set 返回原 library；缺失 equipment 业务数据不变但当前 helper 会重建外层 library、保留 gear set 原引用。有效更新和所有 cell transaction 均验证源 library/嵌套对象不变。
- ID 语义：空列表、中间缺号和连续编号继续固定当前 `existingIds.length + 1` 行为，不在本批修复。
- 范围控制：本批只重组测试，editing 实现和 React CRUD 仍在页面；新建 effect 的 updater 外部变量风险等待纯事务迁移时单独处理。
- 验证：model/workbook/editing targeted tests、`npm run typecheck` 与 `git diff --check` 通过。
- 下一刀：workbook 迁移提交后，将这组 editing 断言直接接到独立模块，再为 create/duplicate/delete/normalize/mapping 返回值补合同。

### 37. 迁出 Equipment row 与 workbook projection

- 实际改动：新增 329 行 `equipmentSheetWorkbook.ts`，迁出 EquipmentRow、7 列 schema、row build、等级摘要、折叠过滤、列地址、cell/source projection 和 row class；页面由 2,633 行降至 2,285 行。
- 领域接线：three-piece business type 的纯派生 helper 收回 `equipmentSheetModel.ts`，页面和 workbook 共用；没有把 `operatorDraftBuffModel` 或 drawer 逻辑复制进 projection。
- 页面保留：selection、公式地址导航、折叠 state/toggle、focus/scroll、editable cell JSX 和 context menu 全部留在组合层；editing 事务没有混入本提交。
- 测试接线：workbook contract 改为直接导入新模块，原有 row/value/address/class/不可变断言未放宽；model 与 editing 合同同时通过。
- 纯边界：新模块只依赖 `equipmentSheetModel`，不导入 React、DOM、浏览器全局、route、storage、图片平台、Blob 或 ExcelJS。
- 完整门禁：typecheck、全量 Node、repository check（tracked=510）、Timeline smoke、3040 E2E（约 14 秒）、production build（235 modules）和 atomic offline shell（`72d9e6e2a40df5f9`，40 files）全部通过；Equipment route chunk 163.41 kB（gzip 22.61 kB）。
- 下一刀：将 `applyCellValueToLibrary` 与 update helper 迁入 `equipmentSheetEditing.ts`，随后为 CRUD/normalize/mapping 建立显式返回值事务，修掉 updater 外部变量焦点风险。

### 38. 迁出 Equipment 基础 editing 事务

- 实际改动：新增 230 行 `equipmentSheetEditing.ts`，迁出 `applyCellValueToLibrary`、`makeNextId`、`updateLibrarySet` 与 `updateLibraryEquipment`；页面由 2,285 行降至 2,074 行。
- 测试接线：`equipmentSheetEditing.test.ts` 改为直接导入新模块；set/equipment/fixed/effect/levels/three-piece 字段矩阵、显式 mapping 保护、缺失目标引用语义和不可变断言原样通过，页面不再反向重导出 editing。
- 纯边界：新模块只依赖 Buff domain/service、`operatorDraftBuffModel`、Equipment model/workbook 类型，不导入 React、DOM、浏览器全局、route、storage、图片平台、Blob 或 ExcelJS。
- 范围控制：本提交只移动已有纯函数；React 内联的 create/duplicate/delete/normalize/mapping callbacks 暂未重写，defense preset、ID 缺号和缺失 equipment 外层重建等现行为也未修。
- 验证：model/workbook/editing targeted tests、`npm run typecheck`、`git diff --check` 与完整 3040 E2E（约 14 秒）通过。
- 下一刀：先为 CRUD 结果对象建立测试，再让页面用 `{ library, createdId, selectedRowKey }`，消除 state updater 内修改外部 `nextEffectId`。

### 39. 收口 Equipment CRUD 纯事务

- 失败证明：先扩展 `equipmentSheetEditing.test.ts`，要求 create/duplicate/delete/normalize/mapping 返回 `{ library, changed, createdId, selectedRowKey }`；迁移前实现没有 `createEquipmentGearSet` 等导出，targeted test 在第一项稳定报 `createEquipmentGearSet is not a function`。
- 实际改动：`equipmentSheetEditing.ts` 增加套装、装备、普通 effect、三件套 effect 的新建与复制，固定属性补建，五类节点删除，library 排序和显式词条数值映射；所有函数接收当前 library 并返回结果，不读取 React state、DOM、storage 或浏览器平台。
- 合同范围：新增断言覆盖 effect1/effect3 缺口优先创建 effect2、连续事务继续分配 equipment-003 且保留前一结果、三词条满载 no-op、三件套递增 ID、装备 copy ID、嵌套 levels 深拷贝、缺失目标 no-op、五类删除、套装/部位/effect 顺序、映射保留自定义 label/其他 effect，以及所有源对象不变。
- 页面接线：删除原页面重复 CRUD callback 内容，统一通过 `commitEditingTransaction` 标脏并补 `updatedAt`；页面从 2,074 行降至 1,900 行，只保留 active/selection、折叠、drawer、消息与 context menu 编排。
- 时序修复：三件套和普通 effect 的新 ID 由纯事务同步返回，页面不再在 React state updater 内修改外部 `nextEffectId` 后立即读取；create/duplicate 现在使用同一个结果决定写入与焦点。
- 行为收敛：删除、复制和新增后只在事务确实 `changed` 时写 state；不存在的 set/equipment/effect、已有 fixedStat 和无可用数值 preset 均不再制造无意义脏写。
- 独立复核修正：Sol ultra 首轮拒绝提交并找到两个 P1、一个 P2。页面改为让 `commitEditingTransaction` 从同步维护的最新 `libraryRef` 执行事务，所有初始化、普通 mutation 与保存替换都同步该引用，避免旧闭包整库覆盖或重复 ID；事务切换行统一清空 `selectedCell`，避免副本高亮后公式栏继续改源对象；fixedStat 选择键改为 workbook 的真实 `fixed-*`。同一审查者复验后确认三项关闭且无新增 P0/P1，结论改为可提交。
- 浏览器修复合同：在导入装备保存并刷新后，真实选中其名称单元格建立公式绑定，再通过右键复制装备；要求公式地址清空为 `-` 且“副本” workbook 行成为 active。修复后完整 E2E 约 14 秒通过。
- 完整门禁：typecheck、全量 Node、repository check（tracked=511）、Timeline smoke、3040 分享/保存/刷新/复制 E2E（约 14 秒）、production build（236 modules）和 atomic offline shell（`89cc6d989b91a8de`，40 files）全部通过；Equipment route chunk 165.54 kB（gzip 22.81 kB）。
- 下一刀：先固定公式 binding/apply 的当前矩阵并处理 description/value 提交分裂；随后迁 persistence/share，最后用真实用户图片合同解决持久化 `blob:` 引用。

### 40. 固定并迁出 Equipment 公式 binding/apply

- 补丁基线：初版 `99031f9b` 曾允许 fixed value、effect unit/raw 自由编辑；后续 `8ec63b96` 为统一 Buff 编辑与数值映射明确撤销 fixed value/effect unit，`a9e8d97f` 只恢复 Lv0–Lv3，`152a7243` 又把 effect description 改为等级摘要并设为 fx 只读。因此旧 Phase 3 spec 的相关矩阵已被后续 LTS 补丁覆盖，本轮按提交历史保护现状，不把旧能力误恢复。
- 纯合同：新增 263 行 `equipmentSheetFormula.ts` 与 324 行直接测试；覆盖 set/equipment 图片、部位、fixed type、effect category/type、three-piece 普通/extraHit、Lv0–Lv3、header/ID/固定值/单位/摘要只读、缺失目标和源 library 不变。
- 接口边界：binding 只暴露 key/value/inputMode/control/options/placeholder/readOnly 与 `apply(baseLibrary, rawInput)`；不接收 React callback，不导入 DOM、浏览器全局、storage、route、图片平台或 JSX。
- 页面接线：图片选择、select/search-select 即时写回、普通 fx blur/Enter 与“未 blur 直接保存”全部改走同一个 `binding.apply`；页面删除重复矩阵和 effectLevels 特判，从 1,900 行降至 1,764 行。
- 保存一致性：保存仍以同步维护的最新 `libraryRef.current` 为 base，再应用尚未 blur 的 formulaInput；不再由页面复制一份 row/level apply 逻辑，后续 persistence 抽取可直接复用这个最终 library。
- 完整门禁：formula targeted、typecheck、全量 Node、repository check（tracked=513）、Timeline smoke、3040 E2E（约 14 秒）、production build（237 modules）和 atomic offline shell（`183e62138b00e881`，40 files）全部通过；Equipment route chunk 166.08 kB（gzip 23.09 kB）。
- 已知 correctness：`152a7243` 已让 fx 摘要只读，但 table inline 仍把同一摘要渲染成可编辑 input；下一提交先用浏览器/矩阵合同固定冲突，再只移除这条不可兑现的 inline 编辑入口。

### 41. 对齐 Equipment effect 摘要的只读语义

- 失败合同：Equipment 分享 fixture 增加一条明确的 percent effect；保存并刷新后真实选中该装备，要求 effect description 单元格显示等级摘要、且内部 `input/select` 数量为 0。修复前稳定收到 1 个 input。
- 历史判断：没有按旧 Phase 3 spec 恢复 raw 编辑；`152a7243` 明确把该列投影改成 `formatEffectLevelsSummary` 并让 fx/清空路径只读，当前唯一漂移是 `renderEditableCell` 遗漏移除 description。
- 修复：从普通 effect 的 inline editable 矩阵删除 `description`；set/equipment 图片、fixed raw、three-piece raw、effect name/category/type 和 Lv0–Lv3 编辑保持不变。
- 浏览器结果：同一真实导入/保存/刷新流程现在看到纯文本摘要，随后公式单元格绑定、复制装备、清空公式选择和副本 active 合同继续通过。
- 验证：失败断言记录后，`npm run typecheck`、`git diff --check` 与完整 3040 E2E（约 14 秒）通过。

### 42. 收口 Equipment durable SQLite repository

- 原风险：页面直接对两个 key 调用 `persistentLocalStorage.setItem` 后立即清脏并显示成功；SQLite adapter 实际延迟 60ms 批写，flush 失败会变成后台 console error，用户仍看到“已保存”。
- 纯边界：新增 49 行 `equipmentSheetPersistence.ts`，注入 `getItem/setItem/flush`；读取保持 library key 有数据优先、否则 draft fallback 并统一 normalize，保存向两个 canonical key 写同一 JSON 后必须 `await flush()`。
- 合同范围：197 行测试覆盖 key、library/draft 优先级、空/空白/null/malformed fallback、领域 normalize、双 key 字节一致、set 顺序、deferred flush 未完成前 save promise 不 resolve，以及两个 setItem/flush 错误原样传播。
- 页面接线：删除 JSON/storage helper 与 key 常量，初始化改走 repository；`performSave` 先在最新 library 上应用 pending formula，再 await durable save。只有成功才 replace library、清脏、关框和提示；失败保留未保存状态并显示错误。
- LTS-native 边界：repository 不导入 React、DOM、window、route、图片平台、SQLite singleton 或 fetch；页面仍负责内置 JSON fetch、确认框、warning 和消息。页面从 1,764 行降至 1,742 行。
- 完整门禁：persistence targeted、typecheck、全量 Node、repository check（tracked=515）、Timeline smoke、3040 保存/刷新 E2E（约 14 秒）、production build（238 modules）和 atomic offline shell（`d80bed42f1dd7bec`，40 files）全部通过；Equipment route chunk 166.26 kB（gzip 23.16 kB）。
- 下一刀：抽 Equipment share build/parse/normalize/merge；先固定 malformed `null`、外层 key 与内部 ID 冲突、current export label 三项语义，再分别修复。

### 43. 收口 Equipment share build/parse/merge

- 提交证据：`81a5376e` 在原始 Equipment 页面建立分享流程；旧 slim 的 `e79112f0` 只把同一逻辑搬进 `useEquipmentSheetShare.ts`，仍保留 `null` entry 崩溃、内部 `gearSetId` 优先和 effect 标题冒充套装 label 三个问题。本轮只借它确认职责边界，没有移植旧 hook、React state 或 Electron-era controller。
- 失败合同：浏览器向真实导入框粘贴 `{ payload: { broken: null } }`，修复前页面既不显示“没有有效套装”，处理链也被异常打断；另在选中导入 effect 后读取“导出当前”预览，要求 label 为所属套装名、payload 只含该套装。
- 纯边界：新增 132 行 `equipmentSheetShare.ts` 与 320 行直接测试，统一 current/all build、错误分类、逐项 normalize、canonical merge 和 selection fallback；模块不导入 React、DOM、window、route、storage、图片平台、Blob 或下载 API。
- ID 语义：分享 payload 的 Record 外层 key 是 canonical identity；内部 `gearSetId` 冲突时外层胜出，非法外层 ID 走现有英文 ID normalizer，规范化碰撞追加 `-2`，最终保证 record key 与对象 `gearSetId` 一致且不静默覆盖。
- 异常输入：`null`、primitive 与 array entry 被逐项过滤，单条深层 malformed 也不能拖垮其余套装；错误 JSON/type/payload shape 与“类型正确但没有有效套装”继续给出两类既有中文反馈。
- 合并保护：导入 payload 在边界内先 normalize，确认导入后只覆盖 `gearSets`；不再对整个现有 library 重跑 `normalizeEquipmentLibrary`，因此保留 `updatedAt/migration`、未覆盖套装引用和当前旧百分比值，不触发二次迁移。
- 页面接线：顶部导出、右键导出当前套装、粘贴/文件导入和确认 merge 全部使用新边界；选中 equipment/fixed/effect 时 current label 统一来自所属 `gearSet.name`。页面由 1,742 行降至 1,737 行。
- 完整门禁：share targeted、typecheck、全量 Node、repository check（tracked=517）、Timeline smoke、扩展后的 3040 E2E（1 passed，约 14 秒）、production build（239 modules）和 atomic offline shell（`b463208d3ef89079`，40 files）全部通过；Equipment route chunk 166.82 kB（gzip 23.36 kB）。
- 下一刀：用真实 Image Manager 用户图片建立失败合同，禁止把临时 `blob:` URL 写进 Equipment 数据；保存 canonical `assets/images/...` 引用并通过 resolver 预览、分享和刷新恢复后达到 Equipment STOP。

### 44. 修复 Equipment 用户图片持久引用

- 真实夹具：浏览器流程先进入 Image Manager，通过真实 file chooser 把 1×1 PNG 写入浏览器 SQLite，确认列表出现 `slim-equipment-user-image.png`；随后回到 Equipment，在新建装备的图片公式控件中搜索并选择该资源，不伪造 React state 或直接写 storage。
- 失败证据：修复前保存、刷新和分享流程可以继续执行，但导出对象的 `imgUrl` 明确收到 `blob:http://127.0.0.1:3040/...`，而合同期望 `assets/images/slim-equipment-user-image.png`。这证明页面把本次 document 的显示 URL 当成了业务持久引用。
- 夹具校正：第一次测试把 workbook row locator 锚在会随输入变化的 `value="新建装备"` 上，导致等待超时；改用填写后的稳定值后才形成产品失败。选中图片单元格时公式控件会占据消息区，因此保存断言改读现有 `.equipment-sheet-save-status`，没有把测试定位问题冒充实现缺陷。
- 修复：图片 option 继续同时携带 `relativePath` 与 `displayUrl`；选择时只把 canonical `relativePath` 交给 formula transaction、formula input 和搜索状态。缩略图及顶部预览仍通过 `getWebImageUrl/normalizeAssetUrl -> resolveWebImageUrl` 把相对引用解析成运行时 blob 或静态 URL。
- 选中态兼容：picker 用 `canonicalizeWebImageReference` 比较当前值和 option 的 `relativePath`，继续兼容带前导斜杠、旧桌面别名与当前 display URL；没有把图片平台逻辑塞进纯 model/formula/share 模块。
- 浏览器结果：真实用户图选择后可预览，显式保存并刷新后图片 slot 不进入 broken、预览 `src` 由 resolver 重新得到 `blob:`，而分享 JSON 始终保留 `assets/images/...`，不泄漏临时 object URL。
- 完整门禁：修复前 canonical path 断言稳定失败；修复后 typecheck、全量 Node、repository check（tracked=517）、Timeline smoke、扩展后的 3040 E2E（1 passed，约 15 秒）、production build（239 modules）和 atomic offline shell（`d25879c030e18e6c`，40 files）全部通过；Equipment route chunk 166.89 kB（gzip 23.40 kB）。Equipment STOP 独立架构复核执行中。

### 45. 阻止 Equipment canonical 百分比二次迁移

- 独立复核 P1：用户把 percent 等级改为 `2` 时会保留旧 `raw="+20%"`；此前缓存重载、OperatorConfig 和 snapshot refresh 都会再次调用 legacy heuristic，把规范值 `2` 错缩成 `0.02`。这不是显示误差，而是保存后业务数值永久变化。
- 失败合同：model 要求 normalized library/set 带 schema 2，修改后的 `2` 再 normalize 仍为 `2`；repository 要求旧的无 marker 浏览器缓存也按 canonical 读取；share 要求带 schema 2 的套装在外层 ID normalize 后仍保留 `2`。修复前第一条 marker 断言即稳定收到 `undefined`，旧缓存和分享分别会得到 `0.02`。
- 迁移边界：`normalizeEquipmentLibrary` 新增 `assumeCanonicalValues`，输出 library 与每个 gear set 均标记 `schemaVersion: 2`。未标记的文件/旧分享继续执行 legacy percent 迁移；已标记 library/set、三件套 schema 2 或 repository canonical cache 只做结构 normalize，不再按 stale raw 猜测单位。
- 既有缓存兼容：Web LTS 的两个 Equipment storage key 从安装包开始就存 canonical decimal，因此 repository 对有无 marker 的缓存都明确使用 canonical 模式；首次从 `public/data/equipments/equipments.json` 读取的 raw 百分比仍走默认 legacy 模式。新建库和新建套装同时写 schema 2，后续分享可携带边界信息。
- 下游收口：`OperatorConfigPage` 与 `operatorConfigSnapshotRefreshService` 只读取 Equipment canonical storage，不再各自维护并执行一份 legacy heuristic；它们仍保留各自的结构投影和“最后实际能力词条”语义，旧文件迁移唯一归 Equipment file/import 边界负责。
- 完整门禁：model/persistence/share 三组 targeted 合同由红转绿；typecheck、全量 Node、repository check（tracked=517）、Timeline smoke、3040 E2E（1 passed，约 15 秒）、production build（239 modules）和 atomic offline shell（`691e9359fb02f92a`，40 files）全部通过；Equipment route chunk 167.15 kB（gzip 23.48 kB）。

### 46. 防止 Equipment 异步保存覆盖新编辑

- 独立复核 P1：此前 `performSave` 捕获旧 library，等待 SQLite flush 后无条件 `replaceLibrary(nextLibrary)` 并清 dirty；覆盖保护关闭时页面仍可立即编辑，确认框路径也会在调用异步保存前关闭，因此 flush 期间的新事务会被旧快照覆盖。
- 失败合同：repository 增加 deferred flush 场景，保存开始后把 current library 替换为 `updatedAt: edited-during-flush`；要求 durable write 完成后返回 `superseded` 且新对象保持不变。实现前稳定报 `saveLibraryRevision is not a function`；未发生替换的对照场景必须返回 `current`。
- repository 语义：`saveLibraryRevision(snapshot, getCurrentLibrary)` 先复用已有双 key + flush durable save，只有 flush 完成后才比较当前对象 identity；存储错误继续原样抛出，不会提前读取 current 或误报成功。
- 页面接线：保存开始时先把 pending formula 形成的 timestamped snapshot 同步写入 `libraryRef/state` 并保持 dirty，再 await revision save。返回 `current` 才清 dirty；返回 `superseded` 时不替换当前 library，继续标脏并提示“已保存开始时的版本；保存期间有新修改”。失败同样保留当前内存状态与 dirty。
- 边界说明：selection、搜索文本等非业务 state 不影响 revision；formula 文本若在保存后继续输入，即使尚未 blur，也仍由既有 `hasUnsavedChanges` 比较保护。没有为此引入全局锁或阻塞整个编辑器。
- 完整门禁：deferred/current revision targeted test、typecheck、全量 Node、repository check（tracked=517）、Timeline smoke、3040 E2E（1 passed，约 15 秒）、production build（239 modules）和 atomic offline shell（`df72c51f102ba871`，40 files）全部通过；Equipment route chunk 167.39 kB（gzip 23.61 kB）。

### 47. 修复 Equipment 图片索引 hydration 竞态

- 独立复核 P1：装备库与 Web 图片索引并行读取；若 canonical 用户图片路径先进入预览，resolver 会暂时退回静态 URL。旧页面第一次 `onError` 后直接卸载 `<img>`，而失败状态只监听原始 `imgUrl`，图片索引稍后把同一路径解析成 blob 时既不会清失败，也没有图片节点可重试。
- 失败合同：新增 `equipmentSheetImagePreview.test.ts`，先要求“图片索引加载期间不抢跑”“失败提示存在时仍保留可重试图片节点”“hydration 后使用新 blob URL”；实现前 targeted test 因缺少该 presentation 边界稳定失败。
- 纯边界：新增 `equipmentSheetImagePreview.ts`，只投影 stored reference、resolved URL、索引 loading 与 load failure；不导入 React、DOM、图片平台或 storage。页面初始即把图片索引标为 loading，并在 `imageAssets` hydration 后重新计算 resolver URL。
- 重试语义：索引完成前不挂载预览；首次失败后 `<img>` 保留但通过 `hidden` 让失败提示可见，后续 URL 改为 blob 时浏览器会重新请求，`onLoad` 再清失败状态。原始 canonical `imgUrl` 始终不被运行时 URL 覆盖。
- 标签修正：官方 release 图片不再归为 `user`；只有浏览器 SQLite 导入资源显示 user，builtin/release 都归官方内置侧。该分类同样由纯 helper 直接测试。
- 完整门禁：image-preview 及全部 Equipment targeted 合同、typecheck、全量 Node、repository check（tracked=519）、Timeline smoke、3040 E2E（1 passed，15.1 秒）、production build（240 modules）和 atomic offline shell（`47da1dc4296d9875`，40 files）全部通过；Equipment route chunk 167.81 kB（gzip 23.80 kB）。独立 STOP 复核继续检查剩余 P2，不把本次绿灯提前当成模块完成。

### 48. 固定当前 LTS 的 OperatorDraft 纯模型合同

- 提交基线：旧 slim 的 `5d8f9688` 曾迁出 `operatorDraftPageModel.ts`、字段组件和 library hook；共同祖先到当前 LTS 的原页面只经过 `b603cba6` Web 导航与 `0414e72f` 图片库两批 25 行级补丁。旧 model 仅作为职责索引，旧 hook 直接使用 `window.localStorage`，明确禁止移植。
- 失败合同：新增 378 行 `operatorDraftPageModel.test.ts`，先直接要求当前页面导出既有纯逻辑；加测试出口前定向测试稳定报 `createDefaultDraft is not a function`，没有先创建新 model 让测试天然变绿。
- 合同范围：覆盖默认/空草稿、属性 normalize、malformed import、typed skill 与 draft/skill/hit/effect ID、skill order sync/move/build、结构重排与 key map、hitCount、schema 2 Buff、multiplier/extraHit normalize/validation 和源对象保护。
- 当前怪异语义：`normalizeDraft` 原地修改输入；`skills: []` 会通过 import 校验；旧 hit 的 `multiplier` 会回填缺失等级后被删除；extraHit 仍保留输入 value；`buildOrderedDraft` 只重建 skills 容器并保留 skill 引用。全部先按真实 LTS 固定，不在 characterization 中顺手修正。
- 范围控制：生产页面只给已有类型/函数增加 export，逻辑字节未改；没有触碰 storage、`webImageLibrary`、分享、React JSX、CSS 或浏览器流程。
- 验证：targeted test、`npm run typecheck`、全量 Node、repository check（tracked=520）与 `git diff --check` 通过。
- 下一刀：把这组测试直接接到 LTS-native `operatorDraftPageModel.ts`；随后单独收口 SQLite durable repository 与 share 边界，不能采用旧 slim 的 localStorage hook。

### 49. 从当前 LTS 迁出 OperatorDraft 纯模型

- 实际改动：新增 489 行 `operatorDraftPageModel.ts`，从当前页面原样迁出 schema、catalog、默认构造、拼音 ID、属性/Buff normalize、import validation、skill/hit/effect ID、skill order 与结构重排；页面由 2,281 行降至 1,840 行。
- 测试接线：378 行 characterization 改为直接导入新 model，默认/空草稿、malformed 输入、schema 2 multiplier/extraHit、ID、排序、重排 key map、不可变性和已记录怪异语义均未删减或放宽。
- LTS-native 边界：model 只依赖 `pinyin-pro`、Buff domain type 与 `operatorDraftBuffModel`；不导入 React/JSX、DOM、`window/navigator`、route、storage、`webImageLibrary`、asset raw、Blob/File/URL 或 share/download。
- 页面保留：`persistentLocalStorage`、Web 图片列表、导航 links、静态资源清单、clipboard/download、分享 modal、Markdown JSX、SearchablePathSelect 和全部交互 state 仍由当前 LTS 页面组合，没有搬入纯模型。
- 旧 slim 差异：没有复制 `5d8f9688` 中同时容纳 route/asset/storage 常量的旧 model，更没有采用直接写 `window.localStorage` 的 `useOperatorDraftLibrary`。
- 验证：targeted model、typecheck、全量 Node、`git diff --check` 通过；该迁移同时参加完整 3040 E2E、production build 与 offline shell 门禁，页面保存/刷新流程继续通过。
- 下一刀：先固定并迁出 OperatorDraft SQLite repository 与 share parse/build/merge；显式保存必须 await durable flush，不能沿用页面当前“setItem 后立即成功”的假成功路径。

### 50. Equipment 独立复核收口与正式 STOP

- 复核过程：同一位 Sol ultra 连续执行三轮只读审查。首轮确认百分比二次迁移、保存并发覆盖等 P1 已关闭，但发现同一路径重复 `listAssets()` 会撤销仍被页面持有的 blob URL；第二轮继续发现图片选择器缓存旧 `displayUrl`，使替换后的缩略图悬空、删除项仍可点击、新增项不可见。两轮均保持 NO-STOP，未用局部门禁冒充完成。
- URL 生命周期：新增独立 `webImageObjectUrlRegistry`，按路径、MIME 与内容指纹复用 URL；同内容重复 hydration 不创建、不 revoke、不广播，内容变化先创建新 URL再撤销旧值，删除撤销最后 URL。直接合同还会在重复 hydration 后实际 `fetch` 旧 URL，证明既有消费者未被误伤。
- 页面失效与竞态：Equipment 将首次图片读取和 change subscription 合并；变更时立即推进 revision 并重新 `listAssets()`。单调 request sequence 阻止旧请求覆盖新列表，卸载后取消 state commit 并 unsubscribe；二次同内容 hydration 返回 unchanged，不形成通知循环。
- 真实浏览器合同：在页面实际加载的同一 Vite 模块实例内依次执行同路径内容替换、删除和新增。要求主预览轮换到可 fetch 的新 blob、picker 缩略图同步新 URL、删除项消失、新增项不刷新页面即可出现并可选；测试先证明模块内 `beforeUrl` 等于页面现有 blob，避免测试另一份 registry 的假绿。保存、刷新和分享仍只持久化 canonical `assets/images/...`。
- 编辑矩阵收口：新增统一 cell policy，穷举所有 row kind × 7 列的 editable/clearable/control；three-piece field 与 fixed type 不可 Delete，extraHit effectKey 按 effectKind 动态只读。fixed defense preset 只在用户显式从非 defense 切到 defense 时应用，改名、改 raw、空类型和同类型编辑均保留自定义值。
- 分享选择：确认导入后使用 share 边界返回的首个导入套装，清空旧 equipment/cell selection；浏览器断言导入套装 explorer row 真实带 `is-active`。
- 最终裁决：Sol ultra 在修复后给出正式 STOP，明确无 P0/P1/P2 阻断；每个现存路径只保留一个 object URL，替换/删除均 revoke，fixedStat、Delete/Backspace、extraHit 与 share 选择均未回归。可推迟项仅为把指纹强化为 SHA-256/逐字节比较，以及应用退出时显式 dispose。
- 完整门禁：`git diff --check`、typecheck、全量 Node、repository check（tracked=527）、Timeline smoke、3040 E2E（1 passed，16.4 秒）、production build（244 modules）和 atomic offline shell（`ac6c0e2fac1da99e`，40 files）全部通过；Equipment route chunk 168.18 kB（gzip 24.09 kB）。该次集成门禁同时包含尚待下一提交独立审查的 Operator persistence 边界，Equipment 提交范围仍单独 stage。
- 下一刀：Equipment 第一阶段停止继续拆分；转入 OperatorDraft SQLite persistence、share 与图片 canonical 引用，分别建立合同和独立 STOP。

### 51. 收口 OperatorDraft durable SQLite repository

- 提交证据：当前 LTS 的本地库与分享能力来自 `75dda2dc`、`d531604f` 等连续补丁；旧 slim 的 `5d8f9688` 只作为职责地图参考。其 `useOperatorDraftLibrary.ts` 直接读取 `window.localStorage`，没有 durable flush、并发 revision 或失败回滚，因此明确未移植。
- 失败合同：新增 334 行 persistence 直接测试。最初因新 repository 不存在而红；补 revision 合同时稳定报 `saveDraftRevision is not a function`；独立复核指出删除 flush 失败后的重试漏洞后，测试先收到空 library `{}` 而不是恢复的 `existing`，证明 UI 虽保留弹窗，第二次点击却不会再写 SQLite。
- 纯边界：新增 175 行 `operatorDraftPersistence.ts`，注入 `getItem/setItem/removeItem/flush`；两个既有 key 字节不变。当前草稿读取对空值/malformed 回默认，library 外层 malformed 回空、坏 entry 逐项过滤，其余 entry 继续走当前 `parseImportedDraft` 规范化。
- durable 操作：save 把同一 snapshot 写入 draft key 并 merge 到 library key；share merge 与 delete 同样必须 await flush。测试用 deferred promise 证明 save/merge/delete 在 flush 完成前均不 resolve，set/flush 错误保持原对象 identity 向上抛出，不存在的删除不写、不 flush。
- 失败回滚：save、merge、delete 统一通过 `writeDurablyWithRollback` 保存写前 raw；任一 set/flush 失败都 best-effort 恢复内存值并继续抛原错误，回滚错误不能掩盖首错。删除首次 flush 失败后 library 恢复，第二次点击会真实再执行并成功，不再出现“当前会话看似删除、重启后复活”。
- 保存并发：repository 增加 `current/superseded` revision；页面用 latest ordered draft identity 区分保存期间的新编辑。旧 snapshot 落盘后不覆盖编辑器，若已被新编辑替代只提示“已保存开始时的版本”。同步 `persistencePendingRef` 在任何 library 读取/覆盖判断前拦截重复 Ctrl/Cmd+S，并与 UI pending state 配合禁用保存、导入、删除和覆盖按钮。
- 页面语义：只有 durable 成功才更新本地目录、关闭确认弹窗或显示成功；失败保留 overwrite/import/delete modal 以便重试。Ctrl/Cmd+S 与按钮 handler 显式消费 async promise，不产生 unhandled rejection。页面当前为 1,906 行；本轮增加的 async 安全编排暂不强行塞进旧 slim hook。
- 独立复核：Sol 首轮 NO-STOP，准确找到“flush 失败后 delete no-op”和“锁检查晚于覆盖判断”两个阻断；修复后复验给出正式 STOP，确认写前 raw 回滚、原错误保持、第二次删除真实执行以及重复保存误弹框均闭合，无新增 P0/P1/P2。
- 完整门禁：`git diff --check`、typecheck、全量 Node、repository check（tracked=527）、Timeline smoke、3040 E2E（1 passed，16.2 秒）、production build（244 modules）和 atomic offline shell（`a34df12060073aca`，40 files）全部通过；Operator route chunk 52.51 kB（gzip 13.66 kB）。
- 下一刀：固定 Operator 分享 current/all、outer key/internal ID、malformed entry、skill order 与导入选择语义；随后把用户图片从 `user-images/...` 收敛为 canonical `assets/images/...`。

### 52. 收口 OperatorDraft share 身份与顺序边界

- 原风险：页面技能拖拽只更新 `skillOrder`，但分享预览与文件导出直接使用原始 `draft`，因此可出现 UI 已重排、分享仍为旧顺序；分享导入又保留外层 key 与内部 `draft.id` 冲突，后续载入再保存会复制出第二条记录。旧 slim `5d8f9688` 只是把同一逻辑搬进 localStorage hook，没有解决这两类问题。
- 失败合同：新增 187 行 share 直接测试，最初因模块不存在而红；主审继续要求合法外层 key 能补足缺失内部 ID，修复前该 case 明确返回 empty payload error。最终合同覆盖 current/all、ordered skills、label、格式错误/空有效载荷、null/primitive/array/坏 entry 隔离、外层身份、非法 key fallback、payload 内碰撞、merge 与选择。
- 纯边界：新增 160 行 `operatorDraftShare.ts`，只依赖通用 draftShare 与 Operator model；不导入 React、DOM、window、storage、webImageLibrary、Blob/File/URL。`operator-library-share.v1` 保持不变，invalid file 与“文件类型正确但没有有效干员”返回不同错误。
- 导出顺序：current/all builder 明确接收调用方的 `orderedDraft`；current 只导出当前，all 以 canonical local library 为底、用当前 snapshot 覆盖同 ID 旧值。页面预览与下载复用同一个 share file，不再各写一套 payload 逻辑。
- canonical 身份：外层 key 先规范为小写英文 ID，无效 key 走拼音名称与 `custom-operator-NNN` fallback；canonical ID 在 `parseImportedDraft` 前注入，因此内部 ID 缺失或冲突都由外层胜出。payload 内规范化碰撞按出现顺序追加 `-2/-3`，无效 entry 不占 suffix，不会挤掉后续有效数据；与现有 library 同 ID 则按确认弹窗语义明确覆盖。
- merge 与选择：纯 merge 返回 `{ nextLibrary, importedIds }` 且不重跑现有 library；页面只有 durable merge 成功后才把下拉选择切到首个导入 ID。`currentShareFile` 同时依赖 local draft ID revision，导入/删除后“导出全部”不会保留旧 memo。
- 浏览器往返：真实保存一个 Operator，读取当前分享作为夹具；再通过真实 file chooser 导入 outer=`slim-imported-operator`、inner=`inner-id-must-lose`。确认后下拉 option/value 必须使用 outer ID；不刷新页面打开“导出全部”，预览同时包含原记录与新记录，且新对象内部 ID 与外层一致。
- 独立复核：Sol 最终给出 STOP，无 P0/P1/P2；确认 invalid entry 不占碰撞后缀、同 ID 覆盖符合 UI、orderedDraft 顺序生效、selection 只在持久化成功后更新，merge 双读之间无异步竞态。可推迟项仅为去掉一次只为 importedIds 的额外读取、在确认框展示跳过/覆盖数量。
- 完整门禁：`git diff --check`、typecheck、全量 Node、repository check（tracked=529）、Timeline smoke、扩展后的 3040 E2E（1 passed，16.1 秒）、production build（245 modules）和 atomic offline shell（`5077f3d90178c386`，40 files）全部通过；Operator route chunk 53.18 kB（gzip 14.01 kB），页面降至 1,865 行。
- 下一刀：用真实 Image Manager 用户图片固定 Operator avatar/skill icon 的 canonical 持久引用；随后评估剩余编辑事务与页面组合是否达到 Operator STOP。

### 53. 收口 OperatorDraft 用户图片并正式 STOP

- 原风险：Operator 图片候选把 Image Manager 的 `assets/images/...` 先裁成 bridge 相对路径，再重新拼成旧桌面别名 `user-images/...`；同时只列 release/user，不直接采用图片平台给出的 canonical identity。预览暂时能由兼容 resolver 工作，但新保存与分享仍会继续制造旧别名，和 Web LTS 其他编辑器已经统一的 canonical 路径漂移。
- 纯边界：新增 `operatorDraftImageOptions.ts`，只从 `ImageAssetEntry.relativePath` 构造候选；过滤目录与空路径、同路径去重并按中文数字自然排序，不把 `publicUrl/blob:`、`canonicalPath` 或旧 `user-images/...` 写入业务数据。直接合同覆盖 release/user/builtin 同路径、目录、空值、旧别名和临时 blob 隔离。
- 兼容策略：用户点击候选项时保存 canonical `assets/images/...`；输入框仍允许手填旧路径或外部 URL，已有 `user-images/...` 草稿继续由图片 path index/resolver 兼容，没有做破坏性批量改写。
- 真实浏览器合同：先在 Image Manager 通过 file chooser 导入 1×1 PNG，再在 Operator 新建草稿中分别选择头像和新技能图标；两处 input 必须保存 `assets/images/slim-operator-user-image.png`，运行时预览必须解析为 `blob:`。保存并刷新后 canonical 值与 blob 预览均恢复，当前分享 JSON 的 `avatarUrl`、技能 `iconUrl` 仍是 canonical 路径；既有 outer-key 导入、选中和立即“导出全部”往返继续通过。
- 独立复核：Sol 对图片和 Operator 整体分别给出正式 STOP，未发现 P0/P1/P2。inline editing 没有直接污染 state，ID/重排/hitCount/skillOrder 已归 model，保存、JSON 与分享均消费 ordered draft；剩余大块主要是 JSX 与页面编排，继续照搬旧 `OperatorDraftFields` 只会移动代码，不能形成新业务边界。
- 旧 slim 取舍：不复制旧 `useOperatorDraftLibrary`；它直接使用 `window.localStorage`，缺少当前已经建立的 SQLite durable flush、失败回滚和 revision 保护，并把过多页面状态塞进一个 hook。只有未来支持页面不卸载时实时修改图片库、编辑事务明显继续复杂化或需要独立视图性能/组件测试时，才分别考虑图片 change subscription、纯 editing transaction 或视图拆分。
- 完整门禁：`git diff --check`、typecheck、全量 Node、repository check（tracked=531）、Timeline smoke、3040 E2E（1 passed，17.6 秒）、production build（246 modules）和 atomic offline shell（`ed4f8483c5138a4b`，40 files）全部通过；Operator route chunk 53.24 kB（gzip 14.03 kB）。Operator 第一阶段到此停止继续拆分。
- 下一刀：转入 BuffBatch 与 SkillButton；仍先用当前 LTS 行为和提交历史判断真实稳定边界，不按旧 slim 文件清单机械搬运。

### 54. 固定 BuffBatch / SkillButton 当前 LTS 行为并完成第二轮历史审查

- 历史定位：BuffBatch 旧 slim 只有 `5087af66`，迁出 340 行 model、983 行 controller hook 和 87 行叶子按钮；SkillButton 由 `c202338a`、`bee921d4` 迁出 damage/info/context view、interaction、damage runtime 和一个近 90 个返回字段的总控 runtime。前者的职责分组可参考，后者证明“文件变短”但没有真正降低耦合，本轮禁止照搬总控 hook。
- LTS 补丁保护：BuffBatch 必须保留 `342416ab` 的 Web 图片解析与 `6f21374d` 的 skill type/mask；SkillButton 必须保留 `9aea09d7` 异常倍率修复，以及 `1e6d8b3b`、`6f21374d`、`99f377d0`、`67e35603`、`51dc3eb2` 的主题 data attribute、三段 outline、mask 与渐变边光。旧代码中的 `127.0.0.1:31457` 和桌面手册均不进入当前实现。
- 纯 characterization：新增 `buffBatchEditModel.test.ts`，直接测试当前页面已有函数；生产逻辑只增加 test export。合同覆盖候选模式循环、legacy/current staff/line、显式与回退位置、排序、最小 group 数、反向框选矩形与边界相交、Buff label/source/value、自然排序、来源过滤、候选完整字段、异常状态候选、去空去重和输入不变。
- 真实浏览器合同：在既有狼卫 Timeline 按钮上进入批量工作台，先普通选择/取消/清空，再用真实 mouse drag 框选；选择一个动态发现且尚未拥有的 Buff，分别证明 pending `+1` 的取消不落数据、确认后 count `+1`、筛选命中同一按钮、pending `-1` 的取消不落数据和确认后 count 恢复。回到排轴后 route、主题与三段 SVG 继续成立；详情明确显示新 `TimelineSkillDetailWorkbench`，旧 `.skill-button-modal-pair` 数量为 0。E2E 独立提交为 `73a66f9b`，1 passed（18.2 秒）。
- 不可达代码证据：`TimelineSkillDetailWorkbenchProps` 虽从 `5a1cd8c6` 保留 `children` 类型，但当前组件从未解构或渲染 children；`SkillButton.tsx` 仍传入约 500 行旧信息/伤害 modal JSX。旧 slim 又把这批不可达 JSX精心迁到 `SkillButtonInfoModal` / `SkillButtonDamageModal`，因此本轮应删除而不是复刻。
- 独立 NO-STOP：Sol 未发现 P0/P1，但确认三个 P2。BuffBatch 把权威空 Timeline `[]` 当读取失败并 fallback 到全局旧表；SkillButton 的 panel base 和 panel state 不随 `resistanceRevision` 刷新；删除 Buff 后持久化 panelConfig 中的 global disabled、分段 disabled 与手工层数仍可在重加同 ID 时复活。旧 slim 三条均未解决。
- 验证：BuffBatch targeted characterization、全量 Node、typecheck、`git diff --check` 与扩展后的 3040 E2E 全部通过；本批只建立护栏和审查结论，不把当前可用误报为 STOP。
- 下一刀：先分别用失败合同证明权威空 Timeline、panel revision 和 Buff override 清理，再作独立修复；随后迁 BuffBatch 纯 model/controller/叶子 view，并删除 SkillButton 不可达旧 modal 后重做窄职责 controller。

### 55. 区分 BuffBatch 权威空 Timeline 与 fallback

- 独立复核 P2：旧页面及旧 slim `5087af66` 都用 `visualButtons.length > 0 ? visualButtons : globalTable`；因此当前 Timeline 合法存在但没有按钮时，会显示并可修改全局表中其他 Timeline 遗留的按钮。
- 失败合同：characterization 增加显式 authority 输入，要求 `timelineData=null` 返回 `null`，而 `{ staffLines: [] }` 返回 `[]`；同时用非空 Timeline 证明 persisted button 配置仍覆盖 timeline `buffIds`。实现前稳定红在 `projectVisualSkillButtons is not a function`。
- 修复：新增纯 `projectVisualSkillButtons({ timelineData, table, selectedCharacters, gridContentOffsetX })`；它不自行读取 storage。页面 adapter 才读取 session Timeline 与 button table，`visualButtons` state 改为 `array | null`，仅 `null` 使用全局 fallback，权威空数组保持空工作台。
- 行为保持：非空 Timeline 的 runtime skill、位置、图标、custom hit、persisted selectedBuff/panel/runtime snapshot 合并逻辑未改；add/remove/edit 确认后的刷新继续通过同一 adapter。
- 验证：新增 authority targeted 合同由红转绿；完整 BuffBatch model characterization、typecheck、`git diff --check` 与包含普通选择、真实框选、增删取消/确认、筛选、回排轴和详情路由的 3040 E2E（1 passed，18.4 秒）全部通过。
- 下一刀：把当前页已固定的纯投影/几何/候选逻辑迁入 LTS-native model，再迁叶子按钮与 controller；迁移不得重新引入 `length > 0` fallback。

### 56. 完成 BuffBatch 的 LTS-native 职责拆分并正式 STOP

- 参考范围：旧 slim 的 `5087af66` 只作为职责地图，确认 model/controller/叶子按钮是可复用边界；实际代码从当前 `f31ba3ab` 逐段迁出，没有复制旧分支实现，也没有恢复 Electron-era 图片路径或旧的 `length > 0` fallback。
- 纯模型：新增 338 行 `buffBatchEditModel.ts`，集中 Timeline 投影、legacy/current 坐标兼容、排序、框选几何、Buff 展示/来源过滤、候选转换与异常状态候选。模型不导入 React、DOM、浏览器 storage 或 repository；runtime skill 和 skill icon 通过调用方注入。
- 控制器与视图：新增 1,039 行 `useBuffBatchEditWorkbench.ts`，集中状态、session/repository adapter、候选刷新、add/remove/edit 模式和 pointer/keyboard 生命周期，并按 `layout/buttons/filters/modes/candidate/catalog` 六组返回；主页面降至 784 行，仅负责组合渲染。104 行 `BuffEditSkillButton.tsx` 成为叶子视图，不把状态机塞回页面。
- LTS 补丁保护：`projectVisualSkillButtons` 继续以 `null` 表示无权威 Timeline、以 `[]` 表示权威空 Timeline，controller 只对 `null` fallback；技能/头像/武器图片继续走 Web resolver，`data-skill-type`、图标 mask、加载失败文字 fallback 和主题 DOM 均保留。
- 测试接线：295 行 characterization 直接导入新 model，不再通过 React 页面获取纯函数；普通点击、真实框选、Buff 增删 pending 的取消/确认、筛选、回排轴、详情路由和主题结构继续由 3040 Playwright 合同覆盖。
- 独立复核：Sol xhigh 将 controller、view 与 `f31ba3ab` 原文件逐段比较，确认 runtime skill adapter 的 sandbox 参数、权威空 Timeline、Web 图片与 mask/data attribute 全部等价，未发现 P0/P1/P2，正式给出 STOP。控制器仍长但职责清楚，不因行数本身继续拆分。
- 完整门禁：targeted model、`git diff --check`、typecheck、全量 Node、repository check（tracked=537）、Timeline smoke、3040 E2E（1 passed，18.9 秒）、production build（250 modules）和 atomic offline shell（`b1e631a66a3d95cf`，40 files）全部通过。
- 下一刀：转入 SkillButton；先用失败合同关闭 panel revision 与删除 Buff 后持久化 override 复活两个 P2，再删除确认不可达的旧 modal children，最后只按窄职责抽取交互/伤害边界。

### 57. 关闭 SkillButton panel revision 与 stale Buff override 两个 P2

- 持久 override 失败合同：新增纯 helper 测试，最初因 `skillButtonPanelConfig.ts` 不存在稳定红；最终覆盖 `selectedBuff`、button stack、panel selected、全局禁用、分段禁用、分段手工层数、空 segment 清理、stale-only 引用、无 panelConfig、输入不可变，以及 `manualDisabledHitKeys/runtimeSnapshot` 引用保持。
- 中央修复：`detachBuffFromSkillButton` 以 Buff identity 一次性清理所有持久引用，`removeBuffFromButton` 在重算面板前统一调用。因此 SkillButton、BuffBatch、Workbench 命令、Hook alias 和 decrement fallback 都受益；不是只在一个 React 右键入口临时清 state。
- UI 同步：SkillButton 删除后从 repository 重新加载 manual/global/stack override，再加载 Buff 列表和 panel；不会出现存储已清、当前详情仍保留旧开关的双状态。浏览器先在当前 Vite module 中给真实新增 Buff 写入三类 override，再通过 BuffBatch pending/确认删除，最后直接核对持久按钮中五处引用全部消失。
- panel revision 失败合同：详情保持打开时，浏览器把当前狼卫 operator cache ATK 从 `674.73` 提高到 `10674.73`，再通过真实“批量设置敌方抗性 → 应用到全部按钮”推进 revision。修复前 runtime snapshot 已更新为 `23624.73`，但页面摘要持续停在“期望 5201”直至 15 秒超时，证明不是测试没写进 storage，而是详情仍用旧 panel base。
- 纠错记录：第一次修补误把 `resistanceRevision` 加到了同文件更早的 `loadSkillLevelModeMap` memo，真正的 `panelBase` 依赖仍未变化；新的浏览器合同准确把这次错误抓红。最终由 `panelBase` 依赖 revision，并用 `runtimeDamageRevisionRef` 区分真实 revision 与普通父级重渲染：已打开详情/inspect 只在 revision 变化时完整刷新，首次打开仍由原 effect 只加载一次。
- 独立复核：Sol xhigh 确认 helper 不误删无关字段，中央 service 覆盖所有单 Buff 移除路径，React state 与 repository 同步，E2E 的 mutation 使用页面实际加载模块而删除/确认/revision 走真实 UI handler；未发现 P0/P1/P2，正式 STOP。
- 完整门禁：helper targeted、`git diff --check`、typecheck、全量 Node、repository check（tracked=537）、Timeline smoke、扩展后的 3040 E2E（1 passed，19.1 秒）、production build（250 modules）和 atomic offline shell（`e2b4a914836d8ddb`，40 files）全部通过。
- 下一刀：删除 `TimelineSkillDetailWorkbench` 从未读取的约 500 行旧 modal children，并由 typecheck 反向清掉只服务于不可达 JSX 的 state/derived callback；随后再判断现存交互与伤害逻辑是否需要窄职责拆分。

### 58. 删除 SkillButton 被新 Workbench 取代的不可达旧弹窗

- 可达性证据：`TimelineSkillDetailWorkbenchProps` 声明了 `children?: ReactNode`，但组件从未解构、读取或渲染 children；浏览器合同也已持续证明新 `.timeline-detail-layer` 可见而旧 `.skill-button-modal-pair` count 为 0。因此 SkillButton 传入的技能信息、抗性、Buff、Hit 伤害和信息快照 JSX 全部不会进入 DOM。
- 历史取舍：旧 slim 的 `c202338a` 曾把同一批旧 JSX 迁成 `SkillButtonDamageModal` / `SkillButtonInfoModal`，形式上缩短主文件，却把已经失去入口的实现继续永久维护。本轮只用旧提交确认来源，不复制这些组件。
- 实际删除：`TimelineSkillDetailWorkbench` 改为自闭合，删除 children prop；再由 typecheck 反向删除旧弹窗独占的 `isExpanded/isAnomalyFormulaExpanded/isTargetResistanceExpanded`、active normal/anomaly Buff options、toggle callback、render helper、`buildAnomalyBuffOptionsBySegmentKey` 和 `DeferredNumberInput`。新 search layer、Hit tuning、异常公式、状态选择、目标抗性组件和主题 DOM 均保留。
- 规模变化：两个文件合计 2 insertions / 613 deletions；`SkillButton.tsx` 从 2,730 行降至 2,120 行，Workbench 从 714 行降至 713 行。这是功能替代后的真实删除，不把代码移到新文件制造虚假瘦身。
- 完整门禁：`git diff --check`、typecheck、全量 Node、repository check（tracked=537）、Timeline smoke、扩展后的 3040 E2E（1 passed，18.7 秒）、production build（250 modules）和 atomic offline shell（`706fefb0243425aa`，40 files）全部通过；主 entry chunk 由上一提交的 294.23 kB（gzip 81.16 kB）降至 274.07 kB（gzip 77.65 kB）。
- 下一刀：对剩余 2,120 行按真实依赖重新审查；优先考虑可直接 characterization 的 presentation/search 纯规则和独立 context/interaction 叶边界，明确禁止复制旧 slim 近 90 返回字段的 `useSkillButtonRuntime`。

### 59. 用异常倍率集成 golden 正式停止 SkillButton 拆分

- 独立架构复核：Sol xhigh 对剩余 SkillButton、搜索层、右键菜单、点击/长按、持久 tweaks 与伤害派生重新审查；结论为无 P0/P1/P2，搜索模型和交互叶边界都不是本阶段阻断项，旧 slim 近 90 返回字段的总控 Hook 不应复制。
- 唯一缺口：LTS 补丁 `9aea09d7` 已在底层 `buffZoneCalculator` 固定技能倍率使用 `(基础倍率 + 加算) × 乘算`，但此前没有直接覆盖 `skillButtonAnomalyDamage.ts` 的集成路径。
- 测试留痕：新增直接 golden，以固定 1000 攻击、40% 倍率加算和 1.2 倍乘算，分别锁定普通异常、燃烧持续段与额外伤害的 `multiplierText`、公式文本、取整后的非暴击文本和未取整数值。生产代码未修改。
- 正式 STOP：继续抽右键菜单或搜索 JSX 只会移动代码，不会减少真实职责或风险；BuffBatch 与 SkillButton 的 LTS-native 第一阶段拆分到此完成。
- 完整门禁：targeted golden、`git diff --check`、typecheck、全量 Node、repository check（tracked=538）、Timeline smoke、3040 E2E（1 passed，18.8 秒）、production build（250 modules）和 atomic offline shell（`706fefb0243425aa`，40 files）全部通过。
- 下一阶段：分别审查 Damage Sheet、ExcelJS、CanvasBoard 和 OperatorConfig；每项先形成保留、删除或窄拆决定，再写对应合同，不把四项揉成一次重构。

### 60. 对最后四项形成 LTS-native 决策（中间判断，已废止）

> 2026-08-03 纠错：本节遗漏了用户已经在旧提交链中确认的产品决定。下列 ExcelJS KEEP 与 Damage Sheet MINIMAL SPLIT 不再有效；最终口径见“当前有效产品决定”和第 64 节。保留本节只为诚实记录判断如何发生和被测试、提交证据纠正。

- 独立架构复核：Sol xhigh 对当前 LTS、旧 slim `68977bc9/f34930de/37809ac8` 和后续补丁重新比较。旧 slim 通过删除 ExcelJS、XLSX 与整个 Damage Sheet 获得体积下降；CanvasBoard 只少了约 5 行入口，OperatorConfig 基本未处理，因此不能把旧最终树当实现答案。
- ExcelJS：KEEP。XLSX 是当前可达的真实导出能力；只从普通前端投影路径移除，最终改为用户点击导出时再加载。
- Damage Sheet：MINIMAL SPLIT。保留页面、格子相关 Buff 与 XLSX；删除写死 `false` 后仍执行 Hook 的 advanced sidebar，再以当前 LTS 为源迁出纯投影。拒绝复制旧 `damageSheetPageModel.tsx` 的大规模搬运。
- OperatorConfig：MINIMAL SPLIT。页面与后台刷新各自维护 Equipment normalize，已经发生页面丢 `fixedStat`、后台保留的实际漂移；先用同输入双路径合同修 P1，再收敛共享投影。
- CanvasBoard：MINIMAL SPLIT。保留 Timeline/SQLite/Work Node 编排；先删除从未被置为 `true` 的旧 `ReportTab` modal，并修复 Workbench 仍展示但 Canvas 永久声明不可用的“计算侧栏”P2。高风险 command/archive controller 没有足够自动验收前不做巨型 Hook 搬家。
- 审查严重度：无 P0；发现 Damage Excel 公式与 OperatorConfig `fixedStat` 两个 P1，以及失效计算侧栏一个 P2。

### 61. 从普通 Buff 投影移除 ExcelJS，并修复 XLSX 乘区公式（历史过程，能力已退役）

> 此处公式修复曾正确描述当时实现，但不构成继续维护 XLSX 的理由。专用导出器及其 round-trip golden 已随 `969dab32` 删除；共享网页/PPT 伤害计算与异常倍率 golden 继续保留。

- Buff 投影：现有 `buffDraftWorkbook.test.ts` 已完整固定列、分组、地址、合并宽度、alignment 与 source mapping；据此把“构造 ExcelJS Workbook 再读成前端 view”的绕路改成直接构造相同 view，删除 97 行、增加 31 行，生产输出不变。
- 构建变化：Buff lazy chunk 从 63.36 kB（gzip 17.67 kB）降到 61.56 kB（gzip 17.23 kB）。ExcelJS 只剩 Damage Sheet 静态消费方后暂时被 Rollup 合入该 route，形成 1,002.88 kB（gzip 287.71 kB）的单 chunk；与此前 Damage 65.34 kB + ExcelJS 936.59 kB 的合计加载量相当，后续 Damage 阶段必须改为点击导出才动态加载。
- Excel P1 红证据：新 workbook golden 最初读到 `PRODUCT(1.5) * (1 + 0.16)`，公式结果为 1.74，与网页非技能乘区 `1 + 0.16 * 1.5 = 1.24` 不同；缓存 result 虽是 1.24，但工作簿开启 full recalculation，用户打开 Excel 后会重算成错误值。
- 修复：非技能结构化乘区统一生成 `base + additiveTotal * multiplierProduct`；技能倍率继续保持 `(base + additiveTotal) * multiplierProduct`。golden 同时固定 1.24 与 `(3.3 + 0.4) * 1.2 = 4.44`，并执行 `writeBuffer` 后重新读取工作簿验证公式和缓存结果。
- 完整门禁：typecheck、全量 Node、repository check（tracked=539）、Timeline smoke、3040 E2E（1 passed，19.1 秒）、production build（250 modules）和 atomic offline shell（`2527c4359f940037`，39 files）全部通过。
- 下一刀：以同一 Equipment fixture 锁定 OperatorConfig 页面和 snapshot refresh 的 `fixedStat/effects/三件套/ConfigSnapshot` 一致性，再修第二个 P1。

### 62. 统一 OperatorConfig 的 Equipment 投影并修复 fixedStat 漂移

- 红证据：当前 LTS 页面和后台刷新各维护一份 Equipment normalize；同一份带固定属性的装备输入，页面路径得到 `fixedStat: undefined`，后台刷新则保留 `{ typeKey: 'defense', value: 56 }`。这是实际数据漂移，不是单纯重复代码。
- 单一边界：新增 `operatorEquipmentLibrary.ts`，集中负责 Equipment library normalize、装备查找、等级值读取和三件套 Buff 投影；`OperatorConfigPage` 与 `operatorConfigSnapshotRefreshService` 都改为调用它，不再分别复制约 200 行规则。
- LTS 语义保持：非最后一个实际存在的 `mainStatBoost/subStatBoost` 继续投影为固定能力值，最后一个继续保留百分比；modifier/extraHit 三件套、旧单个 `threePieceBuff` fallback、等级数值解析和 storage key 均未改变。
- 修复：共享 normalize 明确保留 `fixedStat`。同一 fixture 同时经过页面模型等价路径和公开的后台 `applyOperatorEquipmentSelectionsToSnapshot`，直接比较固定属性、装备 effects、三件套和最终 `ConfigSnapshot` 数值，防止两条路径以后再次分叉。
- 完整门禁：targeted 合同、typecheck、全量 Node、repository check（tracked=541）、Timeline smoke、3040 E2E（1 passed，18.8 秒）、production build（251 modules）和 atomic offline shell（`2233f8239b9c4521`，39 files）全部通过；Operator route chunk 为 45.00 kB（gzip 11.81 kB）。
- 提交：`cb799117 fix(operator-config): share equipment normalization`。OperatorConfig 的最终窄拆完成，后续不再做无行为收益的页面搬家。

### 63. 删除 CanvasBoard 不可达 ToolPanel 与报表弹窗

- 提交证据：`d359bfc9` 从 2026-05-08 起用 `isCandidatePanelEnabled = false` 永久阻断 ToolPanel；`f9251af0` 又在 2026-06-20 把“计算伤害”改为跳转独立 PPT 报表路由，此后 `isReportModalOpen` 再无任何写 `true` 的路径。`b603cba6` 后来重新展示“计算侧栏”，实际点击只改标签/高亮，右区仍是 SkillSandbox。
- 红证据：3040 浏览器打开真实 Workbench drawer，新增合同先稳定收到一个“计算侧栏”按钮而预期为 0；这直接固定了用户可见的失效入口，不使用源码字符串代替行为。
- 收口：删除 `toolPanel` mode、`forceShowToolPanel`、CanvasBoard→CanvasArea→SkillButton 的旧 modal callback、不可达 ReportTab modal，以及零生产引用的整个 `components/ToolPanel` 和两个专用旧 Hook；候选 Buff repository、storage key 和 SkillButton/BuffBatch 当前刷新链明确保留。
- 样式：删除 CanvasBoard 内旧 report sheet/modal CSS、四套主题中的孤儿 ToolPanel/report 选择器和两个 Liquid Tide surface rule；精确零引用的旧 `tab-content-*`、DamageTab/ReportTab 子选择器也同步清零，独立 Damage Sheet 与 PPT 报表样式不受影响。
- 浏览器闭环：drawer 不再显示假入口；真实技能按钮仍可双击进入 `TimelineSkillDetailWorkbench`，关闭后 URL 回到 `/timeline`、Canvas 与 SkillSandbox 继续可见，再次双击仍能重开详情。
- 独立复核：Sol xhigh 对提交历史、可达入口和删除面逐项复核，无 P0/P1，并明确给出继续删除结论；其指出的 P2 孤儿 Hook/主题规则已在同轮全部关闭。
- 完整门禁：typecheck、全量 Node、repository check（tracked=533）、Timeline smoke、3040 E2E（1 passed，18.9 秒）、production build（243 modules）和 atomic offline shell（`30246dd144149ddc`，39 files）全部通过。删除 2,820 行；主入口 JS 274.07→262.28 kB（gzip 77.65→75.18 kB），Canvas CSS 45.70→43.02 kB（gzip 7.42→7.03 kB）。
- 提交：`24fead1f refactor(canvas): remove unreachable tool panel`。CanvasBoard 第一阶段正式停止继续拆分。

### 64. 按最终产品决定退役 Damage Sheet、XLSX 与 ExcelJS

- 纠错依据：用户明确指出三者早已确定全部删除。重新阅读 `68977bc9` 与 `37809ac8` 后确认：前者是“先删 XLSX/ExcelJS”的过渡，后者才是“整页退役、PPT 保留”的最终决定，覆盖第 60 节的错误中间判断。
- 删除范围：删除 2,650 行 `DamageSheetPage.tsx`、1,352 行专用 Excel model/exporter、178 行已失去产品意义的 XLSX round-trip test、公开 route/lazy preloader、画布“表格”、Workbench“伤害表”和 Workbench command 枚举；`exceljs` 从 package 与 lock 中移除，并随之清掉 90 个依赖包。
- 样式解耦：原 `DamageSheetPage.css` 同时被 Buff、Weapon、Equipment、Image Manager 复用，不能整份删除。依据旧提交把仍可达的 440 行公共外壳改名为 `WorkbookSheet.css`，由四个页面显式导入；Damage 独占规则随旧页清除。
- 保留边界：`DamageReportPptPage`、`damageReportService`、Toolbar“计算伤害”、SkillButton 详情、抗性区、异常/燃烧/Dot/额外 hit 与共享 Buff 乘区计算全部保留。E2E 不只检查按钮存在，还实际点击进入 PPT 并返回时间轴。
- 测试发现与修复：新增 PPT 点击闭环最初发现全局悬浮菜单遮住左上“返回”按钮；没有用 force click 掩盖，而是给 PPT toolbar 预留 launcher 安全区，并移除 Liquid Tide 的重复偏移。
- 结果：提交 `969dab32 refactor(damage-sheet): retire legacy worksheet` 为 26 insertions / 5,464 deletions。此前 1,002.88 kB（gzip 287.71 kB）的 Damage/ExcelJS chunk 完全消失，生产构建不再出现大于 500 kB 的 chunk 警告，模块数 243→237；`npm audit` 为 0 vulnerabilities。
- 完整门禁：`git diff --check`、typecheck、全量 Node、repository check（tracked=529）、Timeline smoke、3040 E2E（1 passed，18.8 秒）、production build（237 modules）和 atomic offline shell（`0ad9630986674d1c`，36 files）全部通过。
- STOP：Damage Sheet/XLSX/ExcelJS 不再是待拆模块，也不是后续兼容目标；旧规格只作历史记录，不得据此重新实现。至此 1.8 LTS slim 第一阶段全部完成。

### 65. 完成原 LTS 与 1.8 slim 的最终同套双跑

- 目标：不再用“改前测试 + 改后测试”代称双跑，而是在同一个 Playwright runner 中，对未瘦身 LTS 和当前 slim 执行完全相同的公开流程并直接比较结果。
- 固定拓扑：原 `v1.8-LTS@920a6f94` 运行于 `3030`，slim 运行于 `3040`，没有启动 `3041` 或其他第三端口。两个目标分别使用全新隔离 context，测试不依赖手工缓存。
- 入口纠错：第一次切回 `3030` 后合同在登录首页立即变红。进程 cwd 证明该端口当时运行的是旧 `codex/core-file-slimming-research-20260721@b4acc204`，不是 LTS；只替换该错误的 `3030` 进程为真实 LTS，保持 `3040` 不动。这个红灯证明测试能阻止错误基线被误记为通过。
- 自动入口：提交 `0b2bb094 test(lts): add final dual-branch acceptance` 新增 `scripts/run-lts-dual-e2e.mjs`、`tests/e2e/lts-dual-run.spec.ts` 和 `npm run test:e2e:lts-dual`。runner 默认只使用 `3030/3040`；当 `3030` 未运行时只会从明确的 `v1.8-LTS` worktree 启动该端口。
- 公共合同：访问密码与首次安装计数、七个 lazy route、四类编辑器的创建/保存/刷新/分享、Timeline 选四人和拖入真实技能、SkillButton DOM/详情/刷新恢复、完整伤害汇总与计算过程、PPT 报表总伤害、OperatorConfig、BuffBatch 选择，以及两端零 page/console error。两端 common observation 必须整体 `toEqual`。
- 差异白名单：仅允许旧 Damage Sheet route、XLSX 导出、“表格”、“伤害表”和失效“计算侧栏”在 LTS 为真、在 slim 为假；不能通过忽略任意 UI 文本或路由来隐藏额外漂移。
- 正式结果：`npm run test:e2e:lts-dual` 在正确的 `3030/3040` 上 `1 passed (18.5s)`。先前为调试临时使用 `3041` 的结果明确作废，不计入本节。
- 双边门禁：原 LTS 的 typecheck、全量 Node、repository check（tracked=458）、Timeline smoke、production build（215 modules）和 atomic offline shell（40 files）全部通过；slim 的 typecheck、全量 Node、repository check（tracked=531）、Timeline smoke、完整 3040 E2E（1 passed，19.4 秒）、production build（237 modules）和 atomic offline shell（36 files）全部通过。
- 独立复核：Sol xhigh 先审查可比矩阵与差异白名单，要求公共结果精确比较并把 release-only 平台场景单列。本轮据此补入完整计算过程文本对照；SQLite 导入导出、真实断网 Service Worker 和双标签接管继续留在平台终验，不被本次双跑虚报为已覆盖。
- 结论：当前 slim 在本次公共前端矩阵内与原 LTS 功能一致；观察到的差异全部是已确认的 Damage Sheet/XLSX/ExcelJS 退役结果，没有发现新的功能回归。

### 66. 完成四个复杂编辑器的第二阶段真实减重

- 原则：本轮只减少重复实现，不改变 Buff、Weapon、Equipment、Operator 的业务规则、状态边界、保存格式、交互顺序或主题敏感 DOM。旧 slim 的巨型 Controller/View 拆分只作线索，不作为正确答案。
- 试验取舍：实际试做过公共 Header、Ribbon、DragPreview、ImagePicker 和 Operator ViewParts；这些拆法虽然让主文件变短，却增加新文件、prop 管道或 lazy chunk gzip，已在提交前完整撤回。最终只保留能让生产包真实变小、且语义可由现有合同证明不变的合并。
- 公共外壳：新增 `WorkbookContextMenu`，只合并 Buff/Weapon/Equipment 真正重复的菜单渲染和 SVG 路径；仍保留 workbook 与 equipment 原有的图标、包裹层、事件阻断和定位值差异。`WorkbookToolButton` 的相同 SVG 改为路径数据映射，按钮层级和 Liquid Tide 直接子节点不变。
- 页面内减重：Buff 公式文本框直接复用已有并已测试的 `formulaTextBinding`；Equipment 将五类 select 分支先归一为原值与原选项，再只渲染一次 select；Operator 用本地字段描述表生成五个基础下拉，复用现有元素/技能按钮常量，并合并两份完全相同的确认弹窗外壳。Weapon 除共享菜单渲染外不动高风险拖拽和图片状态机。
- 主题适配：双跑逐侧覆盖 `office-excel`、`apple-midnight`、`apple-warm`、`lieflat-mono`、`liquid-tide` 五个主题和四个编辑器，精确比较根 class、关键节点数量、计算样式、主题 token、关键输入/表格交互，以及 Liquid Tide 按钮的 `data-liquid-glass-*` 标记。四列 Operator 布局、topbar/ribbon/workspace 层级和直接按钮子节点均保持原合同。
- 测试补强：同一份双跑新增 Weapon 图片搜索、选择、预览和清空；Equipment 对所有内联 select 的当前值与完整 option 签名作两侧精确比较；Operator 对职业、武器、元素、主/副属性保存刷新，以及覆盖弹窗取消/确认作两侧实操。共享菜单的 workbook/equipment 两种渲染分支继续分别由 Buff 和 Equipment 的真实右键点击流程覆盖。
- 基线遗留：原 LTS 的 Weapon 菜单在已滚动条目上可能被页面级 pointer/scroll listener 提前关闭；这是瘦身前既有行为。本轮没有借减重之名修改它，也没有用 force click 把它伪装成新验收结果。
- 体积：相对本轮实现基线 `7758bb4f`，受影响的四个 lazy chunk 与共享 chunk 合计由 336,052 降至 329,564 raw bytes、由 72,687 降至 71,977 gzip bytes；全站 JS 同样减少 6,488 raw bytes、738 gzip bytes。实现提交为 284 insertions / 413 deletions，其中生产源码净删 174 行。
- 验收：`git diff --check`、typecheck、全量 Node、repository check（tracked=534）、Timeline Bundle V2 smoke、production build（240 modules）、atomic offline shell（`855cb1ad55fe101f`，37 files）、3040 完整浏览器回归（1 passed，18.7 秒）全部通过；固定 `3030/3040` 的同套双跑为 1 passed（30.7 秒），两侧公共 observation 完整相等且均无 page/console error。
- 提交：`46c062af refactor(workbook): slim repeated editor markup`。本批已经完成可证明的结构减重；不继续为了缩短单文件而制造新的展示层或状态搬家。

### 67. 二次复核并归档后续瘦身与代码质量风险

- 复核基线：2026-08-03 在干净的 `codex/v1.8-lts-slimming@beaa2ba4` 上重新执行完整检查，没有修改产品代码，也没有使用旧 slim 最终树代替当前 LTS 事实源。
- 完整门禁：`npm run check` 全绿；repository check 为 `tracked=534 syntax=14 data=100 images=559 docs=19`，依赖审计为 0 vulnerabilities，typecheck、全量 Node、Timeline Bundle V2 smoke、240 modules production build 和 atomic offline shell（`855cb1ad55fe101f`，37 files）全部通过。额外严格执行 `tsc --allowUnreachableCode false --noUnusedLocals true --noUnusedParameters true` 也通过。
- 浏览器复跑：固定使用现有 `3040` 完成 slim 细流程，结果 `1 passed (19.6s)`；随后继续使用固定 `3030/3040` 完成原 LTS 与 slim 的同套双跑，结果 `1 passed (31.2s)`。没有启动第三个端口，公共观察结果没有新增漂移。
- 结论边界：以上绿灯继续证明正常用户路径和既有双侧合同成立，但不会覆盖数据库写失败、并发 CAS、旧提交复用、回滚后重载、异步缓存覆盖或 Buff `pointercancel/blur`。本节的质量问题来自第二轮源码控制流复核；尚未新增对应失败测试，不能把现有绿灯误写成这些问题不存在。

#### 已确认的后续瘦身面

1. **统一 Buff 属性元数据。** `fireFragile/fireVulnerability` 等同一套名称和分组至少散落在 `buffDraftCatalog.ts`、`weaponDraftCatalog.ts`、`equipmentSheetModel.ts`、`operatorDraftBuffModel.ts`、`operatorDraftPageModel.ts` 和 `operatorPanelCalculator.ts`。漂移已经发生：Buff catalog 把灼热 Fragile 标成“脆弱”、Vulnerability 标成“易伤”，其余模型与 `buffCalculator.ts` 的 `Fragile=易伤、Vulnerability=脆弱` 相反。后续应建立唯一 canonical registry，再由各编辑器增加自身 keywords 或展示差异；不得只批量替换中文文本。
2. **删除 OperatorConfig 已归档死层。** `EQUIPMENT_FORM_ROWS` 只服务于 `display:none` 的 `.operator-config-page-equip-archive`，内部主体又全部处于 JSX 注释；角色解析 effect 还重复构造三份 Map 后仅输出 `console.log`。这批可以在不改装备 UI、快照或数值规则的前提下直接删除。
3. **收敛 Canvas 命令完成外壳。** `CanvasBoard/index.tsx` 当前有 28 处 `patchMainWorkbenchCommand(commandEntry.id, ...)` 状态写入；除 running/error 外，约 26 个分支重复“标 done → push result”。后续只抽局部 `complete/settleCommand`，保留现有按 `command.op` 显式分派和类型收窄。
4. **收敛可证明重复的页面内事务。** `SkillButton` 的 persisted `panelConfig` 合并写入、BuffBatch 的目标计数/碰撞矩形/确认收尾，以及 Canvas 三种 Operator 配置命令的 snapshot-refresh-checkout-rollback 外壳可分别收敛。只共享事务骨架或纯模型，不合并 hit/异常伤害、不同 Buff 操作或命令字段解释。
5. **共享编辑器无领域基础设施。** Buff、Weapon、Equipment 的分享 modal controller 和 Buff/Weapon/Equipment/Operator 的 Blob/URL JSON 下载过程可复用；Weapon/Equipment 图片选择器只适合共享纯展示层。不得把四个编辑器改成一个通用 schema editor，也不得用主文件行数作为验收。
6. **清理主题重复所有权。** Liquid Tide 外层 lazy gate 已订阅主题，两个内层 hook 又各自订阅；主题 CSS 中存在同选择器后写覆盖、完全重复规则和无 DOM 生产者的退役详情 selector；两个 snapshot hook 还重复 canvas copy/release/LRU 原语。这里只删除可由 selector、调用图或纯函数合同证明的重复，不因 CSS 文件长而重排级联层。

#### 已确认的代码质量风险

| 级别 | 问题 | 已确认的代码事实 | 修复验收要求 |
| --- | --- | --- | --- |
| P1 | SQLite flush 失败丢脏值 | `persistentStorage.ts` 在 `webDatabase.batch()` 成功前清空 `pending`，失败路径只记日志，不恢复待写值 | 失败→恢复→重试；并覆盖失败期间同 key 新写入不能被旧值覆盖 |
| P1 | Work Node 内容 CAS 非原子 | `updateWorkNode()` 在 JavaScript 中读后比较 revision，SQL 无条件更新；元数据更新也重写 payload/revision | SQL `WHERE content_revision=?` CAS；并发 rename/payload 和双 payload 测试只允许正确写入成功 |
| P1 | 旧 commit 可应用新内容 | checkout lifecycle 会复用最新未应用 commit；mark applied 不比较 commit payload 与当前 node payload/revision | commit 绑定 content revision 或 payload hash；不匹配必须拒绝并重新提交/批准 |
| P1 | restore base 不持久 | Canvas hydrate `basePayload`，但 `markWorkNodeRollbackApplied()` 只改状态和审计，不改 working payload 或权威 checkout | 恢复后刷新仍为 base；SQLite、session 和可见 Canvas 使用同一身份，写失败必须回滚 UI |
| P1 | 临时工作区替换删除过早 | 新文档 import 后先删除旧临时文档，随后才 apply、flush、activate；后半段失败会失去旧工作区 | 新工作区 durable 并激活后再删旧工作区，所有失败点均能恢复原状态 |
| P1 | Operator 快照并发覆盖 | refresh 在 await 前读取整份 cache，结束后写回基于旧副本构造的整份 nextCache | 不同角色并发刷新不能互相覆盖；同角色使用 generation/CAS 拒绝晚到旧结果 |
| P2 | Buff 名称和拖拽取消漂移 | Buff catalog 的 Fragile/Vulnerability 标签与计算语义相反；Buff `pointercancel/blur` 调用 commit 型 finalize，而 Weapon 使用 cancel | canonical 属性合同；Buff 增加与 Weapon 相同的 pointercancel/blur 不提交浏览器测试 |
| P2 | Buff cache 与 Timeline 双存储可能漂移 | module-global Buff cache 优先于 repository，checkout apply 不失效缓存；Timeline mutation 又分开写 table 与 timeline，repair 只比较 ID | 同 ID 不同 Buff 定义 checkout；错误 staff index、部分失败和字段级一致性测试 |
| P2 | 数据库 Worker 失败后不可恢复 | worker error 只拒绝当前 pending，不 terminate/reset；后续 request 无 timeout | error 后重建或有限时失败，并证明后续 query 不会永久等待 |
| P2 | 主题结果依赖加载历史 | 动态 Apple foundation 含无主题限定的 `.theme-option*` 与 `:root`，加载后不会随切回默认主题卸载；主题 timeout 又不记录晚到成功 | 五主题冷启动与不同切换顺序 computed-style 等价；20 秒晚到成功、离线恢复测试 |
| P2 | Liquid Tide 图片等待不可取消 | `load/error` 两个 once listener 只会自动移除触发的一侧；卸载只置 `cancelled`，不能结束 pending 图片等待 | 成功、失败、永不完成三种图片在卸载后 listener/observer/canvas 全清理 |

#### 实施顺序与测试口径

1. 先修 SQLite flush、Work Node CAS/commit identity/rollback 和临时工作区替换；这些是数据安全问题，不应夹在结构瘦身提交里。
2. 再统一 Buff 属性元数据、修复 Buff 取消拖拽，并处理 Operator snapshot 并发覆盖；先增加会在当前代码上失败的直接合同或浏览器测试。
3. 最后依次删除 OperatorConfig 死层，收敛 Canvas/SkillButton/BuffBatch 的局部事务样板、编辑器 share/download 基础设施和已证明失效的主题规则。
4. 无逻辑瘦身继续使用原 LTS 与 slim 的同套双跑证明行为等价；确认缺陷后的修复允许有意改变错误行为，不要求与有缺陷的原 LTS 保持相同，但必须有独立回归合同。

- 本次归档没有修改任何生产实现或测试断言。归档后仍以当前 LTS 代码和新增测试为最终裁判；本节不是授权一次性大改，也不把尚未运行时复现的竞态写成已经发生过的用户事故。

### 68. 完成五项窄范围代码瘦身并再次双跑

- 边界：本轮只做第 67 节中已有静态证据、且能由现有合同证明等价的五项，不修改伤害、Buff、装备、命令字段、持久化格式或主题视觉规则。实现提交为 `41e24bf9 refactor(slimming): finish audited frontend cleanup`。
- OperatorConfig：删除 `display:none` 的旧装备归档、只服务于该归档的常量和 CSS，以及只构造三份 Map 后输出日志的诊断 effect；当前角色解析、装备选择、快照和四列布局未改。
- Canvas 命令：以局部 `settleCommand` 收敛 27 处“patch 最终状态 → 异步推送结果”回执，保留每个 `command.op` 的显式分支、结果对象、error 文本、提前返回和 fire-and-forget 时序。
- BuffBatch：把四份目标计数、三份按钮命中矩形和三份确认收尾分别收敛为纯函数/局部事务；新增直接合同固定矩形数值和多 Buff 目标计数，既有真实点击、框选和增删确认 E2E 继续通过。
- 编辑器基础设施：Buff、Weapon、Equipment 共用同一个 share modal controller，四个编辑器共用 JSON 下载，前三者共用剪贴板写入与 fallback；各自的 share build/parse/merge/selection 领域模型、文案和确认动作保持独立。既有 3040 细测继续实际覆盖三页复制、下载、非法 JSON、预览、确认导入，以及 Operator 文件导入/导出。
- 主题：删除两个零引用 theme asset export、Liquid Tide 内层重复主题订阅、无 DOM 生产者的旧 SkillButton modal selector，以及由后续同权重声明覆盖的 CSS；主题选择仍由外层 lazy gate 统一控制。五主题双侧合同继续比较四编辑器的根结构、关键节点、computed style、token、交互和 Liquid Tide 标记。
- 日志：清除生产 `src` 中 46 处 `console.log/debug/info` 调试输出，保留 `warn/error` 故障信号；因日志删除而无用的局部变量一并收口，未删除任何业务分支。
- 规模：实现共 27 files changed、315 insertions / 846 deletions，净删 531 行；其中唯一新增测试为 17 行，生产源码净删 548 行。不是把旧页面整体搬到新文件，也没有恢复旧 slim 的过时实现。
- 完整门禁：`npm run check` 全绿；repository check 为 `tracked=536 syntax=14 data=100 images=559 docs=19`，依赖审计 0 vulnerabilities，typecheck、全量 Node、Timeline Bundle V2 smoke、242 modules production build 和 atomic offline shell（`9d064b5fc141c6f0`，37 files）全部通过。3040 完整浏览器回归为 1 passed（19.4 秒）。
- 对称验收：固定原 `v1.8-LTS@920a6f94` 于 `3030`、当前 slim 于 `3040`，没有第三端口；`npm run test:e2e:lts-dual` 为 1 passed（32.4 秒），两侧公共 observation 完整相等，均无 page/console error。
- 独立复核：Sol xhigh 逐项核对当前 diff，未发现 P0/P1/P2。残余测试边界是主题并非逐像素截图、剪贴板 fallback 与文件 picker 没有单独专测、27 类 Canvas 命令没有逐条端到端触发；现有静态逐分支比较、完整门禁和双跑足以验收本次等价瘦身，但不把这些边界虚报为已覆盖。
- 结论：这五项正式完成并 STOP。第 67 节的数据安全与并发风险仍是独立修复任务，不因本轮双跑通过而自动消失。

### 69. 清除整树核验发现的两批退役 CSS

- 范围：只处理整树核验已证明没有任何当前 TSX 生产者的两批 selector，不修改组件、业务状态、伤害计算、主题 token 或可见布局。
- SkillButton：删除旧四联详情遗留的 `skill-button-modal*`、`skill-damage-*`、`skill-info-snapshot-*` 共 26 个 unique class，并从 Apple、Lieflat、Liquid Tide 的混合主题规则中只摘除对应死 selector，保留仍在使用的 Buff、Timeline 和异常面板 selector。
- Damage Sheet：删除 7 组退役 class 的全部主题引用，并移除 Liquid Tide surface registry 中两个永远无法命中的 control selector。源代码和 production build 对全部目标 pattern 的检索结果均为零。
- 防回归：`repository-check.mjs` 新增生产源码门禁；旧 SkillButton 详情 selector 或这 7 组 Damage Sheet selector 再次出现在 `src` 时，`npm run check:repo` 必须失败。临时同时放回两个家族的 selector 后，门禁按预期报告 2 个失败；删除夹具后恢复绿色。
- 规模：6 个 CSS 文件共 19 additions / 538 deletions，CSS 净删 519 行；门禁新增 23 行。production CSS 继续减少 9,520 raw bytes、1,320 gzip bytes。
- 验证：`npm run check` 全绿，包含 0 vulnerabilities、typecheck、全量 Node、Timeline Bundle smoke、242 modules production build 和 37-file atomic shell；3040 详细 E2E 为 `1 passed (18.7s)`。
- 对称验收：人工确认 3030 cwd 为原始 LTS、3040 cwd 为当前 slim 后，固定端口双跑为 `1 passed (31.5s)`；公共 observation 继续完全相等。
- 结论：整树核验中的两批死 CSS 已正式关闭；本轮没有修复或改变任何产品逻辑。

### 70. 增加双跑身份门禁并封存 1.8 Slim 验收基线

- 身份门禁：`run-lts-dual-e2e.mjs` 在 Playwright 启动前读取 3030/3040 监听 PID 的 cwd，再核对真实 Git worktree、branch 和 HEAD。默认要求 3030 为本机 `v1.8-LTS`，3040 为当前 `codex/v1.8-lts-slimming`；复用旧进程与自动启动 3030 使用同一套检查。
- 红灯证明：故意交换两个 URL 后，runner 在 0.2 秒内拒绝继续，准确报告期望 `v1.8-LTS@920a6f94`、实际收到 `codex/v1.8-lts-slimming@fbcc8b40`，没有进入 Playwright。端口接反、服务来自错误工作树、分支不符或 HEAD 不符均不能得到假绿。
- 双侧门禁：原 LTS 与 slim 的 `npm run check` 均通过；原 LTS 为 215 modules / 40 个 atomic shell 文件，slim 为 242 modules / 37 个文件且依赖审计 0 vulnerabilities。两侧额外严格执行 unreachable/unused TypeScript 检查，也全部通过。
- 浏览器门禁：3040 详细 E2E 为 `1 passed (18.7s)`；恢复正确身份后，runner 明确打印 3030 为 `v1.8-LTS@920a6f94`、3040 为当前 slim，同套双跑为 `1 passed (30.9s)`，公共 observation 继续完全相等。
- 范围：本轮只修改双跑基础设施和维护档案，没有修改产品逻辑、测试白名单或允许差异。SQLite、Work Node、Equipment 原子性等质量问题仍留给后续单独讨论，不与 Slim 封版混为一谈。
- 结论：本节提交作为 1.8 Slim 的验收基线；完成提交后再在干净 HEAD 上复跑身份门禁与同套双跑，并用 annotated Git tag 锁定该提交。

### 71. 退役三个重复 Buff 类型入口并修正易伤/脆弱展示

- 产品决定：Buff Sheet、Weapon Skill3 和 Equipment 三件套顶部公式栏不再各自维护 type 搜索/下拉；表格与公式栏只显示当前 `中文名 · typeKey`，统一由双击行打开的 `BuffEffectEditorDrawer` 修改。普通装备词条的类型搜索/下拉属于另一套编辑能力，明确保留。
- 语义纠错：按领域约定固定 `Fragile = 易伤`、`Vulnerability = 脆弱`。原 Buff catalog 中 `fire/electric/ice/nature/magic` 两组中文被对调，`physicalFragile` 还写成“物伤易伤”；本轮统一修为“物理/灼热/电磁/寒冷/自然/法术 + 易伤/脆弱”。
- 真实减重：删除三个旧入口独占的 query state、过滤器、type option/keyword 表、公式栏搜索与下拉 JSX、旧乘算勾选、编辑 apply 分支和孤儿 CSS。生产源码为 103 insertions / 445 deletions，净删 342 行；没有把代码搬到新展示层。
- 保护合同：新增跨 Buff、Weapon、Equipment、共享 Drawer 四个 labeler 的术语合同，逐项覆盖六种伤害属性的 Fragile/Vulnerability；同时更新 Weapon formula、Equipment policy/formula/editing/workbook 合同，固定只读值和 apply no-op。
- 浏览器闭环：3040 细流程实际在三个页面双击打开共享 Drawer，分别选择 `fireFragile` / `fireVulnerability`，关闭后检查表格与公式栏中文正确、旧 search/select 为零；随后展开普通装备，确认其行内 select 与顶部搜索仍各为一个。结果 `1 passed (19.0s)`。
- 对称验收：固定 3030 为 `v1.8-LTS@920a6f94`、3040 为当前 slim，没有第三端口。同一双跑继续精确比较保留能力，并把“原版有三件套旧 type 下拉、slim 无”提升为显式 capability 差异，不再只静默过滤；结果 `1 passed (30.8s)`。
- 门禁结果：本轮较早一次 `npm run check` 曾在同一锁文件上完整通过并报告 0 vulnerabilities；提交前复跑时 npm registry 新增 `brace-expansion` 高危和 `postcss` 中危公告，故聚合命令停在 audit。没有把依赖升级混入本次 UI 瘦身；其余 repository check、typecheck、全量 Node、Timeline Bundle smoke、242 modules production build、atomic offline shell（`e5d869bb56250f32`，37 files）以及新增 slim/dual 浏览器合同均单独复跑通过。依赖公告需另开独立升级提交处理。
- 边界：本轮没有修改 Buff 计算、保存 schema、数值或普通装备映射规则。多个领域 label map 仍未彻底收敛为唯一 canonical registry，因此第 67 节“统一 Buff 属性元数据”的长期项只完成了纠错与交叉合同，不能冒充全部完成。

### 72. 关闭依赖审计并完成 Buff 类型元数据单一来源

- 依赖修复：独立提交 `48566bce fix(deps): patch audited transitive vulnerabilities` 只更新传递依赖 override 与锁文件：`brace-expansion@5.0.9`、`fast-uri@3.1.5`、`postcss@8.5.25`、`undici@7.29.0`。没有降级插件、改业务代码或降低 audit 门槛；`npm audit --audit-level=high` 恢复为 0 vulnerabilities。
- 红灯证据：先扩展 `buffTypeTerminology.test.ts`，直接引用尚不存在的 canonical 模块并覆盖 Equipment 全部类型、五项历史显示差异、未知 key、空 key 和只读映射；测试按预期因模块不存在失败，随后才加入实现。
- 单一来源：新增 `core/domain/buffTypeMetadata.ts`，集中维护 typeKey→中文名及 plain/display 两个纯函数；删除 Buff、Weapon、Equipment、Operator Drawer、Operator Page 和 Operator panel formatter 六处重复 label map。生产源码合计净删 306 行，不把映射搬成另一组页面副本。
- 保留差异：Equipment 的“普通攻击伤害加成 / 对失衡目标伤害加成 / 治疗效率加成 / 生命值 / 全伤害减免”作为 5 个显式 override 保留。各编辑器原有 type 选项、顺序、筛选、空值文案和普通装备编辑入口均未合并或改写。
- 计算边界：Operator panel 的文本格式化改用同一 label 入口；Buff 数值计算、保存 schema、倍率、普通装备 preset 与伤害结果没有变化。源码检索确认生产代码不再存在第二份 `BUFF_TYPE_LABELS`、`WEAPON_BUFF_TYPE_LABELS` 或 `OPERATOR_BUFF_TYPE_LABELS`。
- 体积：相对依赖修复后的同树构建，Buff / Weapon / Equipment / Operator 四个 lazy chunk 合计减少约 8.48 kB raw、2.48 kB gzip；最终 production build 为 243 modules。
- 完整门禁：`npm run check` 全绿，包含 repository check（tracked=539）、0 vulnerabilities、typecheck、全量 Node、Timeline Bundle smoke、production build 和 atomic offline shell（`f541daf4c4105278`，37 files）。3040 细流程为 `1 passed (19.8s)`。
- 对称验收：固定 3030 为原 `v1.8-LTS@920a6f94`、3040 为当前 slim，没有第三端口；同一份双跑合同为 `1 passed (32.8s)`，两侧公共 observation 完整相等。第 67 节的“统一 Buff 属性元数据”至此关闭；Operator snapshot 并发覆盖仍是独立缺陷任务。

### 73. Buff 资源树取消拖拽按产品决定不修

- 说明：这里的“Buff 拖拽”仅指 Buff 编辑页左侧资源树的长按排序，不是 Timeline 技能按钮拖拽，也不是常用主流程。
- 决定：该能力使用频率很低，`pointercancel/window blur` 复用提交函数的边角不再安排修复；当前实现、正常 pointerup 重排和既有测试全部保持原样。
- 留痕：此项标记为 `WONTFIX` 并从活跃缺陷待办移除。以后不得仅依据第 67 节的历史源码审计重新立项；除非出现真实用户问题或产品重新启用该交互。

### 74. 修复 SQLite 转换假失败并整理可复用回归包

- 真实复现：共享“五色队-热启动循环”已经成功写入 SQLite，但旧页面继续用转换前的运行时技能目录还原，错误拒绝洛茜 `bzb6ptf17` 的 A 技能；同一个 catch 又把后置加载错误误报成“存档转换 SQLite 失败”，并跳过 SQLite 列表刷新。手工刷新后数据、技能和当前工作区均正常，证明不是数据丢失。
- 产品修复：提交 `57cc363b fix: reload after SQLite archive conversion` 让转换成功后沿用“应用 SQLite 工作区”的可靠重载路径，由 AppContext 根据新选中干员重建可信技能目录；真正的转换失败与“已创建但自动应用失败”分开报告。独立合同固定写入、激活、重载顺序及两类错误边界。
- 复用整理：新增稳定入口 `npm run test:regression`（typecheck + 全量 Node + 单分支浏览器）和 `npm run test:regression:dual`（基线/候选同套双跑），并把 URL、启动命令、worktree、分支、报告名称、访问密码、安装数据量、版本标签和两组 legacy capability 预期全部参数化。默认值继续对应当前 3030/3040 与 1.8，不修改现有测试行为。
- 运行规则：历史结果继续保留在 1.8 基线文档；以后换 LTS 或瘦身分支以 `docs/testing/reusable-regression-suite.md` 为唯一入口，扩展同一份合同，不复制 runner/spec。缺陷修复新增独立失败合同，无逻辑瘦身继续严格深比较公共 observation，允许差异只能显式声明为 capability。
- 验收：参数化单分支入口包含全部 Node 合同并以系统 Chrome 完成 `1 passed`；参数化双跑核对 `v1.8-LTS@920a6f94` 与当前 slim 的真实进程身份后完成 `1 passed`，公共行为继续完全相等。repository check、typecheck 和 diff check 同轮通过；未把 SQLite 文件往返、双标签接管或真实断网冒充已覆盖。

### 75. 建立测试专用满乘区资料并完成 SQLite 金值双跑

- 测试资料：新增完全合成的测试干员、测试武器、四件测试装备和测试三件套；干员含 A/B/E/Q/Dot 与额外双段 B 技能。数据不读取、不复制用户存档。
- 定向设计：A 验证 Hit key，B 消费武器电磁条件 Buff，E 消费三件套自然条件 Buff，Q 验证寒冷元素，Dot 验证技能类型；每个按钮同时带一个明确不应命中的 Buff。综合技能覆盖攻击、倍率、暴击、伤害、防御、抗性、增幅、易伤、脆弱、连击和失衡。
- Buff 形态：条件、默认生效、2/3 层可叠层、数值派生和直接乘系数全部有固定样本；纯合同逐层硬编码中间值和最终值，不从被测实现动态生成 golden。
- 持久化链：同一 Local Data 数据包转为 Timeline archive，再通过真实浏览器 SQLite 保存、激活和整页刷新恢复；结构化伤害报告同时对硬金值和 3030/3040 公共 observation 验收。
- 验收：两份专项纯合同和完整 `npm run check` 均通过，后者包含 554 tracked、0 vulnerabilities、typecheck、全量 Node、244 modules production build 与 atomic shell `7049e002817b1071` / 37 files；固定 `v1.8-LTS@920a6f94` 与当前 slim 的双跑为 `1 passed (38.6s)`。

## 当前待办

- [x] 建立独立 LTS slimming 工作树和分支。
- [x] 固定共同祖先、两端 HEAD 和提交数量。
- [x] 归档旧 slim 的 16 个补丁、最终参考树和 Git bundle。
- [x] 建立 18 个重叠文件与关键 LTS 补丁地图。
- [x] 执行未修改 LTS 的确定性命令基线。
- [x] 补第一批自动行为测试与浏览器测试入口。
- [x] 重新验证并删除不可达旧 Buff 编辑器。
- [x] 抽取 Buff 与 Weapon 的稳定目录常量。
- [x] 固定并抽取 Buff 纯模型，不改变 LTS 平台 adapter。
- [x] 固定并抽取 Buff 公式文本绑定。
- [x] 固定并迁出 Buff workbook projection，移除普通前端投影对 ExcelJS 的依赖（ExcelJS 随后整体退役）。
- [x] 抽取并验证 SQLite-backed Buff undo repository。
- [x] 固定并抽取 Buff 分享 parse/merge 数据边界。
- [x] 固定并抽取 Buff explorer drag policy。
- [x] 在真实手势合同下抽取 Buff pointer lifecycle hook。
- [x] 收口 Buff draft/library persistence adapter。
- [x] 统一 Buff 公式输入 apply 路径并正式停止 Buff 第一阶段拆分。
- [x] 固定当前 LTS 的 Weapon 纯模型合同。
- [x] 从当前 LTS 迁出 Weapon 纯模型与 Sheet row projection。
- [x] 固定当前 LTS 的 Weapon workbook projection 合同。
- [x] 迁出 Weapon workbook projection。
- [x] 收口 Weapon draft/library persistence adapter。
- [x] 固定 Weapon 分享导出/导入/刷新浏览器往返。
- [x] 抽取 Weapon 分享 parse/build/merge/selection 数据边界。
- [x] 抽取 Weapon 公式 binding 与不可变 apply。
- [x] 修复 skill3 主 value 名称单元格崩溃。
- [x] 固定 Weapon explorer 的取消、长按、重排和刷新浏览器合同。
- [x] 修复选中 Weapon effect 重排后 draft/library 持久化不同步。
- [x] 禁止 Weapon skill3 主 value 行进入 effect 拖拽。
- [x] 抽取 Weapon explorer 纯 drag policy 并删除不可达重排分支。
- [x] 修复 Weapon 拖拽覆盖当前未保存编辑。
- [x] 在真实浏览器手势合同下迁出 Weapon pointer lifecycle hook。
- [x] 修复 Weapon pointercancel/blur 错误提交重排。
- [x] 收口 Weapon 纯编辑事务并正式停止 Weapon 第一阶段拆分。
- [x] 完成 Buff 与 Weapon 的 LTS-native 拆分。
- [x] 迁出 Equipment schema/normalize/value catalog model。
- [x] 迁出 Equipment row/workbook projection。
- [x] 收口 Equipment editing 与 formula 纯事务。
- [x] 收口 Equipment SQLite persistence 与 share 数据边界。
- [x] 固定并修复 Equipment 用户图片 canonical 持久引用。
- [x] 修复 Equipment 百分比二次迁移、保存并发覆盖与图片 hydration 三项 P1。
- [x] 完成 Equipment 的 LTS-native 拆分。
- [x] 固定当前 LTS 的 OperatorDraft 纯模型合同。
- [x] 迁出 OperatorDraft LTS-native 纯模型。
- [x] 收口 OperatorDraft durable SQLite persistence 边界。
- [x] 收口 OperatorDraft share 身份与 ordered skill 顺序边界。
- [x] 完成 OperatorDraft 的 LTS-native 拆分。
- [x] 完成 BuffBatch 和 SkillButton 的 LTS-native 拆分。
- [x] 统一 OperatorConfig 的 Equipment 投影并修复 fixedStat 漂移。
- [x] 删除 CanvasBoard 不可达 ReportTab，并收口失效的计算侧栏入口。
- [x] 按最终产品决定退役 Damage Sheet、XLSX 与 ExcelJS，保留 PPT 报表和共享伤害计算链。
- [x] 对 Damage Sheet、ExcelJS、CanvasBoard 和 OperatorConfig 分别作最终处理。
- [x] 在固定 `3030/3040` 上完成原 LTS 与 1.8 slim 的同套黑盒双跑，并把预期差异收窄为退役能力。
- [x] 在主题合同和同套双跑保护下，完成 Buff、Weapon、Equipment、Operator 的第二阶段重复实现减重。
- [x] 二次复跑完整门禁和固定 `3030/3040` 双侧合同，归档后续瘦身面与代码质量风险。
- [ ] 先补失败/并发合同并修复 SQLite、Work Node、rollback 和临时工作区的数据安全问题。
- [x] 退役 Buff、Weapon Skill3 与 Equipment 三件套的重复 type 搜索/下拉，保留普通装备编辑入口，并固定 Fragile/Vulnerability 中文语义。
- [x] 统一 Buff 类型中文标签与显示函数，保留各编辑器的选项、筛选、空值文案和显式展示差异。
- [x] Buff 资源树取消拖拽边角按产品决定不修，并从活跃缺陷中移除。
- [ ] 修复 Operator snapshot 并发覆盖。
- [x] 在对应合同和双跑保护下完成 OperatorConfig 死层、Canvas 命令回执、BuffBatch 局部纯逻辑、编辑器 share/download 与主题死代码五项窄范围瘦身。
- [x] 清除整树核验发现的旧 SkillButton 详情和 Damage Sheet 主题 selector，并加入仓库级防回归门禁。
- [x] 为固定 3030/3040 双跑增加 worktree、branch、HEAD 身份门禁，并完成 1.8 Slim 封版验收。
- [x] 将全量 Node、单分支浏览器与双分支黑盒测试整理为可参数化、可跨版本复用的回归包。
- [x] 建立测试专用干员/武器/装备/三件套的满乘区资料，并以独立金值完成 Local Data → SQLite → 刷新恢复的 3030/3040 双跑。
- [ ] 重新评估本轮刻意未动的 SkillButton panelConfig 写入、Canvas Operator 配置事务、图片选择展示层和 Liquid snapshot 原语；只有证明能真实减重且测试充分时再做。
