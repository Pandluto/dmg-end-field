# 开发修改 0.3.9 - 伤害加成 Buff 模糊搜索

## 约束 Prompt

- **强制约束**：所有代码必须添加中文注释，解释逻辑和用途
- **强制约束**：不得修改 SidePanel 以外的其他组件，除非明确提及
- **强制约束**：模糊搜索实现必须复用项目已有的 `weaponFuzzySearch.ts` 工具函数
- **强制约束**：搜索目标字段为 `source`（Buff 来源：角色名或武器名）
- **强制约束**：下滑抽屉样式必须参照 CTI 上滑抽屉（`config-cti-drawer`）实现
- **禁止**：不得删除或破坏现有功能（标签切换、Buff 陈列、刷新按钮等）
- **禁止**：不得修改 Buff 数据接口定义，除非明确需要添加搜索相关字段

---

## 目标

在"伤害加成"标签页的文本输入框中实现模糊搜索功能，根据输入内容在下滑抽屉中展示匹配的 Buff（按 `source` 字段匹配），点击后什么也不发生，不对陈列区进行筛选。

---

## 参考实现

### 1. CTI 模糊搜索（OperatorConfigPanel.tsx 第 1365-1397 行）

特点：
- 使用 `textarea` 作为输入框
- 输入时实时触发搜索
- 使用 `useMemo` 缓存搜索结果
- **上滑抽屉**展示匹配结果（`bottom: calc(100% + 6px)`）
- 使用 `weaponFuzzySearch.ts` 中的 `searchWeapons` 函数

### 2. CTI 抽屉样式（CanvasBoard.css 第 598-632 行）

```css
.config-cti-drawer {
  position: absolute;
  left: 0;
  right: 0;
  bottom: calc(100% + 6px);  /* 下滑：在输入框下方 */
  border: 2px solid #000000;
  background: #F0F0F0;
  overflow-y: auto;
  max-height: 0;
  opacity: 0;
  pointer-events: none;
  transition: max-height 0.18s ease, opacity 0.18s ease;
  z-index: 95;
  scrollbar-width: none;
  -ms-overflow-style: none;
}

.config-cti-drawer.is-open {
  max-height: 180px;
  opacity: 1;
  pointer-events: auto;
}
```

---

## 实现方案

### 方案：输入框 + 下滑抽屉展示（仅展示，不筛选）

不同于 CTI 的选择功能，伤害加成的搜索仅用于**展示匹配的 Buff**：
- 在文本输入框输入关键词
- 实时搜索 Buff 的 `source` 字段（角色名/武器名）
- **下滑抽屉**展示匹配的 Buff 的 `displayName` 列表
- 点击抽屉中的选项后，仅关闭抽屉并清空输入框
- **不对陈列区进行任何筛选**，始终显示全部 Buff

---

## 数据结构

### BuffItem 接口（已有）
```typescript
interface BuffItem {
  displayName: string;  // Buff 显示名称 ← 抽屉中显示此字段
  name: string;         // Buff 名称
  level: string;        // Buff 等级
  value?: number;       // Buff 数值
  type?: string;        // Buff 类型
  source: string;       // Buff 来源（角色名或武器名）← 搜索目标字段
  sourceName: string;   // Buff 来源名称
  description: string;  // Buff 描述
}
```

### 搜索索引结构
复用 `WeaponSearchEntry` 接口：
```typescript
interface BuffSearchEntry {
  name: string;         // source 值（角色名或武器名）
  normalizedName: string;  // 规范化名称（小写，去除特殊字符）
  fullPinyin: string;   // 全拼
  initials: string;     // 首字母缩写
}
```

---

## 组件修改

### 修改文件：`src/components/SidePanel/components/DamageTab.tsx`

#### 1. 导入模糊搜索工具函数和 useMemo
```typescript
import { useState, useCallback, useMemo, useEffect, useRef } from 'react';
import { buildWeaponSearchIndex, searchWeapons } from '../../../utils/weaponFuzzySearch';
```

#### 2. 添加搜索相关状态和引用
```typescript
// 搜索关键词
const [searchKeyword, setSearchKeyword] = useState('');
// 抽屉是否打开
const [isDrawerOpen, setIsDrawerOpen] = useState(false);
// 抽屉容器引用（用于点击外部关闭）
const drawerHostRef = useRef<HTMLDivElement>(null);
```

#### 3. 构建搜索索引
使用 `useMemo` 缓存搜索索引：
```typescript
const buffSearchIndex = useMemo(() => {
  // 从 buffList 中提取所有唯一的 source 值
  const sources = Array.from(new Set(buffList.map(buff => buff.source)));
  return buildWeaponSearchIndex(sources);
}, [buffList]);
```

#### 4. 实现搜索匹配逻辑
使用 `useMemo` 实时计算匹配的 source 列表：
```typescript
const matchedSources = useMemo(() => {
  if (!searchKeyword.trim()) {
    return [];  // 无关键词时返回空数组
  }
  
  // 使用 searchWeapons 搜索匹配的 source
  return searchWeapons(searchKeyword, buffSearchIndex);
}, [searchKeyword, buffSearchIndex]);
```

#### 5. 获取匹配到的 Buff 列表（用于抽屉展示）
根据匹配的 source 获取对应的 Buff 列表：
```typescript
const matchedBuffs = useMemo(() => {
  if (matchedSources.length === 0) {
    return [];
  }
  // 获取所有 source 匹配的 Buff
  return buffList.filter(buff => matchedSources.includes(buff.source));
}, [matchedSources, buffList]);
```

#### 6. 实现点击外部关闭抽屉
使用 `useEffect` 监听点击事件：
```typescript
useEffect(() => {
  const handleClickOutside = (event: MouseEvent) => {
    if (drawerHostRef.current && !drawerHostRef.current.contains(event.target as Node)) {
      setIsDrawerOpen(false);
    }
  };
  
  document.addEventListener('mousedown', handleClickOutside);
  return () => document.removeEventListener('mousedown', handleClickOutside);
}, []);
```

#### 7. 修改输入框和添加抽屉
```typescript
{/* 文本输入区 - 抽屉宿主 */}
<div className="damage-input-section drawer-host" ref={drawerHostRef}>
  <input
    type="text"
    className="damage-input"
    value={inputValue}
    onChange={(e) => {
      const value = e.target.value;
      setInputValue(value);
      setSearchKeyword(value);
      setIsDrawerOpen(value.trim().length > 0);
    }}
    onFocus={() => {
      if (searchKeyword.trim().length > 0) {
        setIsDrawerOpen(true);
      }
    }}
    placeholder="输入 Buff 来源搜索"
  />
  
  {/* 下滑抽屉 - 展示匹配的 Buff displayName 列表 */}
  <div className={`damage-search-drawer${isDrawerOpen ? ' is-open' : ''}`}>
    {matchedBuffs.length > 0 ? (
      matchedBuffs.map((buff, index) => (
        <button
          key={index}
          className="damage-search-option"
          onClick={() => {
            // 点击后仅关闭抽屉并清空输入框，不改变陈列区
            setIsDrawerOpen(false);
            setInputValue('');
            setSearchKeyword('');
          }}
        >
          {buff.displayName}
        </button>
      ))
    ) : (
      <div className="damage-search-empty">未匹配到 Buff</div>
    )}
  </div>
</div>
```

#### 8. Buff 列表渲染（保持不变，始终显示全部）
```typescript
{/* Buff 列表 - 始终显示全部，不受搜索影响 */}
<div className="buff-list">
  {buffList.length === 0 ? (
    <div className="buff-empty">点击刷新加载 Buff 数据</div>
  ) : (
    buffList.map((buff, index) => (
      <div key={index} className="buff-item" title={buff.displayName}>
        {buff.displayName}
      </div>
    ))
  )}
</div>
```

---

## 样式追加

### 修改文件：`src/components/SidePanel/SidePanel.css`

#### 1. 抽屉宿主容器
```css
/* 抽屉宿主 - 相对定位，用于抽屉绝对定位参考 */
.drawer-host {
  position: relative;
  overflow: visible;
  z-index: 20;
}
```

#### 2. 下滑抽屉（参照 CTI 上滑抽屉，方向改为下滑）
```css
/* 搜索下滑抽屉 - 参照 config-cti-drawer，方向改为下滑 */
.damage-search-drawer {
  position: absolute;
  left: 0;
  right: 0;
  top: calc(100% + 6px);  /* 下滑：在输入框下方 */
  border: 2px solid #000000;
  background: #F0F0F0;
  overflow-y: auto;
  max-height: 0;
  opacity: 0;
  pointer-events: none;
  transition: max-height 0.18s ease, opacity 0.18s ease;
  z-index: 100;
  scrollbar-width: none;
  -ms-overflow-style: none;
}

/* 抽屉打开状态 */
.damage-search-drawer.is-open {
  max-height: 180px;
  opacity: 1;
  pointer-events: auto;
}

/* 隐藏滚动条 */
.damage-search-drawer::-webkit-scrollbar {
  display: none;
}
```

#### 3. 抽屉选项样式（参照 config-weapon-option）
```css
/* 搜索选项 - 参照 config-weapon-option */
.damage-search-option {
  display: block;
  width: 100%;
  padding: 8px 12px;
  text-align: left;
  font-size: 12px;
  color: #000;
  background: transparent;
  border: none;
  border-bottom: 1px solid #000000;
  cursor: pointer;
  transition: background-color 0.2s ease;
}

/* 选项悬停效果 */
.damage-search-option:hover {
  background: rgba(255, 255, 0, 0.3);
}

/* 最后一个选项无边框 */
.damage-search-option:last-child {
  border-bottom: none;
}
```

#### 4. 空状态提示
```css
/* 搜索空状态 */
.damage-search-empty {
  padding: 12px;
  text-align: center;
  font-size: 12px;
  color: #999;
  font-style: italic;
}
```

---

## 验证要点

1. 输入框输入关键词后，下滑抽屉实时展示匹配的 Buff 的 `displayName` 列表
2. 搜索按 `source` 字段匹配，支持中文、拼音、首字母缩写（复用 searchWeapons 能力）
3. 点击抽屉选项后，仅关闭抽屉并清空输入框，陈列区保持不变
4. 点击抽屉外部，抽屉关闭
5. 清空输入框后，抽屉关闭
6. 搜索无结果时，抽屉显示"未匹配到 Buff"
7. 刷新 Buff 数据后，搜索索引自动更新
8. 陈列区始终显示全部 Buff，不受搜索影响
9. 不影响现有功能（刷新按钮、悬停效果等）

---

## 待填充内容（本文档不涉及）

- Buff 项的点击交互
- 搜索历史记录
- 高亮匹配文本
