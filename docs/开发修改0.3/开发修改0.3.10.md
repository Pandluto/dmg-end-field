# 开发修改 0.3.10 - 谱线持久化与节点编号系统

## 约束 Prompt

- **强制约束**：所有代码必须添加中文注释，解释逻辑和用途
- **强制约束**：不得修改现有 Buff 搜索和陈列功能
- **强制约束**：节点编号必须按干员独立分配，每组 50 个节点
- **强制约束**：排轴数据仅在点击保存按钮时存储到 sessionStorage
- **强制约束**：弹窗必须显示在界面正中间
- **禁止**：不得删除或破坏现有功能（标签切换、Buff 陈列、刷新按钮等）
- **禁止**：不得修改 SkillButton 基础样式，仅修改交互逻辑

---

## 目标

实现谱线持久化系统，为每个干员的谱线分配独立编号，建立排轴数据结构，点击保存按钮时存储到 sessionStorage，双击技能按钮弹出居中弹窗显示详细信息。

---

## 节点编号规则

### 编号分配原则

每个干员的谱线编号独立，按组分配：

```
第一组（索引 0）：
  管理员：节点 1 ~ 50
  干员2：  节点 1 ~ 50
  干员3：  节点 1 ~ 50
  干员4：  节点 1 ~ 50

第二组（索引 1）：
  管理员：节点 51 ~ 100
  干员2：  节点 51 ~ 100
  干员3：  节点 51 ~ 100
  干员4：  节点 51 ~ 100

第三组（索引 2）：
  管理员：节点 101 ~ 150
  ...以此类推
```

### 节点编号计算

```typescript
// 组索引（从 0 开始）
const groupIndex = Math.floor(nodeIndex / 50);

// 组内节点编号（1 ~ 50）
const nodeNumberInGroup = (nodeIndex % 50) + 1;

// 全局节点编号
const globalNodeNumber = groupIndex * 50 + nodeNumberInGroup;
```

---

## 数据结构

### 1. 技能按钮数据接口

```typescript
/**
 * 技能按钮数据
 * 存储在谱线上的技能按钮信息
 */
interface SkillButtonData {
  id: string;              // 按钮唯一 ID
  characterName: string;   // 干员名称（如：陈千语）
  skillType: 'A' | 'B' | 'E' | 'Q';  // 技能类型
  staffIndex: number;      // 干员索引（0=管理员, 1=干员2, 2=干员3, 3=干员4）
  nodeIndex: number;       // 节点索引（0 ~ N）
  nodeNumber: number;      // 节点编号（1 ~ 50, 51 ~ 100, ...）
  position: { x: number; y: number };  // 位置坐标
}
```

### 2. 排轴数据结构

```typescript
/**
 * 单个干员的谱线数据
 */
interface StaffLineData {
  staffIndex: number;              // 干员索引
  characterName: string;           // 干员名称
  occupiedNodes: number[];         // 已被占用的节点索引列表（自动排序）
  buttons: SkillButtonData[];      // 该干员谱线上的所有按钮（按 nodeIndex 排序）
}

/**
 * 排轴总数据结构
 * 包含 4 个干员的谱线数据
 */
interface TimelineData {
  version: string;                 // 数据版本号
  createdAt: number;               // 创建时间
  updatedAt: number;               // 最后更新时间
  staffLines: StaffLineData[];     // 4 个干员的谱线数据
}
```

### 3. 数据示例

```typescript
const exampleData: TimelineData = {
  version: "1.0.0",
  createdAt: 1234567890,
  updatedAt: 1234567890,
  staffLines: [
    {
      staffIndex: 0,
      characterName: "管理员",
      occupiedNodes: [2, 5, 10],  // 节点索引
      buttons: [
        {
          id: "btn-001",
          characterName: "陈千语",
          skillType: "A",
          staffIndex: 1,      // 干员2的谱线
          nodeIndex: 2,       // 第 3 个节点（索引从 0 开始）
          nodeNumber: 3,      // 节点编号 3
          position: { x: 150, y: 200 }
        },
        // ...更多按钮
      ]
    },
    // ...其他干员
  ]
};
```

---

## 组件修改

### 1. 类型定义扩展

**文件**：`src/types/index.ts`

添加以下接口：

```typescript
// 技能按钮数据
export interface SkillButtonData {
  id: string;
  characterName: string;
  skillType: 'A' | 'B' | 'E' | 'Q';
  staffIndex: number;
  nodeIndex: number;
  nodeNumber: number;
  position: { x: number; y: number };
}

// 单个干员的谱线数据
export interface StaffLineData {
  staffIndex: number;
  characterName: string;
  occupiedNodes: number[];
  buttons: SkillButtonData[];
}

// 排轴总数据结构
export interface TimelineData {
  version: string;
  createdAt: number;
  updatedAt: number;
  staffLines: StaffLineData[];
}
```

---

### 2. 节点编号工具函数

**文件**：`src/utils/nodeNumbering.ts`（新建）

```typescript
/**
 * 计算节点编号
 * @param nodeIndex - 节点索引（从 0 开始）
 * @returns 节点编号（1 ~ 50, 51 ~ 100, ...）
 */
export function calculateNodeNumber(nodeIndex: number): number {
  const groupIndex = Math.floor(nodeIndex / 50);
  const nodeNumberInGroup = (nodeIndex % 50) + 1;
  return groupIndex * 50 + nodeNumberInGroup;
}

/**
 * 计算组索引
 * @param nodeIndex - 节点索引
 * @returns 组索引（从 0 开始）
 */
export function calculateGroupIndex(nodeIndex: number): number {
  return Math.floor(nodeIndex / 50);
}

/**
 * 计算组内节点编号
 * @param nodeIndex - 节点索引
 * @returns 组内编号（1 ~ 50）
 */
export function calculateNodeNumberInGroup(nodeIndex: number): number {
  return (nodeIndex % 50) + 1;
}
```

---

### 3. 排轴数据管理 Hook

**文件**：`src/hooks/useTimelineData.ts`（新建）

```typescript
import { useState, useCallback, useRef } from 'react';
import { SkillButtonData, StaffLineData, TimelineData } from '../types';
import { calculateNodeNumber } from '../utils/nodeNumbering';

const STORAGE_KEY = 'ddd.timeline.data.v1';

/**
 * 排轴数据管理 Hook
 * 管理技能按钮的增删改查，点击保存时存储到 sessionStorage
 */
export function useTimelineData(selectedCharacters: { name: string }[]) {
  // 排轴数据状态（内存中，不自动持久化）
  const [timelineData, setTimelineData] = useState<TimelineData>(() => {
    return createEmptyTimelineData(selectedCharacters);
  });

  // 使用 ref 存储最新数据，供保存时使用
  const timelineDataRef = useRef(timelineData);
  timelineDataRef.current = timelineData;

  /**
   * 创建空的排轴数据结构
   */
  const createEmptyTimelineData = (characters: { name: string }[]): TimelineData => {
    const now = Date.now();
    return {
      version: "1.0.0",
      createdAt: now,
      updatedAt: now,
      staffLines: characters.map((char, index) => ({
        staffIndex: index,
        characterName: char.name,
        occupiedNodes: [],
        buttons: [],
      })),
    };
  };

  /**
   * 添加技能按钮
   */
  const addSkillButton = useCallback((buttonData: Omit<SkillButtonData, 'id' | 'nodeNumber'>) => {
    const newButton: SkillButtonData = {
      ...buttonData,
      id: `btn-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`,
      nodeNumber: calculateNodeNumber(buttonData.nodeIndex),
    };

    setTimelineData(prev => {
      const newData: TimelineData = {
        ...prev,
        updatedAt: Date.now(),
        staffLines: [...prev.staffLines],
      };
      
      const staffLine = { ...newData.staffLines[buttonData.staffIndex] };
      newData.staffLines[buttonData.staffIndex] = staffLine;
      
      // 添加按钮并排序
      staffLine.buttons = [...staffLine.buttons, newButton]
        .sort((a, b) => a.nodeIndex - b.nodeIndex);
      
      // 更新占用节点列表并排序
      staffLine.occupiedNodes = [...staffLine.occupiedNodes, buttonData.nodeIndex]
        .sort((a, b) => a - b);
      
      return newData;
    });

    return newButton;
  }, []);

  /**
   * 删除技能按钮
   */
  const removeSkillButton = useCallback((staffIndex: number, buttonId: string) => {
    setTimelineData(prev => {
      const newData: TimelineData = {
        ...prev,
        updatedAt: Date.now(),
        staffLines: [...prev.staffLines],
      };
      
      const staffLine = { ...newData.staffLines[staffIndex] };
      newData.staffLines[staffIndex] = staffLine;
      
      const button = staffLine.buttons.find(b => b.id === buttonId);
      
      if (button) {
        // 移除按钮
        staffLine.buttons = staffLine.buttons.filter(b => b.id !== buttonId);
        // 更新占用节点
        staffLine.occupiedNodes = staffLine.occupiedNodes
          .filter(n => n !== button.nodeIndex)
          .sort((a, b) => a - b);
      }
      
      return newData;
    });
  }, []);

  /**
   * 获取指定干员谱线上的所有按钮
   */
  const getStaffButtons = useCallback((staffIndex: number): SkillButtonData[] => {
    return timelineData.staffLines[staffIndex]?.buttons || [];
  }, [timelineData]);

  /**
   * 保存排轴数据到 sessionStorage
   * 点击保存按钮时调用
   */
  const saveTimelineData = useCallback(() => {
    const dataToSave = timelineDataRef.current;
    sessionStorage.setItem(STORAGE_KEY, JSON.stringify(dataToSave));
    console.log('【排轴数据】已保存到 sessionStorage:', dataToSave);
    return dataToSave;
  }, []);

  /**
   * 从 sessionStorage 加载排轴数据
   */
  const loadTimelineData = useCallback(() => {
    const saved = sessionStorage.getItem(STORAGE_KEY);
    if (saved) {
      const parsed: TimelineData = JSON.parse(saved);
      setTimelineData(parsed);
      console.log('【排轴数据】已从 sessionStorage 加载:', parsed);
      return parsed;
    }
    return null;
  }, []);

  return {
    timelineData,
    addSkillButton,
    removeSkillButton,
    getStaffButtons,
    saveTimelineData,
    loadTimelineData,
  };
}
```

---

### 4. 修改 SkillButton.tsx - 双击弹窗显示在正中间

**文件**：`src/components/CanvasBoard/SkillButton.tsx`

修改小抽屉为居中弹窗，并接收完整的按钮数据：

```typescript
interface SkillButtonProps {
  button: SkillButtonType & { nodeNumber?: number };  // 扩展 nodeNumber
  size: number;
  onMouseDown: (e: React.MouseEvent) => void;
  onContextMenu: (e: React.MouseEvent) => void;
  // 可选：传入完整的排轴数据用于弹窗显示
  timelineData?: import('../../types').TimelineData;
}

// 在组件内部
export function SkillButtonComponent({ 
  button, 
  size, 
  onMouseDown, 
  onContextMenu,
  timelineData 
}: SkillButtonProps) {
  
  // ...原有代码

  // 双击处理 - 打开居中弹窗
  const handleClick = useCallback(() => {
    if (isLongPressRef.current) return;
    
    clickCountRef.current += 1;
    
    if (clickCountRef.current === 1) {
      clickTimerRef.current = setTimeout(() => {
        clickCountRef.current = 0;
      }, 250);
    } else if (clickCountRef.current === 2) {
      if (clickTimerRef.current) {
        clearTimeout(clickTimerRef.current);
        clickTimerRef.current = null;
      }
      clickCountRef.current = 0;
      
      // 打开居中弹窗
      setIsModalOpen(true);
      console.log('双击技能按钮，打开弹窗:', button.id);
      
      // 输出总数据结构到控制台
      if (timelineData) {
        console.log('【排轴数据】当前总数据结构:', timelineData);
      }
    }
  }, [button.id, timelineData]);

  // ...弹窗渲染代码（见样式部分）
}
```

---

### 5. 添加居中弹窗样式

**文件**：`src/components/CanvasBoard/SkillButton.css`

添加居中弹窗样式（覆盖原有小抽屉样式）：

```css
/* ==================== 居中弹窗样式 ==================== */

/* 弹窗遮罩层 */
.skill-button-modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  animation: modalFadeIn 0.2s ease;
}

/* 弹窗容器 */
.skill-button-modal {
  background: #F0F0F0;
  border: 2px solid #000000;
  border-radius: 8px;
  padding: 24px 32px;
  min-width: 300px;
  max-width: 400px;
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
  animation: modalSlideIn 0.2s ease;
}

/* 弹窗标题 */
.skill-button-modal h4 {
  margin: 0 0 16px 0;
  font-size: 18px;
  font-weight: 700;
  color: #000;
  text-align: center;
  border-bottom: 2px solid #000;
  padding-bottom: 12px;
}

/* 弹窗内容 */
.skill-button-modal .modal-content {
  margin-bottom: 20px;
}

.skill-button-modal .modal-content p {
  margin: 10px 0;
  font-size: 14px;
  color: #333;
  line-height: 1.5;
}

.skill-button-modal .modal-content strong {
  color: #000;
  display: inline-block;
  width: 100px;
}

/* 关闭按钮 */
.skill-button-modal .modal-close-btn {
  display: block;
  width: 100%;
  padding: 10px 20px;
  font-size: 14px;
  font-weight: 700;
  color: #000;
  background: transparent;
  border: 2px solid #000;
  cursor: pointer;
  transition: background-color 0.2s ease;
}

.skill-button-modal .modal-close-btn:hover {
  background: rgba(255, 255, 0, 0.3);
}

/* 弹窗动画 */
@keyframes modalFadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes modalSlideIn {
  from {
    opacity: 0;
    transform: translateY(-20px) scale(0.95);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}
```

---

### 6. 修改 CanvasBoard 集成排轴数据

**文件**：`src/components/CanvasBoard/index.tsx`

```typescript
import { useTimelineData } from '../../hooks/useTimelineData';

// 在 CanvasBoard 组件中
const CanvasBoard: React.FC = () => {
  const { state } = useAppContext();
  const { selectedCharacters } = state;
  
  // 使用排轴数据管理 Hook
  const { 
    timelineData, 
    addSkillButton, 
    getStaffButtons,
    saveTimelineData 
  } = useTimelineData(selectedCharacters);
  
  // 在 handleMouseUp 中保存按钮数据到内存
  const handleMouseUp = useCallback((e: MouseEvent) => {
    // ...原有逻辑
    
    if (nearestLine) {
      // ...原有逻辑
      
      // 创建按钮时同时保存到排轴数据（内存中）
      const newButtonData = addSkillButton({
        characterName: draggingState.characterName,
        skillType: draggingState.skillType,
        staffIndex,
        nodeIndex: targetNodeIndex,
        position: snappedPosition,
      });
      
      // ...原有逻辑
    }
  }, [addSkillButton]);
  
  // 保存按钮点击处理
  const handleSaveClick = useCallback(() => {
    const savedData = saveTimelineData();
    // 可以在这里添加保存成功的提示
    alert('排轴数据已保存！');
  }, [saveTimelineData]);
  
  // 渲染时从排轴数据读取按钮
  const renderSkillButtons = () => {
    return selectedCharacters.map((char, staffIndex) => {
      const buttons = getStaffButtons(staffIndex);
      return buttons.map(button => (
        <SkillButtonComponent
          key={button.id}
          button={{
            ...button,
            isSelected: false,
            isDragging: false,
            element: '',  // 需要从角色数据获取
            skillIconUrl: '',  // 需要从角色数据获取
          }}
          size={skillButtonSize}
          onMouseDown={(e) => handleButtonMouseDown(e, button.id)}
          onContextMenu={(e) => handleButtonContextMenu(e, button.id)}
          timelineData={timelineData}  // 传入完整数据用于弹窗显示
        />
      ));
    });
  };
  
  // 在 Toolbar 或其他位置添加保存按钮
  // <button onClick={handleSaveClick}>保存</button>
};
```

---

## 验证要点

1. 每个干员的节点编号独立（1~50, 51~100, ...）
2. 技能按钮数据结构包含完整信息（角色、技能、谱线索引、节点索引、节点编号、位置）
3. 数据结构自动排序（occupiedNodes 和 buttons 都按索引排序）
4. 双击技能按钮弹出居中弹窗，显示按钮完整数据
5. 点击保存按钮时，排轴数据存储到 sessionStorage
6. 控制台输出总数据结构
7. 不影响现有功能（拖拽、磁吸、Buff 搜索等）

---

## 待填充内容（本文档不涉及）

- 技能按钮的删除功能
- 从 sessionStorage 加载数据自动恢复谱线
- 数据导入导出
- 多组谱线的切换显示
- 节点编号的可视化显示
